# 版本对齐记录

我们的版本号走**自有序列**，与社区版（DSH Desktop）的版本号没有绑定关系。
本文件记录「我们的每个版本出自哪个社区版 / 哪个官方内核」，方便日后回溯。

| 我们的版本 | 日期 | 对齐的社区版 | 官方内核 | 内核 commit | 备注 |
|---|---|---|---|---|---|
| **3.0.0** | 2026-09-15 | 2.0.9 | `0.1.5-rc.1` | `183f08e9c6dde7e36cd2318eaee70b0da08fb35e` | **第一个自有版本号**。此前沿用社区号（2.0.9） |

## 规则（必须遵守）

1. **版本号只增不减、不复用**。发布后不再修改已发布版本的代码。
2. **新版本号必须大于上一个已发出的号** —— 客户端的更新检查是拿版本号比较的，号比对方小会被判定为「没有新版本」，更新功能会失效。
   - 具体：`dsh-plugin-desktop/package.json` 的 `version` 是唯一版本源（electron-builder 用它生成产物名 `deepseekharness-<version>-<arch>-Setup.exe`，客户端更新检查也用它）。
3. **每次"对齐"内核或社区版时，在表格里新增一行，不要覆盖历史行。**
4. **不因为社区版发新版就跟着发新版** —— 只有我们自己需要的东西才进（见 `AGENTS.md` 的仓库定位）。
5. 发新版时同步更新 `I:\deepseekharness更新DIY\发布\version.json`，并把安装包拷进 `发布\`。

## 升级号怎么定（建议，非强制）

| 情况 | 例子 |
|---|---|
| 换了官方内核版本 / 打包方式等底层大改 | 主版本 +1（3.x → 4.0.0） |
| 新增功能、改行为、改界面 | 次版本 +1（3.0.0 → 3.1.0） |
| 只改文案、修小 bug | 修订号 +1（3.1.0 → 3.1.1） |
