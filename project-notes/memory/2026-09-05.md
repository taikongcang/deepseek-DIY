# 2026-09-05 工作日志

## GitHub 私有仓库推送核实（deepseekharness DIY 版存档）

### 背景
- 用户想把自己 DIY 改动（改名 + 便携模式 + 中文化等）推到自己的 GitHub 私有仓库存档。
- 另一个对话框已排查：本机 Windows 凭据管理器有 `git:https://github.com` → `taikongcang` 凭据；git 凭据助手要设成 manager 才能读到它。
- 用户把那个结论带回来让我核实。

### 实测核实结论（全部属实，且补了一个它漏掉的事实）
1. **git 凭据助手已是 manager**：`git config --global` 实测 = `credential.helper=manager` + `credential.helperselector.selected=manager`（无需再改）。
2. **凭据有效**：`git ls-remote` 访问 taikongcang 私有仓库成功（exit=0，无弹窗，裸连 GitHub 慢约 3 分钟）。
3. **两个私有仓库都存在**（另一个对话框只查了 test，漏了 dsh-desktop-DIY）：
   - `taikongcang/test`：main 分支，最新提交 `6088eb9997b0426890b64a98cc8bcabf8b99a9b6`
   - `taikongcang/dsh-desktop-DIY`（用户最初说的那个）：main 分支，最新提交 `223a34d33c4e1a604ab2bdd5015170b7660aa784`
4. **本地 `I:\dsh-desktop` 现状**：
   - origin 仍指向 `anywhere-labs/dsh-desktop`（社区原版，未改）
   - 当前分支 `master`（远程私有仓库是 `main`）
   - 本地有 26 个修改文件 + 3 个新增文件（DIY 改动，**全部未提交**）

### 执行结果（已完成，11:54-12:32）
- 用户拍板：目标仓库 `dsh-desktop-DIY`，先列清单过目再动手。
- 清单已过目：26 改 + 3 新增 = 29 文件，5 类（A中文化/B改名/C便携模式/D市场源替换/E依赖锁定），敏感信息扫描干净（无密钥/API key/密码）。
- 执行步骤（全部成功）：
  1. 配置 repo 级署名：`user.name=taikongcang`、`user.email=taikongcang@users.noreply.github.com`（用户选 GitHub 匿名邮箱）。
  2. `git remote add diy https://github.com/taikongcang/dsh-desktop-DIY.git`（保留 origin 指向上游）。
  3. `git add -A` + `git commit`：提交 `568dcecdd7`，29 文件，+433/−140。
  4. `git push -f diy master:main`：**强制推送成功**（远程 main 原只有空壳初始提交 `223a34d`，仅 .gitignore+一行 README，force 覆盖无损），`223a34d...568dcecdd7 master -> main (forced update)`，exit=0。
- 结果：`dsh-desktop-DIY` 的 main 分支现在 = 社区原版完整历史 + DIY commit `568dcecdd7`。

### 备忘
- 本机 git 身份此前为空，已用 repo 级（非 global）配置 taikongcang 身份，只影响 I:\dsh-desktop 这个仓库。
- 裸连 GitHub 慢（ls-remote/push 约 40 秒~3 分钟），但凭据有效、无需弹窗。
