# 2026-09-14 工作日志

（承接 2026-09-13 会话，凌晨 00:02 续）

## 【用户决策】本轮
1. **中文补丁：要做** —— 指截图里"指令"面板那 6 条英文描述的中文化。
2. **绿泡泡（思考过程合并成气泡）：暂缓**，等用户想清楚再说。
3. **新问题**：思考过程中出现英文，能不能在思考过程中**完全转成中文**？

## 【查证】"指令"面板 7 条的文案来源
用户截图（设置 → 指令）共 7 条：`compact / export / feedback / goal / permission / plan / model`，**只有 model 是中文**。

| 指令 | 描述写在哪 | 语言 |
|---|---|---|
| `model` | `dsh-client-ui-model-selection/lib/client.js` | **官方自带中文词典**：`const zh = { "command.description": "选择本会话使用的模型", "option.loadError": "目录加载失败：{message}", "trigger.fallback": "选择模型", "trigger.loading": "正在加载模型…", "trigger.selectAria": "选择模型" }` |
| `compact` | `dsh-command-compact/lib/index.js` | 英文硬编码 `"Compact older conversation history"` |
| `export` | `dsh-session-log-export/lib/index.js` | 英文硬编码 `"Download this Session log as a ZIP archive"` |
| `feedback` | `dsh-command-feedback/lib/index.js` | 英文硬编码 `"record feedback about this session"` |
| `goal` | `dsh-command-goal/lib/index.js` | 英文硬编码 `"set or view the goal for a long-running task"` |
| `permission` | `dsh-permission-presets/lib/index.js` | 英文硬编码 `"Switch the permission preset (sandbox mode + approval policy)"` |
| `plan` | `dsh-plan-mode/lib/index.js` | 英文硬编码 `"Enter or leave plan mode"` |

→ 要中文化 = **给这 6 个官方包各写一个补丁**（走 `patches/`）。已列入 redolist 第八类第 27 项。
→ ⚠️ 这 6 条是按截图列的；实施前应先把"所有已注册指令"全量捞一遍，避免漏。

## 【查证】思考过程（Think）现状补充
- `@deepseek-ai/dsh-client-ui-conversation` **本身有完整 zh/en 词典**（如 `"row.running": "运行中"`，`lib/client.js:6271-6273`；英文词典在 `:6444-6446`）。
- **但思考行的标题 `title: "Think"` 是硬编码字面量**（`lib/client.js:9418`），**没走 `t()` 词典** → 界面切中文也不变。这就是"思考过程中出现英文"的**界面层**来源之一。
- 其余思考行现状（详见 9-13 日志）：默认折叠、点了展开、`ReasoningRow` 每段一个、`data-variant="think"`。

## 【重大发现】官方预留了 `personaPrefix` 配置位 —— 可注入"用中文思考"
- `@deepseek-ai/dsh-system-prompt` 的 `Config`（`lib/index.js:199-205`，新版 0.1.5-rc.1 解压确认）：
  ```js
  static Config = z.object({
    includeHarnessIdentity: z.boolean().default(true),
    includeRuntimeContext: z.boolean().default(true),
    personaPrefix: z.string().default(""),
    personaSuffix: z.string().default(""),
    toolOrder: z.array(z.string()).default(void 0)
  });
  ```
- `personaPrefix` 注册为 `deployment:persona-prefix` section，**order 0**（仅次于 `harness:identity` 的 -1000），即**排在系统提示词最前面**。
- 该包只有一条内置文案：`"You are an AI agent powered by DeepSeek Harness."`
- **官方默认配置**（`@deepseek-ai/dsh-base/package/cordis.patch.yml:463-468`）原文：
  ```yaml
      # The deployment persona is a deployment choice; plan-mode and tool plugins own
      # their own prompt sections.
      - id: system-prompt
        name: '@deepseek-ai/dsh-system-prompt'
        config:
          personaPrefix: ''
  ```
  → **官方留空 + 注释明说"部署人格是部署方的选择"** = 预留给我们填。
- 我们要改，就改**桌面版自己的** `dsh-plugin-desktop/cordis.patch.yml`（配置层，不碰官方包）。
- 用户实际 profile 的装配（`E:\app\deepseekharness\data\profiles\desktop\package.json`）：
  ```json
  "dsh": { "profile": { "bundles": ["@deepseek-ai/dsh-base", "@deepseek-ai/dsh-web-app"], "patchReload": "live" } }
  ```
  该 profile 的 `cordis.yml` 内容是 `[]`（空）。
- ⚠️ 如实结论：改 `personaPrefix` 是**给模型下指令**，能强引导但**不能 100% 保证**模型每句都用中文（模型行为，非代码开关）。
- ⚠️ 注意：`personaPrefix` / `systemPrompt` 字符串在**旧版（2.0.4 / 0.1.2-alpha.1）的 app.asar 里搜不到（0 hit）**，说明该配置项**是 0.1.5-rc.1 新版才有的**（或旧版结构不同）。我们做的是新版，所以可用。

## 【方法备忘·可复用】探测打包版内部代码的三个手段
1. **asar 是二进制归档，ripgrep 默认跳过 → 必须用 Node 按字节搜**：
   ```js
   const buf = fs.readFileSync('...\\app.asar')
   const i = buf.indexOf(Buffer.from('目标字符串', 'utf8'))
   // 打印 buf.slice(i-300, i+300).toString('utf8') 看上下文
   ```
   脚本样例：`C:\Users\50667\AppData\Local\Temp\electron-probe\findstr.js`、`find2.js`。
2. **解析 asar 目录结构（列出包清单）**：
   ```js
   const size = buf.readUInt32LE(12)
   const header = JSON.parse(buf.slice(16, 16 + size).toString('utf8'))
   // header.files['node_modules']['@deepseek-ai'].files → 包名列表
   ```
   实测成功：asar 内共 **223 个 `@deepseek-ai/*` 包**。脚本：`asar-list.js`。
3. **解压官方 tgz（Windows 自带 tar 即可，无需额外工具）**：
   ```powershell
   tar -xzf "<vendor>\deepseek-ai-dsh-xxx-0.1.5-rc.1.tgz" -C "<目标目录>"
   ```
   （bash 沙箱 PATH 时常异常，用 PowerShell 调 tar 更稳。）
4. **PowerShell 输出不回显时**：把结果 `Out-File` 到文件，再用 Read 工具读（`Get-Content -Raw` 有时拿不到，Read 工具最稳）。

## 【用户确认 2026-09-14 00:08】第 29 项 = 模型思考内容
用户明确："我指的是：模型思考内容"。→ 第 29 项定为**走 `personaPrefix` 配置注入中文指令**（改 `dsh-plugin-desktop/cordis.patch.yml`，不打补丁）。
- 我给的建议措辞（待用户定稿）：
  ```
  始终使用简体中文进行思考（reasoning）与回答，包括中间分析步骤、
  工具调用前后的说明。专有名词、代码、命令、文件路径保持原文。
  ```
- 性质如实说明：提示词引导，非硬开关，不能 100% 保证。
- ⚠️ 注意第 29 项**不需要**改 `ReasoningRow.title`（那是界面层的 "Think" 标签，用户这次没要求；如果以后要，归到第四类中文化补丁一起做）。

## 【产出】清单整体重整（`redolist.html` 全量重写，2026-09-14）
按用户要求"把清单整理出来"重写整份文档，结构变为：
- **总览表**：8 行（一~六类 + 增量需求 3 项），含编号区间、规模、状态。
- **一、改名（1-12）** / **二、便携模式（13）** / **三、市场源替换（14-20）** / **四、中文化补丁（21-22）** / **五、升级源（23-26）** / **六、增量需求（27 指令中文化已定 / 28 绿泡泡暂缓 / 29 思考内容转中文已定）** / **七、不用手动改** / **八、执行顺序（8 步）**。
- 修掉了原文档的样式缺陷（`.easy`/`.hard`/`.mid` 三个 class 之前未在 CSS 定义，现已补上）。
- 压缩了第五类（原 5.1-5.9 九小节 → 6 小节），保留全部关键信息（机制、改动、实测证据、命名耦合、目录结构、四条限制）。
- 明确区分："升级重做只需 1 个补丁（第 21 项）"vs"新增 6 个指令补丁（第 27 项）"，避免混淆。
- 文档末尾标注唯一悬空点：**第 29 项中文指令的措辞待用户定稿**。

## 【查证】设置 →「插件」三个标签页各是什么（用户问"插件列表 184"）
用户截图两问：① 图1（含"终端/Agent 循环/Subagent/网页搜索"四张卡片）是什么？是已装的全部插件吗？② 图2（"插件列表 184"，可展开看已启用/已停用）里面所有东西是什么？

**三个标签页的归属（均已解压官方包读 README.zh.md 确认）**：
| 标签页 | 官方包 | 性质 |
|---|---|---|
| **插件配置**（图1） | `@deepseek-ai/dsh-client-ui-settings-plugins` | **可编辑**：卡片式配置 + 保存/重置 |
| **插件列表**（图2，184） | `@deepseek-ai/dsh-client-ui-settings-plugin-inventory` | **只读**：Loader 清单投影 |
| 插件市场 | `dsh-community-market`（我方改过） | — |

**图1「插件配置」= 只有 4 个卡片，不是全部插件**。README 原文点名这四张卡：shell 执行器（`bash`）→ 界面"终端"、agent 循环（`agent-loop`）→"Agent 循环"、subagent 模型选择（`subagent-model-selection`）→"Subagent"、DeepSeek 搜索提供方（`web-search-deepseek`）→"网页搜索"。
- 出现条件（原文）："标签页读取 Host 服务了哪些 settings 命名空间……渲染出来的是**两份账本的交集**：存活 Host 插件注册的命名空间，以及注册在这些键上的卡片。" → **既要插件注册了可配置项、又要有人写了配置界面卡片，两者都满足才出现**。
- 已知限制（原文）：**只有"宿主平面"的插件会出现**；由 agent preset 挂载的插件把配置内联在 preset 的 `agent.cordis.yml`，无法注册 settings 命名空间，因此**不会出现在这一页**（那些值归 preset 编辑器管）。
- 写入机制：卡片本地暂存草稿，用户点保存才写入；保存以"读取时的 revision"设栅，配置在加载后被改过则**拒绝保存**而非覆盖；"重置"恢复部署默认值。

**图2「插件列表 184」= 当前 Loader 实际加载的插件条目数**（不是"装了多少包"）。
- 宿主侧实现：`@deepseek-ai/dsh-host-plugin-inventory`，对外只暴露 Remote `pluginInventory/list`；客户端懒调用（**首次切到该标签页才读**）。
- README 原文："每一行是一个**非组 Loader 条目**：其条目 id、精确模块标识、有效启用状态（含被禁用的祖先组）与当前根 Fiber 阶段。**结构性的 group 行会被跳过**。" → 这就是 184 的定义。
- 卡片阅读：收起时 = 模块短名（主标题）+ 稳定条目 id（次行）+ 启停状态小标签；**已启用条目还有彩色根 fiber 状态圆点**。展开显示：来自哪个预设、运行状态、禁用条件、发现失败原因。
- 分两组：**Agent 预设组合**（按会话提供）在前 + **全局清单**（默认收起）；搜索跨两组。行 key 按作用域限定（`global:`、`preset:<id>:<index>`）。
- **fiber 阶段词汇**：`pending` 等待加载 / `loading` 正在读取 / `active` 正在运行 / `failed` fiber 被拒绝 / `unloading` 正在拆除 / `null` 无存活根 Fiber。
- **只读**（原文）："该清单是供展示与诊断的快照……**不能启用、停用、添加或移除插件**，也不携带历史"；"两个平面都只读"。
- 每次 Settings 挂载或重试只读一份快照，不订阅 Loader 变化、重连不自动重读。

**184 与 asar 里 223 个 `@deepseek-ai/*` 包的区别**：223 = 磁盘上安装的包（含基础库如 cordis/cosmokit/schemastery、含未被加载的）；184 = Loader 实际组合的插件**条目**（跳过分组行）。两者不是一回事。

**方法**：这两个包的文案在旧版 asar 里搜不到（0 hit），最后是**解压新版 vendor tgz 读 README.zh.md** 拿到权威说明的 —— 官方包都自带中英双语 README，查包用途时**优先读 `README.zh.md`**，比读源码快得多。

## 【产出+澄清】官方 265 个插件包的一行用途索引
**产出**：`I:\deepseekharness制作\.workbuddy\plugin-index.txt` —— 265 个官方包（新版 0.1.5-rc.1）各一行「包名 + 官方中文一句话用途」。
**方法（可复用）**：官方每个包的 `README.zh.md` frontmatter 第一行就是 `description: "一句话用途"`。用 Node 脚本 `zlib.gunzipSync` + 手写 tar 头解析（name@0-99、size@124-135 八进制、数据块 512 对齐）批量提取，脚本 `C:\...\electron-probe\pkgdesc.js`。**注意：脚本结果别经 PowerShell 打印（会 UTF-8→GBK 乱码），直接 `fs.writeFileSync(..., 'utf8')` 写文件再用 Read/Grep 读**。

**用词澄清（用户问"你说的官方是哪个"）**：本对话中我说的"官方"**一律指 DeepSeek 官方（`deepseek-ai/deepseek-harness` 内核）**。上面引用的两份 README 均来自官方包 `@deepseek-ai/dsh-client-ui-settings-plugins` / `dsh-client-ui-settings-plugin-inventory`。**唯一例外**：设置里「插件市场」那个标签页是**社区版自研**（`dsh-community-market`），不是官方的。

**另一发现**：截图「插件列表」里的 `include` / `timer` / `hmr` **不是 DeepSeek 的业务插件包**（不在 265 个 `deepseek-ai-*` 包中），它们来自**底层插件框架 Cordis**（asar 包目录里对应 `cordis-plugin-include`、`cordis-plugin-timer`、`cordis-plugin-hmr`、`cordis-plugin-group`、`cordis-plugin-loader`、`cordis`）。界面上 `hmr` 显示"已停用"属正常（生产构建不需要浏览器插件重载）。

## 【查证】"侧边栏的市场入口"与"设置里的插件市场 tab"是同一个东西
用户问：设置→插件里的「插件市场」和外面（侧边栏左下角）那个是不是一样？

**结论：完全一样，同一套界面、同一份数据，只是"外壳"不同。** 证据（读新版 `dsh-community-market/src/client/` 源码）：

`dsh-community-market/src/client/index.ts` 一个包注册了**三个插槽**：
| 插槽 | 组件 | 作用 |
|---|---|---|
| `settings.plugins.tab`（order 20） | `MarketSettingsTab` | 设置里的「插件市场」标签页 |
| `sidebar.footer.action`（order 10） | `MarketLauncher` | 侧边栏左下角那个**按钮**（点击 = `actions.open()`） |
| `shell.overlay`（order 10） | `MarketOverlay` | 上面按钮点开的**全屏浮层** |

**关键实锤**：`MarketOverlay.tsx:8` 从 `MarketSettingsTab.js` 导入 `MarketSurface`，第 53 行 `<MarketSurface showHeader={false} …/>`；而 `MarketSettingsTab.tsx:1114-1120` 的实现就是 `return <MarketSurface …/>`（`showHeader` 用默认 `true`）。

→ **两个入口渲染的是同一个 `MarketSurface` 组件**（定义在 `MarketSettingsTab.tsx:244`）。
- `MarketLauncher` 与 `MarketOverlay` 共享同一个 `marketView` store（控制 open/close）；`MarketSettingsTab` 不传 store（它嵌在页面里，不需要开关）。
- 差别仅在：设置 tab 内嵌（显示自己的标题头）；侧边栏入口弹出**模态浮层**（`role="dialog" aria-modal="true"`，ESC / 点遮罩 / 右上角 X 关闭，`showHeader=false`）。
- `MarketSurface` 有 4 个视图：`discover`（发现）/ `installable`（可安装）/ `installed`（已安装）/ `sources`（来源）。数据走同一套 api：`readMarketCatalog` / `readMarketInstallable` / `readMarketInstallations` / `readMarketState` / `mutateMarketSource` 等。

**归属提醒**：这个市场是**社区版自研**（`dsh-community-market`，非 DeepSeek 官方），而且正是我们清单**第三类"市场源替换"要改的对象**（换成 Awesome DSH Plugins 源）。

**【补充·已核实】"设置→插件"三个 tab 的注册者与归属**（用户 2026-09-14 求证）：
| tab | 注册 id | order | 注册者包 | 归属 |
|---|---|---|---|---|
| 插件配置 | `configurable` | **0** | `dsh-client-ui-settings-plugins` | **DeepSeek 官方** |
| 插件列表 | `all` | — | `dsh-client-ui-settings-plugin-inventory` | **DeepSeek 官方** |
| 插件市场 | `community-market` | **20** | `dsh-community-market` | **社区版** |

- 证据：`settings-plugins/package/lib/client.js:1773-1777` → `ctx.slots.inject("settings.plugins.tab", …)` 注册 `id: "configurable", order: 0`。
- **插槽 `settings.plugins.tab` 本身由官方 `dsh-client-ui-settings` 声明**（见 settings-plugins README"进一步探索"一节）。
- → 精确说法：**整个"插件"设置分区（入口 + 标签栏 + 前两个 tab）都是官方的；社区市场只是往官方预留的扩展点插槽里挂了自己的一页**。这也意味着**我们以后也能往这个插槽注册自定义设置页**。

## 【用户需求 2026-09-14 00:39 → 清单第 30 项】去掉"设置→插件"里的「插件市场」标签页
理由（用户）：外面侧边栏已经保留了完整的市场入口，设置里这个是重复的。

**查证结论：可做，且不影响外面的市场。** 依据 —— `dsh-community-market/src/client/index.ts` 把三个位置**分三次独立注册**：
| 行 | 插槽 | 组件 | 处置 |
|---|---|---|---|
| `:32-39` | `settings.plugins.tab` | `MarketSettingsTab` | **删除** |
| `:40-47` | `sidebar.footer.action` | `MarketLauncher` | 保留 |
| `:48-55` | `shell.overlay` | `MarketOverlay` | 保留 |

**改动清单（编号 30）**：
- **30a** `dsh-community-market/src/client/index.ts`：删 `:32-39` 注册块（8 行）+ 删 `:13` 的 `import { MarketSettingsTab }`（否则未使用导入告警）。
- **30b** `tests/client-index.spec.ts`：`:68` 标题去 "settings tab"；`:77-81` injections 列表去掉 `'settings.plugins.tab'`；`:90` `toHaveLength(3)`→`2`；`:91-110` registrations 删第一条；`:111` 解构 `[settings, launcher, overlay]`→`[launcher, overlay]`；`:112-113` 删对 settings 的断言。
- **30c** `tests/browser-plugin.spec.ts`：`:6` import 清理；`:129-171` 整个测试去掉 settings 相关断言（`:131/:137/:140/:143/:146/:150/:153/:157/:159/:162/:164/:167`）；`:176` 名字数组去掉 `'settings.plugins.tab'`，删 `:185`。

**明确不动**：
- `src/client/MarketSettingsTab.tsx` —— 真正的市场界面 `MarketSurface` 定义在此（`:244`），`MarketOverlay` 依赖它渲染，**删了外面就废** → 只删注册、不删文件。
- `tests/market-settings-tab.spec.tsx` —— 测的是组件本身渲染，组件保留，测试不动。

**预期效果**：设置→插件只剩「插件配置 / 插件列表」两个 tab；侧边栏「插件市场」入口照旧弹全屏浮层。
- redolist.html 已同步：元信息改"30 项改动（26 重做 + 4 增量）"、总览表加第 30 项行、新增 6.4 节、执行顺序第 7 步加 30。

## ⚠️【自查发现重大疏漏·已修复】重写 redolist.html 时漏掉了 requirements.html 的三项需求
**用户问"侧边栏底部排列样式修改在清单里吗" → 一查，不在。** 进一步把两份文档做完整比对，发现**漏的不止一项**：

| requirements.html 原项 | 内容 | 当时状态 | 在旧 redolist 里？ |
|---|---|---|---|
| **B1** | 删除废弃代码（`_deprecated/`）+ README 宣传图 | 待执行 | ❌ **漏** |
| **B2** | 侧边栏底部「插件市场」「设置」改成卡片样式 | 待执行 | ❌ **漏**（用户本次问的就是这项） |
| **C1** | 会话目录命名改成"日期+时间"（含 3 个待拍板选项） | 待拍板 | ❌ **漏** |
| A2 | 局域网访问为什么置灰（只查不改） | 已查清 | ❌ 未收录（属结论非改动） |
| D1 | 窗口标题与产品名不一致 | 待表态 | ❌ 未收录（用户 9-13 已定"名字按现状"≈不改） |

**根因（如实记录）**：`redolist.html` 是我从"升级重做 29 文件 + 后续增量需求"的角度**另起炉灶**重建的；`requirements.html` 是更早的**需求总账**（含与升级无关的独立需求）。我后来只在 redolist 内部演进，**没有回头跟 requirements 对齐**，导致三项掉队。这违反了铁律第 3 条（漏掉必须主动报告）与第 6 条（间断自查）。

**已修复**：三项全部并入 redolist 第六类（**31 = 删废弃代码+宣传图 / 32 = 侧边栏卡片化 / 33 = 会话命名**），并在新版工作副本上**逐项重新核实**：
- B1：新版 `_deprecated/` 仍 25 个文件（无引用）；`assets/` 仍 15 张 —— 推广类 8 张（`community-*` 5 + `sponsors/` 3）可删，**截图类 7 张要保留**（README 在用）。
- B2：新版 `dsh-community-market/src/client/MarketLauncher.tsx` 仍在（自有代码直接改）；「设置」入口在官方包 `dsh-client-ui-settings`（要打补丁，行号待在新版重定位）。
- C1：新版 `dsh-api-session-controller` 包仍在（`0.1.5-rc.1`），但**具体行号需重新定位**（旧调查基于 0.1.2-alpha.1 的 `:577`/`:692`）。

**同时归位两项**：A2（局域网置灰结论）与 D1（名字保持现状≈不改）写入 redolist 新的「七、不改的 / 已查清的」7.1 节，两份文档内容至此合并完毕。

**教训（要长期遵守）**：
- **需求清单只保留一份权威**（redolist.html）；`requirements.html` 已全量并入，以后不再各自演进。
- **每次重写/大改清单后，必须回头跟旧清单做一次逐项比对**，确认零遗漏，再交给用户。
- redolist 现为 **33 项**（26 重做 + 7 增量），两个悬空点：29 项措辞、33 项三个选项。

## 【方案变更·重要】第 32 项改为"只改排列方向"，不是"卡片化"
用户 2026-09-14 00:57 提出：**保持图中每一项的大小样式不变，只把"上下排列"改成"左右排列"**；以后要加新入口也按左右排列加。
→ **这跟原 requirements B1/B2 里的"改成卡片样式（上图标/中标题/下描述）"是两回事**，必须按新意图改写清单第 32 项。用户还没最终点头，需再确认一次。

**查证结果（新版 0.1.5-rc.1 解压 `dsh-client-ui-sidebar` 确认）**：
- 结构（`lib/client.js:292-301`）：
  ```jsx
  <div className="footArea">              // 底部总区
    <div className="footerActions" />     // 插槽 sidebar.footer.action（←「插件市场」）
    <div className="settingsArea"  />     // 插槽 sidebar.settings（←「设置」）
  </div>
  ```
- **上下排列是官方 CSS 写死的**（同一文件内 SidebarRoot.module.css）：
  ```css
  .footArea { flex-direction: column; flex: none; display: flex; }   /* ← 根因 */
  .settingsArea, .footerActions { flex: none; width: 100%; min-width: 0; }
  .footerActions { display: flex; }
  .collapsed .footArea { align-items: center; }
  .collapsed .settingsArea, .collapsed .footerActions { justify-content: center; width: auto; display: flex; }
  ```
- **归属分辨**：布局 = **DeepSeek 官方**（`dsh-client-ui-sidebar`）；「设置」= 官方（`dsh-client-ui-settings` 注册 `sidebar.settings`）；「插件市场」= **社区**（`dsh-community-market` 注册 `sidebar.footer.action`）。
- **改法**：给官方包 `dsh-client-ui-sidebar` 打补丁，把 `.footArea` 的 `flex-direction: column` 改 `row`，并把两个子块的 `width: 100%` 改 `auto`（否则 row 方向各自撑满）。
- **⚠️ 必踩的坑**：collapsed（56px）态下两个图标并排会超出宽度 → 收窄态必须单独处理（保持上下排列，或缩小图标/只留一个）。这是本方案最主要的风险点。
- 另注：`footerActions` 与 `settingsArea` 是**两个独立插槽区域**，不是同一列表的两项。

### 【更正我的理解】用户澄清：新增入口是"往上一行一行加"，不是往旁边挤
用户 2026-09-14 01:02 指正：现在只有两个入口这一行**左右排列**（我理解对了）；但**"以后新增入口"我理解错了** —— 不是横向往旁边排，而是**每两个一行、往上叠加**。用户给了示意图：上面一行两个红框（新增占位），下面一行是现有的「插件市场」「设置」。

→ **正确理解：底部区域是一个"两列网格"，从下往上增长**：
```
[新增] [新增]   ← 以后加的（先加的在下面、后加的在上面）
[插件市场] [设置]  ← 现在这一行（最底）
```
- 实现思路：容器 `flex-direction: row; flex-wrap: wrap-reverse;`，每个入口项 `flex: 0 0 50%`（wrap-reverse 让新行出现在**上方**）。
- **仍是结构级改动**（不只是 CSS）：现在两个入口被包在 `footerActions` / `settingsArea` 两个 100% 宽的容器里；要"两列可增长"，需让入口项成为该容器的直接子项。
- 收窄 56px 问题**依旧存在**（两个图标并排放不下），需单独处理。
- **用户 2026-09-14 01:05 已确认"两列、从下往上长"理解正确** → 已改写清单第 32 项（6.6 节全量重写、总览表该行、执行顺序第 7 步文案）。第 32 项标题从"两个入口改成卡片样式"改为"侧边栏底部入口改成**两列、从下往上增长**"。
- **收窄态已定（2026-09-14 08:32 用户认可）**：**收窄时保持上下排列，只有展开时才左右排**。→ **第 32 项至此全部定稿**（清单 6.6 节已把该行从"待你确认"改为"✅ 已定"）。
- 剩余悬空点只剩两个：**第 29 项**（"用中文思考"指令的措辞）、**第 33 项**（会话命名三选项）。

## 【用户决策 2026-09-14 08:35】第 29 / 33 项定稿
**第 29 项**：采用我给的措辞建议 ——
```
始终使用简体中文进行思考（reasoning）与回答，包括中间分析步骤、
工具调用前后的说明。专有名词、代码、命令、文件路径保持原文。
```
**第 33 项**：① 日期格式 = **`2026-09-09-233825`（带横杠）**；② 随机后缀 = **8 位**；③ **中文转义问题一起修复**。
→ 最终形态：`session-2026-09-09-233825-a1b2c3d4`。

### 【查证】中文转义的根因已在新版定位
- **第 33 项要打「两个」补丁**（分属两个官方包）：
  1. `dsh-api-session-controller` —— 会话编号生成（日期时间 + 8 位随机）；行号需在新版重定位（旧调查基于 0.1.2-alpha.1 的 `:577`/`:692`）
  2. `dsh-session-persistence-jsonl` —— **中文转义在这**：`projectKey()` 函数只保留 `[A-Za-z0-9._-]`，**其余字符一律转成 `~XXXX`**（UTF-16 码元十六进制），源码原文 `else { readable += "~" + code.toString(16).toUpperCase().padStart(4, "0") }`（解压 0.1.5-rc.1 确认，约 `lib/index.js:852-893`；`encodeSegment()` 是给 session id 用的，**与中文无关**）。
  - 返回形态：`` `--${(readable.replace(/^-+/, "") || "root").slice(0, 251)}--` ``（`projectKey` 末尾）。
- **⚠️ 新发现的后果**：改算法后**旧目录名与新算法不匹配** → 程序按新名找不到 → **旧会话在界面上消失**（数据文件仍在磁盘，只是读不到）。
- **兼容兜底：用户 2026-09-14 08:44 已定「加」**。做法（已写入清单 6.7）：
  1. 新增一个"旧算法"函数，把原 `projectKey()` 的 `~XXXX` 转义逻辑**原样保留**；
  2. `projectDir(root, cwd)`（`lib/index.js:901-904`，当前是纯函数 `join(root, projectKey(cwd))`）里加兜底：先算**新名** → 若新名目录不存在、而旧算法目录**存在**，则**沿用旧目录**；两者都无则用新名（新建）。
  3. 实现时需引入文件存在性检查（`fs.existsSync` 或等价），并注意该函数是同步纯函数、被 `sessionDir` → `generationLogPath` 等链路频繁调用。
  - 效果：老会话目录保持原样照样可读；新建项目用中文名。
- **清单至此 33 项、零悬空点**（2026-09-14 08:44）。下一步进入"出详细施工方案"阶段。

## 【诊断】`npx @deepseek-ai/dsh web` 打开会话报 SessionFormatUnsupportedError（2026-09-14 08:46）
用户报错：历史加载失败，`session-c660ad16-80e0-4fa3-a30d-19966cecb9f6` 含事件类型 **`model/selection`**（seq 249），"本机束未知且未标记为可忽略；拒绝解释日志"。

**用 skill `dsh-session-log-inspect` 诊断（流程完全对上）+ 自行比对版本，根因已 100% 坐实**：

| 环节 | 实测结论 |
|---|---|
| 日志头部 | `{"type":"session","version":0,…,"cwd":"I:\\deepseek-harness","agentPreset":"standard"}` → **v0 格式**；创建 2026-09-01 16:46，末事件 2026-09-02 06:21 |
| 日志规模 | **1803 个 zstd 帧**（多帧拼接）、解压后 1,868,409 字节、**2641 行事件** |
| 触发事件 | `model/selection` 共 3 条：seq249 `deepseek-v4-flash-vision-exp/high`、seq29394 `deepseek-v4-pro/high`、seq29395 `deepseek-v4-pro/max` |
| `ignorable:true` 的事件 | **0 条** → 无豁免通道 |
| **当前读取方** | **`E:\app\Node\node_global` 的全局 dsh = `0.1.1-rc.2`**（`node_modules\@deepseek-ai\dsh\package.json:4`） |
| 0.1.1-rc.2 认识它吗 | ❌ `dsh-session\lib\types\known-event-types.js` 的 47 项清单里**无任何 `model/` 开头项** |
| 0.1.5-rc.1 认识它吗 | ✅ `known-event-types.js:44` **有 `'model/selection'`** |
| 0.1.2-alpha.1（桌面版内核） | 是它写的（2026-09-01 期间桌面版继承 `DSH_HOME=I:\deepseek-harness\.dsh`）→ 必然认识 |

**根因**：`npx @deepseek-ai/dsh web` **没有下载最新版**，而是**复用了本机全局已装的 `0.1.1-rc.2`**（npx 行为：本机有同名包就直接用）。而该会话是**更新的 `0.1.2-alpha.1`** 写的 → 老版本不认识 `model/selection` → 按官方设计**拒绝加载整个会话**（源码注释原话：静默跳过一条必需事件会重建出**错误的会话**，所以宁可拒绝）。

**三条出路**：① 换认识它的版本打开（桌面版 0.1.2-alpha.1 即可）；② `npm i -g @deepseek-ai/dsh@latest` 更新全局；③ 放弃该会话。⚠️ **绝不逐字节改日志**（skill 明确定义为伪造数据）。

**对本项目的意义（重要）**：我们马上要升到的 **`0.1.5-rc.1` 认识 `model/selection`** → **升级后这些老会话能正常读**，印证了"官方自带会话格式升级器 v0→v1→v2→v3"。

**方法备忘**：`dsh-session-log-inspect` skill 的脚本可用（`node scripts/decode-dsh-session.js <zstd> <jsonl>` → `node scripts/probe-dsh-session.js <jsonl>`），需 Node ≥ 22.15（用 managed 22.22.2 即可）。**PowerShell 下脚本输出会乱码，建议 `Out-File` 后用 Read 工具读**。

## 【核查】目录存放实况（2026-09-14 10:06）
| 内容 | 位置 | 现状 |
|---|---|---|
| 本次修改工作副本（源码，就地改） | `I:\deepseekharness9-13\9-13deepseekharness最新更新修改` | ✅ 存在 |
| 社区版原样参照（只读） | `I:\deepseekharness9-13\9-13DSH-desktop最新更新一套` | ✅ 存在 |
| 打包产物 | 工作副本 `dsh-plugin-desktop\dist\` | ⏳ 未生成 |
| 更新源（自动更新用） | `I:\deepseekharness更新DIY\发布\` | ❌ **目录不存在**（用户 9-13 约定该路径"以后、不含本次"才用） |
| 清单/文档/记忆 | `I:\deepseekharness制作\.workbuddy\` | ✅ |
- 另：`I:\deepseekharness制作\deepseekharness重做` 是**空目录**（疑似遗留）。
- **⚠️ 待用户确认**：**本次**打包产物的存放位置（A 留工作副本 dist / B 现在就建 `I:\deepseekharness更新DIY\发布\` / C 指定别处）。这是新出现的待定项。

**关于"报错会不会解决"的准确答复**：会 —— `0.1.5-rc.1` 的 `KNOWN_SESSION_EVENT_TYPES` 含 `model/selection`。且用户 `DSH_HOME` 已永久设为 `I:\deepseek-harness\.dsh`，而便携模式设计是"**尊重显式设置的 DSH_HOME**，只改没人指定时的兜底" → 新版仍会读该目录，老会话会被加载且能读。
⚠️ 但该日志是 **v0 格式**，新版读它要走官方格式升级器（v0→v1→v2→v3），**可能改写/生成新文件**。

## 【用户决策 2026-09-14 10:14】存放位置与执行约定（全部定稿）
1. **本次源码**（就地改）：`I:\deepseekharness9-13\9-13deepseekharness最新更新修改`
2. **本次打包产物输出**：`I:\deepseekharness9-13\9-13deepseekharness更新打包`（**需新建**）
3. **更新源**：`I:\deepseekharness更新DIY` **本次不创建**（下次统一建）—— 但**代码本次就改**，地址指向 `file:///I:/deepseekharness更新DIY/发布/…`。
   - 理由（已向用户说明）：不改代码的话，更新地址仍指向社区版官网 → 会下载**社区版原版包**，一装就冲掉我们的改名/便携/中文化/市场源。改完后最坏只是"更新检查静默失败"，**不会误装**。
4. **不备份会话数据** —— 用户明确："这次作为**真正第一次修改我们自己的东西**"，不备份 `I:\deepseek-harness\.dsh`。（风险已如实告知：格式升级可能改写文件，出错可能读不出旧会话。）
5. 已清理确认：`I:\deepseekharness制作\deepseekharness重做` 是**空目录**（遗留，未处理）。

**redolist.html 已同步**：元信息框改为三条路径（本次源码 / 打包产物输出 / 社区版参照）；第五类加"本次只改代码、目录下次建"的范围说明框；第七类新增 **7.2 执行约定**（不备份 / 产物输出 / 更新源目录）。

**→ 清单完全定稿，等待进入"出详细施工方案"阶段。**

## 【产出】第 1 项（改名）施工方案：`I:\deepseekharness制作\.workbuddy\plan-01-rename.html`
用户 2026-09-14 10:16 说"开始第 1 项（改名）"→ 按铁律**先出方案、等点头再动手**（尚未改任何代码）。

**方案核心 = 替换规则表（照搬旧版 ourdiff.txt 的实测改法）**：
| # | 类别 | 旧值 | 新值 |
|---|---|---|---|
| 1 | 产品名（系统层） | `DSH Desktop` | `deepseekharness`（无空格） |
| 2 | appId | `ai.deepseek.dsh.desktop` | `deepseekharness.diy` |
| 3 | 窗口标题 | `DeepSeek Harness Desktop`（`src/index.ts:482`） | `lisa` |
| 4 | 标题栏显示字（`DesktopFrameTitlebarView.tsx:178`） | `DSH Desktop` | `lisa` |
| 5 | 界面文案（中英） | `DSH Desktop` | `deepseek harness`（**带空格**） |
| 6 | 设置页大标题（**特例**） | `DSH Desktop 设置` / `DSH Desktop settings` | `deepseekharness设置` / `deepseekharness settings`（无空格） |
| 7 | 终端名称 | `DSH Terminal` / `DSH 终端` | `DIY终端` |
| 8 | 产物文件名 | `DSH-Desktop-${version}-…` | `deepseekharness-${version}-…` |

**涉及文件（分 4 组）**：
- **A 身份/打包（6）**：`src/product-identity.ts`(2 处，仅 stable 段) / `dsh-plugin-desktop/package.json`(5) / `scripts/verify-win-installer.ts`(2) / `verify-win-portable.ts`(4) / `src/bin.ts`(5) / `src/index.ts`(3)
- **B 界面文案（10）**：`client/desktop-settings-locales.ts`(~16) / `native-dialog-copy.ts`(~20) / `desktop-terminal.ts`(7) / **`recovery-copy.ts`(2，新版新增)** / `tray-locale.ts`(2) / **`startup-recovery-window.ts`(1，新版新增)** / `client/DesktopFrameTitlebarView.tsx`(1) / `client/directory-picker.ts`(4) / `desktop-boot-recovery.ts`(1) / `desktop-data-directory.ts`(1)
- **C 测试（连带）**：`package.spec.ts` / `verify-win-installer.spec.ts` / `verify-win-portable.spec.ts` / `bin.spec.ts` / `desktop-cli.spec.ts` / `client-desktop-settings.spec.ts` / `desktop-copy.spec.ts` / `desktop-installer-quit.spec.ts` / `desktop-runtime-environment.spec.ts` —— **实施策略：先跑测试看哪些红，再逐条对齐**
- **D 不改**：代码注释；`desktop-installation-id.ts` 的 HTTP 头名 `X-DSH-Desktop-Installation-Id`（协议字段）；README/docs；`build/assistedMessages.yml`、`installer.nsh`（**涉安装向导界面，实施前需先读再报**）

**回退方式**：工作副本带完整 git 历史，改动均未提交 → `git diff` 查、`git checkout -- <路径>` 撤、`git checkout -- .` 全撤。

**⚠️ 已向用户强调**：改完后同一软件在不同地方显示**四种名字**（系统 `deepseekharness` / 窗口 `lisa` / 文案 `deepseek harness` / 终端 `DIY终端`），这是用户 9-13 定的"暂不统一"。

### redolist.html 已同步
6.2 措辞框改为"✅ 措辞已定"；6.7 全节重写（拆成"下层会话名"+"上层项目名-中文转义"两张表，含根因/代码原文/后果/待补问）；总览表第 33 项改"2 个补丁 / ✅ 已定"；文末确认框改为"29/32/33 已全部定稿，只剩中文转义兼容兜底一问"。

## 【用户改口径 + 执行】第 1 项（改名）已实施（2026-09-14 10:27）
用户新口径（**覆盖原方案第 3、7 条**）：
> ① 桌面任务栏显示的 `lisa` 改为 `deepseekharness`；② **`lisa` 只需要留在"图1 红框处"**（= 窗口内标题栏）；③ **终端改为 `R9`**。并说"可以修改了"。

### 三处关键位置的代码定位（新版 0.1.5-rc.1 实测）
| 位置 | 文件:行 | 说明 |
|---|---|---|
| **任务栏**（= OS 窗口标题） | `src/index.ts:482` `windowTitle` | 经 `window-options.ts:23` → `title: spec.windowTitle`（win32）传给 BrowserWindow；另 `electron-shell-generation.ts:278` 赋给 `accessibleTitle` |
| **窗口内标题栏**（用户说的"红框"） | `src/client/DesktopFrameTitlebarView.tsx:178` `<span className="dshDesktopFrameProduct">` | 扩展模式的标题栏；兼容模式另有 `src/native-ui/compatibility-chrome/main.tsx:41`（同一视觉位置，一并改 lisa） |
| **终端窗口标题** | `src/desktop-terminal.ts:639` `start "DSH Desktop"` | Windows 控制台窗口标题由 `start "<title>"` 决定；另三套脚本欢迎语 `:343/:386/:412` |

### 实施方式（可复查、可重跑）
| 脚本 | 作用 |
|---|---|
| `I:\deepseekharness制作\.workbuddy\rename-apply.mjs` | 文案类批量替换：**显式规则表**逐行处理，跳过注释行、跳过含 `DSH Desktop Beta` 的行；支持干跑（默认）/ `--write` |
| `rename-tests.mjs` | 测试断言对齐：只改断言/期望值，**纯示例路径不动**（如 `/Applications/DSH Desktop`）；跳过含 `X-DSH-Desktop-` 的行 |
| `rename-audit.mjs` | 查漏：列出 src 中所有**非注释行**仍含旧名的位置，逐条判定该改还是该留 |

### 最终结果
- **改动规模：38 个文件，+210 / −210 行**（全部未提交 → `git checkout -- .` 可全撤）。
- A 组（身份/打包）：`product-identity.ts`、`package.json`(5 处)、两个 verify 脚本、`bin.ts`、`index.ts`、`build/assistedMessages.yml`(安装向导三语文案)。
- B 组（文案/标题栏）：`DesktopFrameTitlebarView.tsx`、`compatibility-chrome/main.tsx`、`desktop-terminal.ts`、`desktop-settings-locales.ts`、`native-dialog-copy.ts`、`recovery-copy.ts`、`setup-wizard-copy.ts`、`tray-locale.ts`、`notifications.ts`、`directory-picker.ts`、`desktop-boot-recovery.ts`、`desktop-data-directory.ts`、`workspace-admission.ts`、`windows-volume-diagnostics.ts`、`update-lifecycle.ts`、`update-download.ts`、`startup-recovery-window.ts`、4 个 `native-ui/*.html` 标题。
- C 组（测试）：`package.spec.ts`、`verify-win-installer/portable.spec.ts`、`electron-runtime.spec.ts`(41 处)、`desktop-terminal.spec.ts`、`desktop-copy.spec.ts`、`client-desktop-settings.spec.ts`、`update-download.spec.ts`、`bin.spec.ts`、`installer-messages.spec.ts`。
- **src 残留 8 处，全部有意保留**：协议头 `X-DSH-Desktop-*`(5)、beta 身份(2)、beta 提示文案(2)… → 精确为：`desktop-installation-id.ts:9` / `update-checker.ts:13,16` / `update-download.ts:24`（协议头）、`product-identity.ts:12` + `native-dialog-copy.ts:87,148`（beta）、`lan-https-certificate.ts:22`（CA CN，**待用户决定**）。

### 两个重要安全核实
1. **改名不破坏安装器的"关闭旧进程"逻辑**：`patches/app-builder-lib@26.15.7.patch` **没有硬编码 exe 名**（只改 macOS keychain 逻辑）；`build/installer.nsh` 用的是 electron-builder 的 `${APP_EXECUTABLE_FILENAME}` 变量 → 自动跟随 productName。✅
2. **`build/assistedMessages.yml` 是安装向导界面文案**（原计划 D 组"实施前先读再报"）→ 已读：确实是安装进度提示（en/zh_CN/zh_TW），属用户可见 → 已改为 `deepseekharness`，并同步 `tests/installer-messages.spec.ts:12`。

### 遗留待用户决定（2 项）
1. `src/lan-https-certificate.ts:22` `CA_COMMON_NAME = 'DeepSeek Harness Desktop Local CA'` —— 改它会让用户已信任的旧证书对不上；且本机没用局域网访问。
2. `patches/dsh-client-ui-directory-picker-browse@0.1.5-rc.1.patch` 里两条 `"DSH Desktop … unavailable"` 报错 —— 改补丁会动到安装时的补丁校验（需重新 `yarn install`），换来的只是一条报错文案。

### ⚠️ 未验证（如实记录）
本机工作副本**没有 `node_modules`**（clone 不带），**测试与构建都跑不了**。语法/类型/测试**需用户在 9-13 那台机器上跑 `yarn install` → `yarn build` → 测试**才能确认。已在此前会话记录"需要手动 PowerShell 就交给用户"的铁律，未自试。

### 产出
`I:\deepseekharness制作\.workbuddy\plan-01-rename.html` 已从"待审方案"改写为**"已执行报告"**（含最终替换规则表、38 文件清单、D 组不改名单、验证方法、回退方法、两项待决）。

## ⚠️【工具教训·当场发现并修复】同一条消息里对「同一个文件」发多条 Edit 会互相覆盖
更新 `redolist.html` 时，我在一条消息里发了 **2 条 Edit 改同一个文件**（改总览表第 59 行 + 改第一节 h2note）。
结果：两条都返回"成功"，但**第 1 条被第 2 条冲掉了**（第 2 条基于旧内容读改写，把第 1 条的成果覆盖）。是我事后 grep 复查才发现。

- **规则（今后遵守）**：**同一个文件的多次修改，必须分多次消息顺序发**；只有"不同文件"才可以在一条消息里并行。
- 本次已当场补回被冲掉的那条改动并 grep 确认两条都在。
- 同类历史教训：本会话早先还多次出现"Edit 的 old_string 误覆盖小标题"（改 memory 时手滑），根因相同 —— **改完必须复查**。

## 【清单变更】新增第 34 / 35 项（待拍板·暂缓），总数 33 → 35
用户 2026-09-14 10:58："先记下两个需要拍板的事情后续讨论"。已写入 `redolist.html`：
- **34** `src/lan-https-certificate.ts:22` 的 `CA_COMMON_NAME = 'DeepSeek Harness Desktop Local CA'` —— 局域网 HTTPS 自签根证书名，改它会让已信任的旧证书对不上；我倾向**不改**。
- **35** `patches/dsh-client-ui-directory-picker-browse@0.1.5-rc.1.patch` 里两条 `DSH Desktop … unavailable` 报错 —— 动补丁会改变安装时的补丁校验（需重跑 `yarn install`），收益只是一条低可见度文案；我倾向**不改**。
- 已加入总览表（标记 ⏸ 暂缓）+ 文档顶部新增"待拍板·暂缓"说明框；元信息行改为 "35 项 / 第 1 项已执行完"。

## ⚠️【重大发现·阻塞第 2 项】本机 `DSH_HOME` 仍设置着 → 已定方案（改兜底值）在这台机器上不会生效
准备动手第 2 项（便携模式）前核查环境，实测：

```
进程环境 DSH_HOME = I:\deepseek-harness\.dsh
HKCU\Environment → DSH_HOME = I:\deepseek-harness\.dsh   （用户级持久变量，仍生效）
```

**后果**：已定方案是"**尊重显式设置的 `DSH_HOME`**，只改没人指定时的兜底值"（redolist 二、第 13 项）。但本机 `DSH_HOME` 是**显式设置**的 → 兜底分支根本不会走到 → **打包后仍会读 `I:\deepseek-harness\.dsh`，便携模式形同没做**。

**新版代码结构（已读实）**：
- `src/main.ts:416` `const desktopUserDataDir = app.getPath('userData')` —— 全文件最早的 userData 读取点，redirect 必须在此**之前**。
- `src/main.ts:689` `const fallbackHome = resolveDshHome()`；`:690` `const defaultHome = resolve(defaultDshHome())`；`:691` `const fallbackSource = process.env.DSH_HOME === undefined ? 'default' : 'environment'`。
- `resolveDshHome(configured, env)`（官方 `@deepseek-ai/dsh-home-paths/lib/index.js:73-76`）优先级：**显式 configured > `$DSH_HOME`（trim 非空）> `~/.dsh`**。
- `resolveDesktopDataDirectory(userDataDir, fallbackHome, fallbackSource)`（`src/desktop-data-directory.ts:229-250`）：若存在用户选过的数据目录 state → 用 state（source='desktop'）；否则用 `fallbackHome`，source=传入值。
- `defaultHome` 用途：`main.ts:963/970/980` —— 恢复助手里"平台默认目录"的参照与"默认目录是否存在"判断。
- `src/main.ts:7` 已导入 `dirname, join, resolve`，**无需改 import**。

**旧版（9-13 已验收）的做法**（`ourdiff.txt:790-798`）——**无条件覆盖**：
```ts
if (app.isPackaged) {
  const exeDir = dirname(process.execPath)
  const dataDir = join(exeDir, 'data')
  process.env.DSH_HOME = dataDir
  app.setPath('userData', join(dataDir, 'desktop'))
}
```
注释原文："Unconditional on purpose: a system-level DSH_HOME must not leak into the desktop build."

**三条路（已向用户提出，等一句话拍板）**：
- **A 按已定方案**（尊重 `DSH_HOME`）→ 本机不会便携。
- **B 旧版做法**（打包时无条件强制便携，忽略系统 `DSH_HOME`）→ 便携真正生效；官方"恢复助手切换数据目录"仍可用。**我推荐这个**。
- **C 折中**：默认强制便携 + 留一个显式开关（如 `DSH_DESKTOP_PORTABLE=0`）可退回官方行为。

⚠️ 另注：若采用 B，`fallbackSource` 会因 `DSH_HOME` 被赋值而变成 `'environment'`（而非 `'default'`），需决定是否在赋值前先取原值，以便恢复助手正确显示"来源：默认/环境变量"。

## 【执行】第 2 项（便携模式）已完成（2026-09-14 11:05）
用户拍板：**选 B（打包时无条件强制便携）** + **`fallbackSource` 保持 `'default'`**。

### 改动：`src/main.ts` 两处，+12 / −2 行（diff 存 `.workbuddy/item02-diff.txt`）
**① `start()` 最开头（必须早于 `main.ts:416` 的 `app.getPath('userData')`）**：
```ts
// Portable data directory: keep the DSH home and the Electron user data beside
// the installed executable, so a reinstall always starts fresh and every piece
// of user data follows the install directory. Deliberately unconditional: a
// system-level DSH_HOME must not drag the desktop build onto another drive.
// The Recovery Assistant's data-directory switcher still overrides this default.
const portableDataDir = app.isPackaged ? join(dirname(process.execPath), 'data') : undefined
if (portableDataDir !== undefined) {
  process.env.DSH_HOME = portableDataDir
  app.setPath('userData', join(portableDataDir, 'desktop'))
}
```
**② 原 `:690/:691`**：
```ts
const defaultHome = portableDataDir === undefined ? resolve(defaultDshHome()) : resolve(portableDataDir)
const fallbackSource = portableDataDir !== undefined || process.env.DSH_HOME === undefined
  ? 'default'
  : 'environment'
```
- `fallbackHome`（`:699` `resolveDshHome()`）**不用改** —— 它读 `process.env.DSH_HOME`，已被 ① 赋值。

### ⚠️ 一个中途自我纠错（要记住的推理）
我最初还改了 `:436` 的 `const inheritedDshHome = process.env.DSH_HOME` → 改成引用"覆盖前的原值"。
**后来撤回**，理由：`inheritedDshHome` 的真实语义是"**Safe Mode 动手之前**的值"（它在 `:515-516` 用于退出安全模式时恢复），
而便携版里那个值**本来就该是便携目录**；把系统旧的 `I:\deepseek-harness\.dsh` 恢复回去反而是错的。
**撤回的附带好处**：`tests/package.spec.ts:103` 断言了这行源码原文 `'const inheritedDshHome = process.env.DSH_HOME'`，撤回后**测试不用动**。
→ **教训：改自己以为"更语义化"的代码前，先查它被谁用、有没有测试锁字符串。**

### 连带影响核查（未发现其它问题）
- `tests/desktop-installer-quit.spec.ts:35-50` 断言 `earlyQuit` 必须 > `lock`（`requestSingleInstanceLock` 的 index）—— 我的插入在 lock **之前**，相对顺序全部保持 ✅
- 全仓库 `app.getPath('userData')`（main.ts 11 处、electron-runtime.ts 6 处）**都在函数内**，无模块级提前读取 → 早期 redirect 覆盖全部 ✅
- `source` 字段仅作数据传递（`DesktopDataDirectoryLocation.source`），不参与分支判断 ✅

### 已知风险（已在清单标注）
装到**不可写目录**（如 `C:\Program Files\`）时 `<安装目录>\data` 创建失败。本工程 `perMachine: false`（默认装用户目录），但安装向导允许改路径 → 提示用户别装 Program Files。

### 清单同步
`redolist.html`：总览表第二行改"✅ 已执行"；第二节全节重写（含 B 方案理由、代码片段、数据布局、6 步验证法、风险框）；元信息行改"第 1、2 项已执行完"。

## 【执行】第 3 项（市场源 / Awesome DSH Plugins）已完成（2026-09-14 11:20）

### ⚠️ 计划形态变了：**从"替换"变成"新增"**
原计划（redolist 第 14–20 项）是"把内置源②`DSH Marketplace` 换成 Awesome"。**新版社区版 2.0.9 已把 DSH Marketplace 整个删除**，实测：
- `dsh-community-market/src/adapters/` 只剩 `dsh-1024store.ts`／`dshfind.ts`／`standard-http.ts`，**无 `dsh-marketplace.ts`**
- 新版 `BUILT_IN_PROVIDERS` 只有 **2 个**：`DSH 1024Store`、`dshfind`
- 所以改成：**末尾追加第 3 个内置源**

### 实际改动（5 改 + 2 新增；tracked 文件 +43 / −1 行）
| # | 文件 | 改动 |
|---|---|---|
| 14 | `dsh-community-market/src/adapters/awesome-list.ts` | **新增 5.7 KB**（从 `I:\dsh-desktop` 原样复制）；额外新增常量 `AWESOME_LIST_LEGACY_ADAPTER_ID = 'market.dsh-marketplace-qilewl-v1'`，并改写 key 注释说明升级兼容用途 |
| 15 | `src/data/awesome-catalog.ts` | **新增 2,095 KB**（原样复制）：2931 条条目、23 分类，快照生成于 2026-09-02 |
| 16 | `src/catalog/service.ts` | **+26 行**：① 导入常量；② `BUILT_IN_PROVIDERS` **末尾追加**（不是中间插入，避免动 order）；③ `adapters` 映射注册本适配器 + 旧 adapterId 别名 |
| 17 | `src/client/locales.ts` | **无需改**（旧版对它只改了终端名，已由第 1 项统一为 `R9`） |
| 18 | `src/index.ts` | **+1 行**：导出 `awesomeListAdapter` |
| 19 | `tests/host-routes.spec.ts` | **+13 行**：导入常量、`builtIns` 断言补第 3 条、`it.each` 增加本源用例 |
| 20 | `PRIVACY.md` / `PRIVACY.zh.md` | 内置来源清单补 Awesome DSH Plugins（en 改写 "and" 连接词） |

### 🔑 关键保障：契约没变（实测，非推断）
**新旧两版的 9 个 contracts 文件 SHA256 逐字相同**：`types.ts`／`validate.ts`／`query.ts`／`identity.ts`／`index.ts` ＋ `generated/{catalog-snapshot,catalog-query,catalog-source,catalog-provider-page}.ts`。
→ 旧适配器可**原样编译运行**，无需适配。
另核：
- `CatalogAdapter.fetchCategories?` / `scanCatalog?` 均为**可选**，签名一致 ✓
- `CatalogQuery` 字段（q/category/capability/cursor/limit/sort/locale）**完全未变** ✓
- `Identifier = string`（无正则），数据里的 `dsh-market/dsh-market` 这类含 `/` 的 id 合法 ✓
- `InstallSource = { kind:'github'; commit:string }` ✓
- `CatalogSnapshot` item 的 `homepage`/`latestVersion`/`displayName` 等字段齐备 ✓

### 其他接线核查（都不需要额外注册）
- `host/routes.ts:669-673` 的 `adapterHttpClients` 映射：**查不到就回退默认客户端**（`adapterHttpClients.get(...) ?? this.http`）→ 本适配器不联网，无需注册 ✓
- `routes.ts:787/1017` 的 `providerQuery` 分支只对 `dshfind`／`isDsh1024StoreAdapterId` 生效 → 本适配器走 `scanCatalog` 路径（正是离线目录想要的）✓
- `routes.ts:564` `nextOrder` 按**已有记录**计算（不是数组下标）→ 追加位置不影响测试 ✓
- 全仓库只有 `tests/host-routes.spec.ts:175` 断言真实 `builtIns` 列表（其他测试用空数组夹具）✓
- 仓库**无 lint 配置**（`check` = build+typecheck+test+verify），导入顺序无约束 ✓

### 未做（已告知用户，列为可选后续）
数据快照仍是 **2026-09-02** 的（12 天前）。生成器在 `I:\deepseekharness制作\.workbuddy\awesome-catalog\`（`parse-catalog.mjs`／`fetch-commits.mjs`／`build-awesome-data.mjs`，含 `.env` 可能存 GitHub token）→ 刷新数据需联网，作为独立一步。

### 文档不一致（已知，未改）
`dsh-community-market/docs/catalog-provider-contract.md` / `.zh.md` 里写"reviewed DSH 1024Store and dshfind adapters"（第 5／335／349 行），**未把 awesome 加进去** —— 旧版 DIY 也没改这两个文档，保持一致；如需同步说一声。

### 清单同步
`redolist.html`：第三节整节重写（含形态变化说明、7 项实际改动表、旧 key 兼容说明、4 步验证法、契约实测保障、可选后续）；总览表第三行 + 元信息进度行同步。

## ⚠️【重大发现 · 第 4 项不需要做了】官方 0.1.5-rc.1 已把权限档位中文化内置

用户 2026-09-14 11:24 说"继续第4项"。第 4 项 = 中文化补丁（第 21 项重写 `dsh-client-ui-conversation@0.1.5-rc.1.patch` + 第 22 项根 package.json 加 patch 声明）。

### 结论：**第 21、22 项取消** —— 官方已把我们当年补的东西内置了

**旧版 `0.1.2-alpha.1` 的状态（证明当年补丁确有必要）**：
- 中文词典里是**半中半英**：`"access.confirm.title": "确认启用 Full access？"`（`lib/client.js:13191`）、`"access.fullLabel": "Full access"`（`:13196`，未译）
- 选择器只翻一档：`optionLabel()` = `return option.value === FULL_ACCESS ? t("access.fullLabel") : displayName(option.name)`（`:14942`）→ 另两档走 `displayName()` 出英文

**新版 `0.1.5-rc.1` 的状态（已解压实测）**：
- 中文词典三档齐全（`lib/client.js:13694-13696`）：`access.preset.readOnly: "仅可查看"`、`preset.workspaceWrite: "工作区内修改"`、`preset.fullAccess: "完全权限"`
- 确认弹窗全中文（`:13697-13701`）：`"确认启用完全权限？"`、`"启用完全权限后，**智能体**将减少确认步骤…"`（连 `agent` 都译成「智能体」了）
- 函数重写为 `permissionLabel(value, name, t)`（`:15646-15654`），**三档全走 `t()` 词典**
- 官方 README（`dsh-client-ui-permission-presets`）原话：内置标签"在中文界面中是**「仅可查看」「工作区内修改」和「完全权限」**"

### 四道核实（全部实测，非推断）
1. **词典逐 key 比对**：新包 `zh`(13634-13794) / `en`(13796-13956) 各 **156 条，一一对应零缺漏**；中英完全相同的仅 5 条 `Bash`/`Pwsh`/`Grep`/`Glob`/`HTTP` —— **都是本就该保持英文的技术名词**；无明显漏译的自然语言条目。
   - 工具：`I:\deepseekharness制作\.workbuddy\locale-gap.mjs`（按花括号配平提取两个词典对象并逐 key 比对，可复用于任何 `dsh-*` 客户端包）
2. **词典挂载确认**：`lib/client.js:16523-16526` → `ctx.effect(() => ctx.locale.register(NS, { zh, en }))`，`NS = "conversation"` ✓
3. **选择器链路确认**：宿主包 `@deepseek-ai/dsh-permission-presets`（`lib/index.js:82-91`）给出的 preset `name` 是**机器名**（`workspace-write`/`danger-full-access`），与 `permissionLabel` 里 `name === value` 条件吻合 → **确实走中文词典**（不会掉进 `displayName()` 分支）✓
4. **排查有无其它真翻译**：回查旧版 29 个改动 —— `notifications.ts`／`recovery-copy.ts`／`PRIVACY.md`／`PRIVACY.zh.md` / `desktop-settings-locales.ts` / `tray-locale.ts` / `native-dialog-copy.ts` **全是改名或市场源改动**（文件本身就是中文），**真正的翻译补丁只有这一个** ✓
   - 另：根 `package.json` 的 `resolutions` 里**没有** `@deepseek-ai/dsh-client-ui-conversation` 条目 → 证实社区版不含我们的补丁（我方独家），也说明第 22 项本需新增声明。

### 唯一差别：用词不同（不涉及功能）
| 档位 | 我们旧补丁 | 官方新版 |
|---|---|---|
| 完全访问档 | `完全访问` | `完全权限` |
| 只读档 | `只读` | `仅可查看` |
| 工作区写入档 | `工作区写入` | `工作区内修改` |
| 弹窗称呼 | 保留 `agent` | `智能体` |

→ 官方用词更完整。**已向用户提出：要么整个取消（推荐，少维护一个官方包补丁），要么保留一个"只改 3 行文案"的精简补丁。等用户拍板。**

### 关联发现（第 27 项不受影响，仍要做）
指令面板那 6 条描述在新版里**仍是纯英文硬编码**，且**指令系统没有多语言机制**：
- `dsh-command-compact/lib/index.js:92-96` → `ctx.commands.register({ name: "compact", description: "Compact older conversation history", handler })` —— `description` 是**普通字符串**，无 key、无 locale
- 6 个包实测均有英文描述：`compact`(command-compact:94) / `session-log-export`(session-log-export:478) / `feedback`(command-feedback:166) / `goal`(command-goal:176) / `permission`(permission-presets:159) / `plan`(plan-mode:182)
- ⚠️ 注意：部分包的描述在 **`lib/index.js` 与 `lib/types/index.js` 两处**都出现（如 permission-presets 159/153、plan-mode 182/179）→ 实施第 27 项时需确认要打哪几处。

### 清单同步
`redolist.html`：第四节整节重写为"✅ 已查证：无需执行"（含结论框、证据对照表、四道核实、用词差别表、第 27 项关联提醒）；总览表第四行改"✅ 已查证无需做 / 0 个文件"；元信息进度行加"第 4 项查证后取消"。
**本次 0 行代码改动**（纯查证）。

## 【用户决策 2026-09-14 12:15】第 21、22 项**整项取消**（选 A）
用户回"A，整个取消"→ 中文化补丁不做（官方已内置），第 22 项的根 `package.json` patch 声明也不加。
清单第四类已标记为"✅ 已查证：无需执行"。

## 【执行】第 5 项（升级源改本地）已完成（2026-09-14 12:4x）

### 改动：4 文件，+67 / −16 行（diff 存 `.workbuddy/item05-diff.txt`）

| # | 文件 | 改动 |
|---|---|---|
| 23 | `src/update-checker.ts` | ① 新增 `DESKTOP_UPDATE_SOURCE_DIRECTORY = 'I:\\deepseekharness更新DIY\\发布'`（Windows 绝对路径，注释说明"机器特定、有意为之"）；② `DESKTOP_VERSION_ENDPOINT` 由 `pathToFileURL(join(目录,'version.json')).href` 现场生成；③ 新增 `node:path` / `node:url` 导入；④ 文件头注释改为 "against this build's own local release source" |
| 24 | `src/update-download.ts` | 删 `DESKTOP_DOWNLOAD_URLS` 常量 → 两个导出函数 `desktopUpdateArtifactName(platform,version,channel)` + `desktopUpdateDownloadUrl(platform,version,channel)`；调用点（原 `:124`，现 `:153`）改为 `desktopUpdateDownloadUrl(platform, options.version, channel)`；新增 `pathToFileURL` 导入与 `DESKTOP_UPDATE_SOURCE_DIRECTORY` 导入 |
| 25 | `tests/update-download.spec.ts` | import 改 `desktopUpdateDownloadUrl`；`:99` → `desktopUpdateDownloadUrl('darwin','2.1.0')`；`:112` → `desktopUpdateDownloadUrl('win32','2.2.0')` |
| 26 | `cordis.patch.yml` | `desktop-updates` 加 `config: { enabled: false }` + 三行注释 |

### 关键设计决定（都基于实测/源码，非猜测）
- **文件名为"免改名"形态**：`desktopUpdateArtifactName` 的 Windows 分支 = `deepseekharness-{version}-x64-Setup.exe`，**与 `nsis.artifactName`（`deepseekharness-${version}-${arch}-Setup.${ext}`，本工程 `${arch}` 固定 x64）逐字对齐** → 打包产物可直接拷进 `发布\` 不用改名。
- **用 `pathToFileURL` 而非手写 `file:///I:/…`**：中文会被标准转义。实测生成 `file:///I:/deepseekharness%E6%9B%B4%E6%96%B0DIY/%E5%8F%91%E5%B8%83/version.json`（`%E6%9B%B4%E6%96%B0`=更新、`%E5%8F%91%E5%B8%83`=发布）。验证脚本存 `.workbuddy/url-check.mjs`。
- **`DESKTOP_UPDATE_SOURCE_DIRECTORY` 放在 `update-checker.ts`**（`update-download.ts` 本来就 import 它，不产生新循环依赖），单一事实来源。
- **macOS 分支**写 `deepseekharness-{version}-mac.dmg`，注释明说"本工程 mac 目标为 `dir`、不产 dmg，此分支只为保持平台契约完整"（**不是**对 electron-builder 输出的断言）。
- **beta 分支**用 `deepseekharness-Beta`，与文件内既有的 `desktopUpdateFilename()` 约定保持一致；本次只构建 stable。

### 四道验证（全部实测）
1. **URL 生成实测**（`.workbuddy/url-check.mjs` + managed node 22.22.2）：四条 URL 全部正确编码 ✅
2. **旧常量零残留**：全仓库 grep `DESKTOP_DOWNLOAD_URLS` → **0 命中** ✅（剩余 `dshdesktop.cn` 只在 README/PRIVACY 文档里）
3. **调用点确认**：`desktopUpdateDownloadUrl` 3 处（定义 + 生产调用 `:153` + 测试 2 处）✅
4. **测试影响面核对**：`update-checker.spec.ts` **全文未引用端点常量**；`package.spec.ts:344` 断言只匹配 `web-runtime` 段（新增 config 不影响）；`electron-runtime.spec.ts:2344` 的社区域名只是传给**已 mock 的** `net.fetch` 的参数。另复核 `update-lifecycle.ts:100` `enabled` **只 gate 后台轮询**，`checkNow()`（托盘 + 设置页按钮）不受影响 ✅
- **YAML 结构**已回读确认，与上方 `desktop-shell` 的 `config:` 缩进形态一致。

### ⚠️ 探针重跑被沙箱拒绝（按铁律未重试）
我写了一个 Electron 探针（测 `net.fetch` + `file://` 中文路径 + `redirect:'error'` + `cache:'no-store'` + 大文件 body 流），执行时被 WorkBuddy 沙箱拦下且用户明确拒绝（提示 Blocked paths: `C:\Users\50667\.ssh`，与命令实际内容无关）。
→ **按铁律第 6 条未自试第二次**。探针脚本已复制到 `I:\deepseekharness制作\.workbuddy\probe-local-update-fetch.js`，用户可手动跑：
```
清除 ELECTRON_RUN_AS_NODE / NODE_OPTIONS 后
& "I:\dsh-desktop\dsh-plugin-desktop\node_modules\electron\dist\electron.exe" "I:\deepseekharness制作\.workbuddy\probe-local-update-fetch.js"
结果写在 C:\Users\50667\AppData\Local\Temp\electron-probe\probe5-result.txt
```
现有依据：本会话早先的探针**已实测通过**（`net.fetch('file:///…')` status=200；中文路径原始/编码两种写法均 200；文件不存在抛 ERR_FILE_NOT_FOUND）。

## ⚠️【第 1 项覆盖盲区·必须报告】改名审计只扫了 `src/`，`scripts/` 有约 30 处漏网
第 5 项过程中顺手全仓库复扫 `DSH Desktop|DSH-Desktop`，发现**第 1 项当时的"零遗漏"结论不成立**：

| 位置 | 数量 | 影响 |
|---|---|---|
| **`scripts/`（功能性漏网）** | ~30 处 / 9 文件 | 见下表 |
| `tests/`（入参/示例路径） | ~30 处 | **不导致测试失败**（多为 argv 输入或示例路径；如 `desktop-copy.spec.ts:71` 传 `'DSH Desktop'` 但期望输出里不含它） |
| 文档（`README*` / `PRIVACY*` / `THIRD_PARTY_NOTICES.md` / `docs/*` / `AGENTS.md` / `CONTRIBUTING*`） | 多处 | 第 1 项**有意不改**（D 组） |

**`scripts/` 漏网明细**（全部是**手动/VM 工具脚本**，不在 `yarn build`/`check`/`dist:win` 常规链路里）：
- `build-windows-nsis-ab.ts:308` — `DSH-Desktop-${version}-x64-Setup.exe`
- `inspect-windows-installed-app.ts:50,76` — `'DSH Desktop.exe'`
- `probe-windows-packaged-runtime.ts:34,45,46` — exe 名 + `productFilename`
- `verify-mac-release.ts:47` / `verify-mac-smoke.ts:55` — `productName: 'DSH Desktop'`（默认参数）
- `verify-licenses.mjs:143` — 断言 `THIRD_PARTY_NOTICES.md` 那句（**与 md 现状一致，不会失败**）
- `run-windows-nsis-ab.ps1`（7 处）/ `smoke-windows-installer-upgrade.ps1`（8 处）/ `reproduce-windows-installer-running-check.ps1:24` — VM 冒烟脚本，按 `DSH Desktop.exe`/注册表 DisplayName 匹配

**根因**：第 1 项的 `rename-audit.mjs` **只扫 `src/`**，`rename-tests.mjs` 只覆盖 `tests/`，**`scripts/` 整个目录没纳入审计**。
**当前后果**：常规构建/打包**不受影响**（这些脚本不会自动运行）；但**一旦手动跑 A/B 或 smoke 脚本就会失败**（找不到 `DSH Desktop.exe`）。
**待用户决定**：要不要补一轮 `scripts/` 改名（属第 1 项补遗，超出已批准范围，未擅自动手）。

## ⚠️【第 1 项补遗 · 全量审计结果】范围远超预期：158 文件 / 646 行，且发现第 1 项**打破了 `yarn check`**
用户 2026-09-14 12:37 要求"把第 1 项的补遗做了"。我先做了全量审计（工具：`.workbuddy/scripts-rename-audit.mjs`，排除 `vendor/`、`node_modules/`、`.git/`、`.yarn/`、`dist/`、文档 `*.md`；扫 947 个文件）。
输出：`.workbuddy/rename-audit-full.txt`。**命中 158 文件 / 646 行。**

### 🔴 阻塞级发现（第 1 项引入的真实破坏，有实锤）
1. **`yarn check` 会失败** —— `package.json:596` 的 `check:layout` 链里包含 **`check:desktop-variants`**（`node scripts/verify-desktop-variants.mjs`）。
   该脚本**逐文件比对 `dsh-plugin-desktop/src` 与 `dsh-plugin-desktop-beta/src`**，只允许 `product-identity.ts` 不同，且 `normalizeIdentity` 仅做 `.replaceAll('dsh-plugin-desktop-beta','dsh-plugin-desktop').replaceAll('DSH Desktop Beta','DSH Desktop')`。
   → 第 1 项**只改了 stable**，beta 原样未动 → **该脚本必然抛 "Desktop variant source drift"**。
2. **至少 3 处测试/脚本断言会红（已逐一核实）**：
   - `dsh-plugin-desktop/src/tray-locale.ts:36,56` 现为 `'Open R9'` / `'打开 R9'`（已改）
     → `tests/terminal.spec.ts:35,37` 断言 `'Open DSH Terminal'` / `'打开 DSH 终端'` **会红**
     → `scripts/verify-profile-boot.mjs:265` 断言 `item.label() === 'Open DSH Terminal'` **会红**
   - `tests/updates.spec.ts:300/335/340/342/358/382/471/514` 断言托盘 `'DSH Desktop 2.1.0 Available'` 等 → `tray-locale.ts` 已改成 `deepseek harness …` **会红**
3. **`dsh-community-market/src/client/locales.ts` 26 处未改** —— 这是**市场界面的用户可见文案**（`'需要 DSH Desktop'`、`'打开 DSH 终端'`、`'无法打开 DSH 终端…'`、`'重启 DSH Desktop'` 等，中英各一套）+ `host/routes.ts:942` 一条错误文案。
   ⚠️ **更正我 2026-09-14 早先的说法**：当时我在清单第三类写"`client/locales.ts` 无需改（旧版只改了终端名，已由第 1 项统一为 R9）"——**这是错的**。第 1 项改的是 `dsh-plugin-desktop/`，**没有碰 `dsh-community-market/`**，所以这 26 处原样留着。
4. **`dsh-plugin-desktop/scripts/` 8 个手动工具 ~40 处**（不是先前说的"两个文件"）。

### 分类统计（按处置）
| 类别 | 规模 | 建议 |
|---|---|---|
| **A 必改·功能性** | `dsh-plugin-desktop/scripts/`（8 文件 ~40 处） | 改 |
| **A 必改·用户可见** | `dsh-community-market/src/client/locales.ts`(26) + `host/routes.ts`(1) | 改 |
| **A 必改·流水线** | `scripts/verify-desktop-variants.mjs`(1) 的归一化 | 见下方决策 |
| **B 测试断言** | `dsh-plugin-desktop/tests/` 约 30 文件 ~90 行（其中部分是入参/示例路径，不红） | **需跑一次测试才能定红名单，不猜** |
| **B 元数据/注释** | 根 `package.json:4`、`dsh-plugin-desktop/package.json:4`、`build/installer.nsh:34`（注释已失实） | 可选 |
| **C 有意不改** | 协议头 `X-DSH-Desktop-*`、beta 身份、`lan-https-certificate.ts:22`（第 34 项待拍板） | 不改 |
| **D 无需处理** | `_deprecated/`（7 处，第 31 项将被删除）、`.github/`（6 处，摆设）、`dsh-plugin-desktop-beta/` 全部（见决策） | — |

### ⏳ 需要用户拍板的两件事（**未动手**）
- **决策 1：beta 包（`dsh-plugin-desktop-beta/`，约 330 处）要不要一起改名？** 直接决定 `check:desktop-variants` 怎么修：
  - **A1 一起改**：约 600 行机械改动（src + tests），保持变体校验有效；缺点是给一个"永不构建"的包做大改，且 beta 的 `windowTitle` 也得跟着变成 `deepseekharness`（会与 stable 无法区分）。
  - **A2 不改 beta，把 `check:desktop-variants` 从流水线摘掉并注释说明**（推荐）：承认"只构建 stable"的现实，改动最小；代价是失去一道（对本项目已无意义的）安全网。
  - **A3 不改 beta，把差异文件逐个加进脚本的允许清单**：不推荐，会让校验彻底失去意义。
- **决策 2：测试断言怎么修？** 先修"已实锤会红的"，还是**等你跑一次 `yarn install && yarn check` 拿完整红名单再一次性修**（推荐后者——我不猜）。

**本轮 0 行代码改动**（纯审计）。

## 【第 37 项 · 二次审计（只读）】用户要求"再审计、不要直接动手、告诉我最终会怎样"（2026-09-14 12:48）

### 🔑 比上一轮更精确的三个结论

**① `check:desktop-variants` 的实际漂移 = 25 个文件**（不是 158）——用只读脚本 `.workbuddy/simulate-variant-check.mjs` 完整模拟其逻辑得出。清单：bin.ts、DesktopFrameTitlebarView.tsx、desktop-settings-locales.ts、directory-picker.ts、desktop-boot-recovery.ts、desktop-data-directory.ts、desktop-terminal.ts、index.ts、main.ts、native-dialog-copy.ts、compatibility-chrome.html、compatibility-chrome/main.tsx、desktop-dialog.html、recovery.html、setup-wizard.html、notifications.ts、recovery-copy.ts、setup-wizard-copy.ts、startup-recovery-window.ts、tray-locale.ts、update-checker.ts、update-download.ts、update-lifecycle.ts、windows-volume-diagnostics.ts、workspace-admission.ts。
- **关键点**：漂移包含**第 1 项（改名）+ 第 2 项（便携 main.ts）+ 第 5 项（update-*.ts）的全部 stable 独有改动** → 该校验的本质是"stable 与 beta 必须逐字同步"，**我们任何一处 DIY 改动都会触发它**。所以对我们这种"只维护 stable"的 fork，它**没有任何正面价值**，每次改 stable 都要顺带改 beta。

**② `yarn check` 的失败面（逐项核实，非猜测）**：
- `check:bilingual-docs` / `check:architecture` / `check:vendored-runtime` / `verify-layout.mjs` —— **均不受改名影响** ✅
- `dsh-plugin-desktop check` = build+typecheck+test+7 个 verify:* —— **只有 `test` 一步会红**；7 个 verify 脚本本身无旧名依赖（`verify-licenses.mjs:143` 与未改的 `THIRD_PARTY_NOTICES.md` 是一对，**现状一致、不会红**）
- `dsh-community-market check` —— **测试不红**：市场测试里**没有任何**对旧名文案的断言（全库只有 `market-settings-tab.spec.tsx:407` 一处 'DSH Terminal'，且只是 `it()` 的**描述字符串**）
- `dsh-plugin-desktop-beta check` —— **不红**（beta 的源码+测试都是旧名，自洽）

**③ stable 测试里"真正会红"的精确清单（逐条读源核实）：6 个文件 / 22 处断言**
| 文件 | 行 | 变红的根因（源码现值） |
|---|---|---|
| `terminal.spec.ts` | 35, 37 | `tray-locale.ts:36,56` → `'Open R9'` / `'打开 R9'` |
| `updates.spec.ts` | 294,295,300,335,340,342,358,382,471,514 | `native-dialog-copy.ts:84,145` → `'deepseek harness Update Available'`；托盘 `updateAvailable` → `deepseek harness …` |
| `notifications.spec.ts` | 208,247,248,327 | `notifications.ts:37,39` → `Open deepseek harness for details.` |
| `startup-recovery-window.spec.ts` | 452,501,540 | `recovery-copy.ts:385,389,248` → `deepseek harness 将改为使用…` / `…将在默认路径创建…` / `Factory reset deepseek harness?` |
| `profile-selection-window.spec.ts` | 89,126 | `recovery-copy.ts:420` → `请重启 deepseek harness …` |
| `recovery-native-ui.spec.ts` | 69 | 恢复页 markup 里现在是 R9 相关文本 |

**stable 测试里"不红"的部分（逐条判定）**：`verify-electron-fuses.spec.ts`(22)、`windows-nsis-ab.spec.ts`(11)、`desktop-cli.spec.ts`(10)、`desktop-terminal.spec.ts`(8/10，`:309,:325` 为入参)、`desktop-installer-quit.spec.ts`(9)、`native-menu.spec.ts`(10)、`verify-mac-*.spec.ts`(15)、`desktop-runtime-environment.spec.ts`(5)、`crash-evidence.spec.ts`(2)、`plugin.spec.ts`(2)、`window-options.spec.ts`(3)、`desktop-copy.spec.ts`(3)、`desktop-dialog-window.spec.ts`(2)、`desktop-boot-recovery.spec.ts`(1，是"断言脚本不含"的负向断言)、其余全部 —— 原因是**入参/夹具/示例路径**，断言的期望值与入参同构，不依赖产品名。

**④ 其他精确计数**：
- `dsh-community-market/src/client/locales.ts` **26 处**（`client/index.ts:15,30` 确认 `ctx.locale.register(NS,{zh,en})` 为**活**）+ `src/host/routes.ts:942` 1 处
- `dsh-plugin-desktop/scripts/` **10 文件 / 31 行**：build-windows-nsis-ab.ts(1)、inspect-windows-installed-app.ts(2)、probe-windows-packaged-runtime.ts(3)、reproduce-windows-installer-running-check.ps1(1)、run-windows-nsis-ab.ps1(10)、smoke-windows-installer-upgrade.ps1(10)、verify-mac-release.ts(1)、verify-mac-smoke.ts(1)、verify-profile-boot.mjs(1)、verify-licenses.mjs(1，建议跳过，改则要连 `THIRD_PARTY_NOTICES.md` 一起改)
- 元数据/注释类：根 `package.json:4`、`dsh-plugin-desktop/package.json:4`（description，旧 DIY 也没改）、`dsh-plugin-desktop/build/installer.nsh:34`（注释已失实）
- **不该改的**：`awesome-catalog.ts:53`（第三方插件 `dsh-market` 的介绍里提到 DSH，**非我方产品名**）、协议头、beta 身份、`_deprecated/`（第 31 项会删）、`.github/`（摆设）

### 待用户决策（不变）
- **① beta 包怎么办**（A1 一起改 ~600 行 / A2 不改并摘掉变体校验【推荐】/ A3 不推荐）
- **② 测试怎么修**（B1 先跑 `yarn install && yarn check` 拿红名单【推荐】/ B2 只修我已实锤的 22 处）
- ⚠️ 另注：`smoke-windows-installer-upgrade.ps1:38` 还有一行 `$taskUserData = %APPDATA%\DSH Desktop` —— 这是**便携模式的语义问题**（数据现在跟着安装目录走），**不只是改名**，需单独定。
产出：`.workbuddy/variant-drift.txt`（漂移清单）、`.workbuddy/simulate-variant-check.mjs`、`rename-audit-full.txt`。本轮 0 行代码改动。

## 【用户决策 2026-09-14 13:11】**暂时认可** A2 + B1（明确"等下讨论"）
用户原话：把拍板的两件事"1. A2 摘掉变体校验（暂时认可），2.测试：B1 你先跑 `yarn install && yarn check` 拿红名单我再修（暂时认可），这两件事也记录下来，等下讨论"。
- **① beta 包 = A2（暂时认可）**：不改 beta，把 `check:desktop-variants` 从 `check:layout` 摘掉 + 注释说明。
- **② 测试 = B1（暂时认可）**：先由用户手动跑 `yarn install && yarn check` 拿完整红名单，再一次性修。
- ⚠️ 两项**均未动手执行**，等后续讨论。已写入 `redolist.html`（第 37 项决策状态表 + 总览表该行状态改为"⏸ 暂时认可·待讨论"）。
- 用户同一轮还提了两个问题（已作答）：**(a)** "逐文件比对 stable 和 beta 源码、只允许 product-identity.ts 不同"是什么意思；**(b)** 既然已经是自己做和更新、社区版只作参考，**yarn / 打包的那些"要求"是不是可以自己改**。

## 【用户决策 2026-09-14 13:19】**A2 已定** + **B1 推迟到全部改完之后**
用户原话："那么，现在就是我们不出 beta了，至于'37② 测试 → B1'记录，**等所有项修改完之后，一起来检查和验证**。那么现在是不是可以进入后面的了？"
- **① beta 包 → A2 已定**：明确"**我们不出 beta 了**" → 不改 beta 包（100% 社区原样）；`check:desktop-variants` 从 `check:layout` 摘掉（并入收尾步骤执行）。
- **② 测试 → B1 推迟**：本轮**不跑测试**；等第 6~8 步（增量需求 + 清理）全部改完，用户手动跑 `yarn install && yarn check` 拿完整红名单，我再一次性修。
- **第 1 项补遗整体暂停**，先推进"第 7 步 · 增量需求"。

### 我答的两个问题的要点（供以后复用）
- **Q1「逐文件比对」大白话**：作者同一套源码发布 stable（`dsh-plugin-desktop/`）与 beta（`dsh-plugin-desktop-beta/`）两个版本，beta 是 stable 的**复制品**；`verify-desktop-variants.mjs` 就是**校对**：逐同名文件比对（比对前先把 beta 的 `DSH Desktop Beta` 归一成 `DSH Desktop`），**任何一页不同就报错**；`product-identity.ts` 是**唯一豁免**（身份本来就该不同）。比喻：普通版/精装版内页必须一样，只有封面可不同。我们只改了"普通版"→ 25 页对不上 → 报漂移。
- **Q2「yarn/打包的要求能不能自己改」**：**能，而且已在改**。`yarn` 只是包管理器，所谓"要求"全是**作者写在仓库里的脚本**（`package.json` scripts + `scripts/*.mjs` + `dsh-plugin-desktop/scripts/*` + electron-builder 配置）→ **完全能动**。分三层：① 我们仓库的构建/检查脚本 → ✅ 随便改；② 第三方工具（yarn/electron-builder/vitest）→ ⚠️ 能改（走 `patches/`）但没必要；③ DeepSeek 官方核心（`vendor/` 265 包）→ ❌ 不能直接改（sha256 锁，只能打补丁）。
- **⚠️ 关键权衡（已向用户强调）**：**能改 ≠ 随便改**。因为策略是"以社区版为前期工作队、每次拉它的新版参考重做"，工具链乱改会让将来 merge 越来越难。**原则：只在规则对我们无意义时才动，工具链整体保持与社区版一致。**
- **我给的保留/摘除建议**：摘 `check:desktop-variants`；保留 `check:vendored-runtime`（官方包指纹，正是我们要的）、`check:architecture`、`verify-layout.mjs`、`check:bilingual-docs`、`typecheck`/`test`；beta 自己的 `check` 暂时保留（绿、不碍事）。
- 清单已同步：`redolist.html` 第 37 项决策状态表重写（①已定 A2 / ②B1 推迟）+ 新增"第 1 项补遗其余工作"表 + 总览表该行改"⏸ 已定方案·待执行" + **执行顺序扩成 11 步**（第 9 步 = 补遗+收尾、第 10 步 = 跑 check 拿红名单、第 11 步 = 构建打包验证）。

## 【开工】进入第 7 步 · 增量需求（2026-09-14 13:20 起）
下一项：**第 27 项 · 指令面板 6 条描述中文化**（给 6 个官方包各打一个补丁）。

## ⚠️【第 27 项 · 重大发现】**也不需要做了** —— 官方 0.1.5-rc.1 已内置（与第 4 项同类情况）

### 结论：6 个补丁 → **0 个补丁**

### 证据链（全部实测）
1. **全量指令清单**：写脚本 `find-commands2.mjs` **正经解析 tar、只扫 `package/lib/**/*.js`**，扫全部 265 个包 → **整个系统只有 7 条指令**：`compact / export / feedback / goal / model / permission / plan`，与用户截图**完全吻合、无遗漏**。
   - 各指令宿主位置：`dsh-command-compact/lib/index.js:92`(compact)、`dsh-session-log-export/lib/index.js:476`(export)、`dsh-command-feedback/lib/index.js:164`(feedback)、`dsh-command-goal/lib/index.js:174`(goal)、`dsh-permission-presets/lib/index.js:157`(permission)、`dsh-plan-mode/lib/index.js:180`(plan)；`/model` 在 `dsh-client-ui-model-selection/lib/client.js:919`（无 description，走客户端词典）。
2. **⚠️ 中途自己抓到一个错**：第一版脚本 `find-commands.mjs` 把 gunzip 后的 **README 示例代码**也扫了，**误报** `dsh-commands` 里有一条 "Enter plan mode"。实际它**只在 `dsh-commands/package/README.md:37` 的示例里**，不是真注册。→ **教训：扫 tgz 内容必须正经解析 tar 并限定路径（只 `lib/`），否则 README/docs 会污染结果。**
3. **新版客户端词典已全套中文**：`dsh-client-ui-commands/lib/client.js:166-171`（zh）/ `:183-188`（en）：
   `description.compact`=压缩以上对话内容 / `export`=将当前会话内容导出为 ZIP / `feedback`=发送关于当前会话的反馈 / `goal`=设置或查看长期任务目标 / `permission`=切换权限预设（沙箱模式与审批策略）/ `plan`=进入或退出计划模式。
4. **生效机制**：`:484-492` 名字→词条映射（6 条）；`:657-660` `hostDescription()`：
   `const key = HOST_DESCRIPTION_KEYS.get(command.name); return key !== void 0 && command.description === en[key] ? this.t(key) : command.description;`
   → **宿主描述"逐字等于"英文词典值就翻译，否则原样透传**（"否则"是给 scoped/第三方指令留的）。
5. **逐字比对**：6 个宿主包的描述与客户端 `en` 词典 **6 条全部完全一致** → 条件成立 → 会翻译。
6. **语言闭环**：`:1088` `NS = "command"`；`:1103` `ctx.locale.register(NS, { zh, en })`；`:517` `this.t = locale.bind("command")`。
7. **旧版确实没有**：旧版 `dsh-client-ui-commands@0.1.2-alpha.1` 的 `lib/client.js` **既无 `description.*` 词条、也无 `hostDescription`** → 完全解释用户截图里那 6 条英文。
8. **`/model` 本就中文**：走自己词典 `dsh-client-ui-model-selection/lib/client.js:796` `"command.description": "选择本会话使用的模型"` ✓
9. 另核实：`dsh-plugin-desktop` 与 `dsh-community-market` **都不注册指令**（grep `commands.register` 无命中）。

### ⚠️ 记录一个脆弱点（将来升级要留意）
这套翻译靠**"逐字相等"**匹配（`command.description === en[key]`）。**官方若改了宿主包英文措辞、却没同步改客户端词典，翻译会静默失效退回英文**（不报错）。升级内核后若发现某条指令又变英文，先比这两个字符串定位。

### 清单同步
`redolist.html`：6.1 节整节重写为"✅ 已查证：官方新版已内置，不需要做"（含 7 条证据 + 脆弱点提示框）；总览表第 27 行改"0 个补丁 / ✅ 已查证无需做"；元信息进度行加第 27 项；执行顺序第 7 步去掉"6 个指令补丁"。
**本轮 0 行代码改动**（纯查证）。

## 【执行】第 29 项（模型思考内容转中文）已完成（2026-09-14 13:3x）

### ⚠️ 执行前查证发现：**原方案有个前提是错的 + 有个会静默丢数据的坑**

| 原方案判断 | 实际情况（实测） |
|---|---|
| "`dsh-base` 里 `personaPrefix: ''` → **官方留空等我们填**" | **错的。** `dsh-base` 确实留空，但 **`dsh-web-app/cordis.patch.yml:16-20` 又填上了**：<br>`personaPrefix: "You are a coding agent powered by the {{model}} model."`<br>`personaSuffix: "Your working directory is {{cwd}}."`<br>而 desktop profile **包含 `dsh-web-app`**（`PROFILE_TEMPLATES.web.bundles = [dsh-base, dsh-web-app]`，见 `dsh-app-boot/lib/index.js:333-336`）→ **这两段本来就在生效**。 |
| "给 `system-prompt` 加一段 `config`" | **有坑**：官方 README 原文「**用户 patch 会替换匹配到的整个配置**——按 id 定位的 patch **不做深度合并**，因此 profile 覆盖必须**重述需要保留的组合包字段**」（`dsh-app-boot/README.md` / `README.zh.md`）。→ 只写 `personaPrefix` 会**静默把 `personaSuffix` 重置为空**（模型不再被告知工作目录）。`dsh-web-app/cordis.patch.yml:5-6` 头注释也写了同一句。 |
| 叠加顺序未确认 | 实测：`dsh-plugin-desktop/src/desktop-plugins.ts:42-47` 的 bundle 列表 = `web 模板(base,web-app)` + `desktop-app` + `DESKTOP_PACKAGE_NAMES` + `dsh-community-market` → 顺序 **base → web-app → desktop → market** → **我方补丁在 web-app 之后、会覆盖它** → **必须重述**。 |
| 落点对不对 | 查了专门的 `dsh-persona` 包：官方 README 明说它**只用于按 agent preset 遮蔽**，且「**要改变部署级人设，应在 `system-prompt` 行自身的配置中修改**」→ **原方案落点正确** ✓ |

### 实际改动：`dsh-plugin-desktop/cordis.patch.yml` 末尾追加（1 文件，纯配置，不碰官方包）
```yaml
- id: system-prompt
  config:
    personaPrefix: >-
      You are a coding agent powered by the {{model}} model.

      始终使用简体中文进行思考（reasoning）与回答，包括中间分析步骤、工具调用前后的说明。专有名词、代码、命令、文件路径保持原文。
    personaSuffix: Your working directory is {{cwd}}.
```
+ 7 行说明注释（解释"整体替换、必须重述"）。
**采用"保留官方那句 + 追加我们的"而非"替换"** → 不丢任何原有信息。

### 验证（实测）
1. **YAML 真实解析**：用 `yaml` 包（借用 `I:\dsh-desktop\dsh-plugin-desktop\node_modules\yaml`）解析整文件 → **错误数 0** ✅
2. 顶层结构 = `insert(7 条)` / `web-runtime` / `system-prompt` ✅
3. **折叠标量实际取值**：`personaPrefix` = `"You are a coding agent powered by the {{model}} model.\n始终使用简体中文…保持原文。"` → `{{model}}` **原样保留**、两段间**恰好一个换行** ✅
4. `personaSuffix` 与 web-app 的**逐字一致** ✅
5. **无第三方覆盖**：全量扫描确认整个 vendor 里只有 `dsh-base`/`dsh-web-app`（及 acp/headless/sdk 等别的 app 包）动过 `system-prompt`；我方 profile 用到的包里**只有这两个**，都在我们之前 ✅
6. 未受影响字段：`includeHarnessIdentity`/`includeRuntimeContext`/`toolOrder` 两层都没设过 → 替换后仍取 schema 默认值，**行为不变** ✅

### ⚠️ 两个要记住的点
- **性质**：提示词层面的引导，能强烈影响但**不能 100% 保证**模型每句都用中文。
- **维护脆弱点**：本行"重述"了官方两段字符串，**将来官方改了这两句，我们这份会盖住他们的改动**（静默生效）。升级内核后来比一下这两行。
- **备选方案（未采用）**：`dsh-system-prompt` 有公开的 `section()` API（`lib/index.js:238-241`），可由我方插件注册一个**新名字**的提示词段落，不覆盖官方任何东西 —— 更干净但要写插件代码。本次按原计划走配置层。

### 清单同步
`redolist.html`：6.2 节整节重写为"✅ 已执行"（含错误前提对照表、实际改动、6 项验证、两个注意点+备选方案）；总览表第 29 行改"✅ 已执行"；元信息进度行加第 29 项。

---

## 【执行】第 30 项（去掉"设置→插件"里的「插件市场」标签页）已完成（2026-09-14 13:55）

### 背景（为什么做）
设置 →「插件」下有三个标签页：**插件配置**（DeepSeek 官方）、**插件列表**（DeepSeek 官方）、**插件市场**（社区版 `dsh-community-market` 注册）。
外面侧边栏左下角已有一个完整的「插件市场」入口 → 设置里这个是重复的，用户定：**去掉它**。

### 🔑 关键前提（早前已查实）：三个位置是**分三次独立注册**
| 位置 | 组件 | 处置 |
|---|---|---|
| `settings.plugins.tab`（设置里的 tab） | `MarketSettingsTab` | **删掉注册** |
| `sidebar.footer.action`（侧边栏入口） | `MarketLauncher` | 保留 ✅ |
| `shell.overlay`（全屏浮层） | `MarketOverlay` | 保留 ✅ |

三次注册**互不依赖** → 删第一个不影响后两个。

### ⚠️ 必须避开的坑：**只删注册，不能删文件**
真正的市场界面 `MarketSurface` 就定义在 `MarketSettingsTab.tsx` 里，而外面的浮层 `MarketOverlay` 靠它渲染。
**实测确认**：`src/client/MarketOverlay.tsx:8` 至今仍 `import { MarketSurface, type MarketView } from './MarketSettingsTab.js'`
→ **删文件会把外面的市场一起废掉**。

### 实际改动（3 文件，**+16 / −38 行**；diff：`.workbuddy/item30-diff.txt`）
**30a `dsh-community-market/src/client/index.ts`**
- 删第 13 行 `import { MarketSettingsTab } from './MarketSettingsTab.js'`
- 删 `ctx.slots.inject('settings.plugins.tab', ...)` 整块（8 行）
- 原处留 **4 行注释**，说明"设置里的市场 tab 有意不注册 / 侧边栏是唯一入口 / `MarketSettingsTab.tsx` 为何保留"

**30b `dsh-community-market/tests/client-index.spec.ts`**
- 测试标题去掉 "settings tab"
- `injections` 名单：去掉 `'settings.plugins.tab'`（3 项 → 2 项）
- `toHaveLength(3)` → `2`
- `registrations` 断言数组删掉第一条（settings tab 那个 `objectContaining`）
- 解构 `[settings, launcher, overlay]` → `[launcher, overlay]`；删掉对 `settings` 的两条断言

**30c `dsh-community-market/tests/browser-plugin.spec.ts`**
- 删 `import { MarketSettingsTab } from '../src/client/MarketSettingsTab.js'`
- 测试 2：标题 `...in official settings and sidebar slots...` → `...in the sidebar and overlay slots...`；`declare` 名单 3 → 2；清掉全部 `settings` / `settingsInject` 相关行
- 测试 3：标题 `for all three entries` → `for both entries`；`names` 数组 3 → 2；删掉 `expect(b.entries('settings.plugins.tab')[0]?.component)...`

### 验证（实测）
1. **全副本搜 `settings.plugins.tab` → 只剩 1 处**：`src/client/MarketSettingsTab.tsx:157` 的 `export type MarketSettingsTabProps = PropsRuntime<'settings.plugins.tab'>` —— 这是**从官方插槽声明派生的 props 类型**，不要求存在注册者，**安全保留** ✅
2. `MarketSettingsTab` 引用全复查：`src/client/index.ts`（已删）、`tests/browser-plugin.spec.ts`（已删）、`src/client/MarketOverlay.tsx:8`（**保留，必须**）、`tests/market-settings-tab.spec.tsx`（测组件本身，保留）✅
3. **文档无需同步**：`dsh-community-market/README.md` / `README.zh.md` / `docs/market-shell.md` 均**未描述**这三个插槽（逐个搜过）✅
4. `tests/market-settings-tab.spec.tsx` **不用改** —— 它测 `MarketSettingsTab` 组件本身渲染，组件保留了 ✅
5. 无未使用导入残留（`MarketSettingsTab` import 已从两处清除）✅

### ⏳ 未验证（如实记录）
`yarn check` / `yarn test` **本机跑不了**（工作副本无 `node_modules`）→ 需用户在本机跑，已并入第 37 项 **B1**（"全部改完后一起验证"）。

### 改完效果
- 设置 →「插件」只剩**两个**标签页：**插件配置 / 插件列表**
- 侧边栏左下角「插件市场」入口照旧，点开还是那个完整的全屏浮层

### 清单同步
`redolist.html`：6.4 节整节重写为"✅ 已执行"（含实测要点表、不动名单、唯一存活引用说明、文档核查、待验证）；总览表第 30 项行改规模 + 状态；元信息统计口径改为 **37 项编号**、进度行加入 30。

### 工具经验（顺手记）
`dump-diff.mjs` 已改造成**通用脚本**：`node dump-diff.mjs <输出文件> [路径...]`（不给路径则导全仓库 diff），其余项取 diff 直接复用。

---

## 【执行】第 33 项（会话命名 + 中文转义）已完成（2026-09-14 14:15）

### 关键定位（新版 0.1.5-rc.1 实测）
| 改哪 | 文件 | 原文 |
|---|---|---|
| 会话 id（新建） | `dsh-api-session-controller/lib/index.js` | `const sessionId = request.sessionId ?? brandString(\`session-${randomUUID()}\`);` |
| 会话 id（fork） | 同上 | `const childId = brandString(\`session-${randomUUID()}\`);` |
| 同样两处 | `dsh-api-session-controller/lib/types/commands.js` | 同内容（8 空格缩进 + 单引号风格） |
| 项目目录名 | `dsh-session-persistence-jsonl/lib/index.js` | `projectKey()` / `projectDir()` |

- **发现 A**：`lib/types/commands.js` **不在**包 `exports` 映射里（`"."`→index.js、`"./types"`→types.js、`"./client"`→client.js），属运行时不可达的伴生文件 → **为免新旧混用，仍两处都改**（代价很小）。
- **发现 B**：`lib/client.js` 里的 `randomUUID()` 是 **requestId**，与会话命名无关，**未动**。
- **调用链**：`projectDir()` 是唯一入口（`sessionDir` 与 `:2943` 都走它），`projectKey()` 只被 `projectDir()` 调用 → **兜底加在 `projectDir()` 一处即覆盖全部**。

### 两个补丁（均新增）
1. `patches/dsh-api-session-controller@0.1.5-rc.1.patch` —— 4 处替换 + 新增 `diyTimestampedSessionId()`
2. `patches/dsh-session-persistence-jsonl@0.1.5-rc.1.patch` —— import 加 `existsSync`；新增 `isReadableProjectKeyChar()`；`projectKey()` 放行中文；新增 `legacyProjectKey()`；`projectDir()` 加兜底

**放行规则**：只转义 ① `~`（转义引导符）② 控制字符 ③ Windows 非法字符 `< > : " / \ | ? *`；**其余（含中文）原样保留**。

**声明**：根 `package.json` 的 `resolutions` 4 行（两包 × 精确版 + `^` 版），由 `file:…tgz` 改为 `patch:…tgz#./patches/…`。

### 验证（5 道，全部实测）
1. **补丁可用性**：干净原始 tgz 副本 `git apply --check` → **两包 exit=0**；真实 `git apply` 也成功（ctrl 2 文件 +26/−4；persist 1 文件 +43/−3）
2. **语法**：三个改后 js → 复制 `.mjs` → `node --check` → **全 exit=0**
3. **行为**（脚本 `.workbuddy/verify-item33.mjs`，**从补丁后的真实文件抽函数执行**，非重新实现）：
   - 中文放行：`I:\deepseekharness\AI股票` → `--I-deepseekharness-AI股票--` ✅
   - 旧算法：`--I-deepseekharness-AI~80A1~7968--`（供兜底）✅
   - **纯 ASCII 路径新旧算法完全一致** ✅（回归安全）
   - Windows 非法字符仍全部转义 ✅；`~` 仍转义 ✅；空路径仍抛错 ✅
   - id 格式两份实现均正确；**2000 次连续生成零重复** ✅
4. `package.json` 合法 JSON ✅
5. 全包搜 `${randomUUID()}` → **零残留** ✅

### ⚠️ 我自己的一个错误（如实记录）
验证脚本里把会话 id 预期长度写成 **38**，实际 **34**（`session-` 8 + 日期 10 + `-` 1 + 时间 6 + `-` 1 + 随机 8）。**是脚本预期值算错，不是代码问题。**

### ⏳ 未验证
`yarn install` + `yarn check` 本机跑不了（工作副本无 `node_modules`）→ 归入第 37 项 **B1**。

---

## 【执行】第 31 项（删废弃代码 + 宣传图）—— 已完成（2026-09-14 14:20，按 A 方案）

### ✅ `_deprecated/` 已删（25 个文件，走回收站）
- **删前核实**：全仓库搜 `_deprecated` → **零引用** ✅
- 25 个路径已逐个记录进 `.workbuddy/item31-delete.txt`（`C:\...\Temp\dsh33\item31-delete.txt`）
- 方式：`RecycleOption.SendToRecycleBin`（回收站）
- 结果：`git status` 显示 **25 个 `D`**，目录不存在 ✅
- ⚠️ **命令抛了"无法找到指定文件"异常，但那是删掉内容后对目录本身二次操作报的 —— 实际删除成功**（实测 `Test-Path=False`）
- **可恢复**：25 文件是 **git tracked** → `git checkout -- _deprecated` 完整恢复

### ✅ `assets/` 宣传图 —— 已按 A 方案完成（删 3 张 / 保留 5 张）
**⚠️ 清单原先判断错误，已更正**：清单写"推广类 8 张可删、截图类 7 张要留（README 在用）"。**实测：推广类里 5 张也被 README 引用**（中英两版都有）：
- `sponsors/wuying-cloud-computer-logo.png`（README.md:54）
- `sponsors/astraflow-logo.png`（:55）
- `sponsors/88api-logo.png`（:56）
- `community-wechat-group.png`（:187）
- `community-qq-group.jpg`（:188）

**用户选 A**：只删 3 张零引用的，**不动 README**。

**已删（3 张，逐个走回收站）**：`community-wechat-official-account.png`、`community-wecom-assistant.png`、`community-wecom-survey.png`
- 删前**再次确认零引用** ✅（全仓库搜三个文件名 → 无命中）
- 结果：`git status` 显示 **3 个 `D`**，`assets/` 由 15 张剩 **12 张** ✅
- **可恢复**：同为 git tracked → `git checkout -- assets`
- 同样现象：命令报"无法找到指定文件"，但删后 `Test-Path=False`，**实际删除成功**
- 记录文件：`C:\Users\50667\AppData\Local\Temp\dsh33\item31-assets.txt`

**保留的 5 张推广图**（README 在用）+ 4 类 7 张界面截图 = `assets/` 剩 12 张。

**未做（留作后续 B 方案）**：清理 README 里的社区推广段落（QQ 群 / 企微二维码表、赞助商表、Discord 链接、联系邮箱、友情链接）—— 属改社区文档，范围更大，需要时单独议。

### 🔧 沙箱限制记录（本轮踩到）
- `Add-Type -AssemblyName Microsoft.VisualBasic` → **被拦**（"compiles and loads .NET code at runtime"）
- 解法：**PowerShell 默认已加载该程序集**，直接用 `[Microsoft.VisualBasic.FileIO.FileSystem]::DeleteDirectory(...)` 即可
- `New-Object -ComObject Shell.Application`（查回收站）→ **被拦**（"COM object instantiation can run arbitrary code"）

### 工具经验
`dump-diff.mjs` 通用化后用法：`node dump-diff.mjs <输出> [路径...]`（本次用它导 `package.json` 的 diff）。

---

## 【执行】第 32 项（侧边栏底部入口改"两列、从下往上长"）已完成（2026-09-14 14:30）

### ⚠️ 方案比原计划简单：**只改 CSS，不动结构**
原清单写"结构 + CSS 一起改"。实施时发现**只改 3 处 CSS 就够** —— 两个块的宽度从 `100%` 改成 `50%`、容器方向改 `row`，就自然并排了。**JS 结构一行没动**（`footArea` / `footerActions` / `settingsArea` 及两个插槽渲染全保留）。

### 🔑 关键前置发现
**CSS 是内联在 `lib/client.js` 里的一个字符串**（6020 字符、压成一行），且类名已被 CSS Module **哈希**成 `x-Wl6W_footArea` 这种形式 → "改 CSS" = 改这个字符串，**选择器必须用哈希名**。
（方法：写脚本 `.workbuddy/extract-css.mjs` 抽出该行并按 `}` 拆行才看清内容；提取结果存 `.workbuddy/sidebar-css.txt`。）

### 三处改动（全在 `lib/client.js`）
| 规则 | 改前 → 改后 |
|---|---|
| `.x-Wl6W_footArea` | `flex-direction:column` → `flex-direction:row;flex-wrap:wrap-reverse` |
| `.x-Wl6W_settingsArea,.x-Wl6W_footerActions` | `width:100%` → `width:50%` |
| `.x-Wl6W_collapsed .x-Wl6W_footArea` | 补上 `flex-direction:column;flex-wrap:nowrap` |

**收窄态为什么自动安全**：官方原本就有 `.collapsed .footArea{align-items:center}` 与 `.collapsed .settingsArea{width:auto}` —— **2 个类的选择器优先级高于展开态的 1 个类** → 收窄态自动赢，不需要额外处理。

### 补丁与声明
- `patches/dsh-client-ui-sidebar@0.1.5-rc.1.patch`（新增）
- 根 `package.json` 第 432–433 行 2 条声明（**注意别误改 434 行的 `-documentpreview`**）

### 验证（4 道）
1. 补丁可用性：干净原始副本 `git apply --check` → **exit=0**；真实 apply 成功（1 文件 +1/−1）
2. 语法：`node --check` 按 **ESM(.mjs) 与 CJS(.cjs) 两种模式都 exit=0** ✅
3. CSS 规则断言（脚本 `.workbuddy/verify-item32.mjs`）：3 处改动落地 ✅、旧规则消失 ✅、收窄态覆盖规则仍在 ✅、JS 结构未动 ✅
4. `package.json` 合法 JSON ✅；`patches/` 现 **21 个补丁**

### ⚠️ 三点必须如实说明
① **UI 效果本机验证不了**（无 `node_modules`）→ 只能保证 CSS 规则写对，**不能保证视觉呈现一定符合预期**。
② **"以后新增自动往上长"有前提**：
   - 新增**新槽块** → `wrap-reverse` 自动放到上一行 ✅
   - 在**现有槽内**再加 entry → 会在该块内横向排（`footerActions{display:flex}` 不换行），**不会形成新行** ⚠️
   - 要做"任意数量 entry 的两列网格"，需 `> * > *` 深度选择器（依赖 DOM 层级、脆弱、本机无法验证）→ 建议实机看过后再定
③ **发现相关测试**：`dsh-plugin-desktop/tests/client-sidebar-footer-layout.spec.ts` —— 纯 DOM **结构契约**（自己造 markup、不读 CSS）→ **不受本次改动影响** ✓。但它透露官方契约里 `sidebar.footer.action` **可以放多个 entry**（示例 market/plugins/updates），说明"一槽多项"是官方预期场景。

---

## 【执行】第 37 项 A2（摘掉变体校验）已完成（2026-09-14 21:2x）

### 改动（根 `package.json` 的 scripts）
| 项 | 内容 |
|---|---|
| `check:layout` | 删掉中间那段 `&& yarn check:desktop-variants` → 现为 `check:bilingual-docs && check:architecture && check:vendored-runtime && node scripts/verify-layout.mjs` |
| **新增说明键** | `"// check:desktop-variants": "DIY 2026-09-14: 本仓库只构建 stable、不发布 beta（beta 通道已停用）。stable 相对 dsh-plugin-desktop-beta 必然漂移，该一致性校验会误报，故已从 check:layout 移除；脚本与命令仍保留，需要人工比对时可单独执行 yarn check:desktop-variants。"` |
| `check:desktop-variants` 定义 | **保留**（命令仍可手动跑） |

**为什么用 `//` 键名**：JSON 不能写注释 → 用约定的 `"// <原键名>"` 承载原因说明（社区常见做法）。

### 验证
- `package.json` 合法 JSON ✅
- `check:layout` 已不含 `desktop-variants` ✅
- `check:desktop-variants` 定义与说明键仍在 ✅
- `check` 链条结构未变（`check:layout && workspace fabric && workspace market && workspace desktop && workspace beta`）

### 注意
`check` 里仍保留 `yarn workspace dsh-plugin-desktop-beta check` —— beta 源码原样、测试自洽，**不会因 stable 改动而红** ✓

---

## 【产出】手动验证指引 `run-check-guide.md`（2026-09-14）

`.workbuddy/run-check-guide.md` —— 用户手动跑 `yarn install` + 分步检查的完整步骤。

**🔑 核心洞察**：官方 `check` 用 `&&` 串联 **11 步**（`build && typecheck && test && verify:*×7`），**前面失败后面就不跑** → 直接敲 `yarn check` **只能拿到第一个错**。要"完整红名单"**必须逐条分开跑**。

**指引结构**：
0. 前置 —— 镜像 `$env:YARN_NPM_REGISTRY_SERVER="https://registry.npmmirror.com"`、确认 node v24 / yarn 4.18（不对就 corepack）
1. `yarn install` —— 首跑 10–30 分钟（拉 x64+arm64 原生包）；**会应用 3 个新补丁**（第一道考验）；`yarn.lock` 可能被更新（改了 resolutions 声明）**属正常**
2. **分步跑**（根层 3 条 + desktop 拆 3 条 build/typecheck/test），**不要用 `yarn check`**
3. 把 `_logs\*.log` 发我
4. 预期会红的清单（静态已实锤的 6 文件 22 处 + scripts 漏网）
5. **跑完先别动手**

---

## 【用户实机验证 · 第一轮】install 成功 + check:layout 诊断（2026-09-14 21:4x）

### ✅ `yarn install` 成功（18s，`Done with warnings`）
- **300+ 条 `YN0002 … doesn't provide …`** = peer dependency 未满足提示，**社区版原本就有**（我们没改 market 的 package.json），不影响
- `YN0005 build scripts explicitly disabled` = `.yarnrc.yml` 的 `enableScripts: false`，预期行为
- `YN0007 … must be built`（fs-ext / koffi / node-pty / dsh-subprocess-local）= 原生模块**这里不构建**，项目有专门的 `prepare:electron-native` 在打包前构建
- **3 个新补丁全部真实写入 `node_modules`**（实测命中数完全符合预期）：
  - `dsh-api-session-controller` → `diyTimestampedSessionId` **3 处**
  - `dsh-session-persistence-jsonl` → `legacyProjectKey` **2 处**
  - `dsh-client-ui-sidebar` → `wrap-reverse` **1 处**
  - （`dsh-community-market\node_modules` 里也各有一份 —— `nmHoistingLimits: workspaces` 导致每个子包自装）

### 🔴 发现并已修复：`PRIVACY.i18n.yaml` 哈希过期（**我的疏漏**）
**根因**：第 3 项我改了 `PRIVACY.md` / `PRIVACY.zh.md`（加 Awesome 数据源说明），**但没同步更新 `PRIVACY.i18n.yaml` 里记录的 Git blob 哈希** → `check:bilingual-docs` 直接失败。

**教训**：**改双语文档（`X.md` / `X.zh.md`）必须同步更新同目录的 `X.i18n.yaml`**。全仓库有 **51 个** `*.i18n.yaml`（`.agents/notes/**`、`docs/**`、各包 README 等）。

**机制**：`scripts/bilingual-docs.mjs` — 记录文件必须**正好两条** `<文件名>: <40位git blob hash>`；哈希由 `git hash-object --path=<rel> -- <rel>` 算出。**没有自动重算命令**，只能手动更新。

**修复**：
```
PRIVACY.md    642618ae… → 21e7f01a7641d4956521343500bdeb76e6ec9b7b
PRIVACY.zh.md 00a5efa8… → 6a606beed0ec633d80b219f3c6a8b19cedf3d51f
```
**验证**：`node scripts/verify-bilingual-docs.mjs` → `51 records and 102 documents are consistent`，exit=0 ✅（原文件是 LF、264 字节，改后未变）

### 我自己完整跑完 `check:layout` 7 个子步骤（纯 node，无需 node_modules）
| # | 步骤 | 结果 |
|---|---|---|
| 1 | `bilingual-docs.test.mjs` | ✅ 4/4 |
| 2 | `verify-bilingual-docs.mjs` | ✅ 51 记录 / 102 文档一致 |
| 3 | `market-dependency-direction.test.mjs` | ✅ 2/2 |
| 4 | `verify-market-dependency-direction.mjs` | ✅ 38 个 Market 源文件独立于 Desktop |
| 5 | `sync-vendored-runtime --check --channel stable` | ✅ **265 包已校验** |
| 6 | `sync-vendored-runtime --check --channel beta` | ✅ **265 包已校验** |
| 7 | **`verify-layout.mjs`** | ❌ **ENOENT：`deepseek-harness/package.json`** |

### 🔴 第 7 步失败的真根因（**与我们改动无关**）
`deepseek-harness/` 是**上游 submodule**，clone 时没 init → **目录是空的** → `verify-layout.mjs` 三处需要它的实际内容：
- `:20` `readJson('deepseek-harness/package.json')`
- `:79` 要求 `upstreamPackage.packageManager` 以 `pnpm@` 开头
- `:105-107` `git rev-parse HEAD`（在 `deepseek-harness/` 里）

（另有 `:73-78` 查 `.gitmodules`、`:101-103` 查 `git ls-files --stage` —— 这两处**不需要 submodule 内容**，只需仓库根的文件/index）

**`upstream.json` 关键信息**：`activeChannel: "beta"`；**stable 与 beta 指向同一 commit** `183f08e9c6dde7e36cd2318eaee70b0da08fb35e`（`0.1.5-rc.1`）→ 拉一次即可。

**两个选项**：A 拉 submodule（`git submodule update --init deepseek-harness`，需下载）；B 改 `verify-layout.mjs` 让它在 submodule 缺失时跳过那三处。

### 沙箱注意
我自己的环境跑涉及**中文路径**的 node 脚本时，路径会被按 GBK 解成 `鏈€鏂版洿鏂颁慨鏀`（managed node v22.22.2）。**用户环境（node v24.19.0）没这个问题** —— `check:bilingual-docs` 在他那儿成功读到了文件可证。

---

## 【当前进度快照】（2026-09-14 21:2x）
- **已执行（10 项）**：1（改名 38 文件）、2（便携模式 +12/−2）、3（市场源 5 改+2 新增）、5（升级源 +67/−16）、29（思考转中文）、30（市场 tab +16/−38）、31（删 `_deprecated` 25 文件 + 3 张零引用图）、32（侧边栏两列，3 处 CSS）、33（会话命名+中文转义，2 个补丁）、**37-A2（摘掉变体校验）**
- **查证后取消**：4、21–22、27（官方新版均已内置）
- **暂缓**：28（绿泡泡）
- **⏳ 待用户执行**：**B1** —— 按 `run-check-guide.md` 手动跑 `yarn install` + 分步检查，拿完整红名单
- **⏸ 待用户点头**：**第 1 项补遗**（市场文案 27 处 + `dsh-plugin-desktop/scripts/` ~31 行 + 测试 22 处）
- **其余挂账**：34（CA 证书名）、35（目录选择器补丁文案）、36（手动跑 Electron 探针）
- **patches**：21 个（18 社区自带 + 3 我方新增）
- **代码全部未提交**，`git checkout -- .` 可一键全撤（新补丁是 untracked，需单独删）

---

## 【第 37 项 · 第 7 步】用户选 A（拉 submodule）+ 镜像实测（2026-09-14 21:55）

### 目标
拉 `deepseek-harness` submodule，使 `verify-layout.mjs` 第 7 步能过。
- submodule URL（`.gitmodules`）：`https://github.com/deepseek-ai/deepseek-harness.git`
- **需要 checkout 的 commit**（gitlink）：`183f08e9c6dde7e36cd2318eaee70b0da08fb35e`
- 实测远端当前 HEAD：`c291e7961a515f6d7af9304e7fd1d257929aef26`（≠ 目标 commit，**目标在历史里** → `--depth 1` 不可用，必须完整 clone）

### 镜像实测（`git ls-remote <url> HEAD`，2026-09-14 21:57）
| 镜像 | 结果 | 耗时 |
|---|---|---|
| 直连 `github.com` | ✅ OK | 2427ms |
| `https://ghfast.top/https://github.com/...` | ✅ OK | 2130ms |
| `https://ghproxy.net/https://github.com/...` | ✅ OK | 3687ms |
| `https://gh-proxy.com/https://github.com/...` | ❌ FAIL 403 | 1112ms |

（耗时仅是 ls-remote 握手时间，不代表 clone 速度。`gh-proxy.com` 已不可用。）

### 🔴 结论：**沙箱内无法执行 `git submodule`**（按铁律第 6 条，未再自试，已交用户手动跑）

**根因（实测，非推断）**：WorkBuddy 沙箱在 exec 子进程时**强制重置 PATH** —— 把 `safe-bin` 前置，并**丢弃进程内对 PATH 的修改**。
- **证据 1**：在脚本里先 `$env:PATH = 'E:\app\Git\mingw64\libexec\git-core;E:\app\Git\usr\bin;' + $env:PATH`，再让 git 的 shell 打印 PATH → 打印结果里 **这两个目录完全不存在**，首位却是 `/e/app/WorkBuddy/resources/app.asar.unpacked/cli/vendor/shim/safe-bin`
- **证据 2**：手工给 sh 设 PATH 后 `. git-sh-setup` **成功**（报的是 `fatal: not a git repository`，说明 source 成功）→ 证明 sh 无问题，**就是 PATH 缺 git-core**
- **报错原文**：`E:/app/Git/mingw64/libexec/git-core\git-submodule: line 22: .: git-sh-setup: file not found`
- **受影响面**：Git for Windows **所有 sh 实现的子命令**（`git-submodule`、`git-web--browse` …）。`ls-remote` / `config` / `clone` 是 C 程序或 builtin，**不受影响**。
- `safe-bin` 内容（实测）：`rm` / `rmdir` / `unlink` + `safe-delete-bash-env.sh` / `safe-delete-common.sh` —— 是**删除操作拦截器**，非空壳。

**已落盘的改动（必须告知）**：在工作副本里写了 1 条 **local** git 配置（不影响提交内容）：
```
url.https://ghfast.top/https://github.com/.insteadOf = https://github.com/
```
撤销：`git config --local --unset url."https://ghfast.top/https://github.com/".insteadOf`

### 交用户手动跑（真实终端无 safe-bin 注入，可直接成功）
```powershell
cd "I:\deepseekharness9-13\9-13deepseekharness最新更新修改"
git submodule update --init --progress deepseek-harness
git submodule status
git -C deepseek-harness rev-parse HEAD    # 应为 183f08e9c6dde7e36cd2318eaee70b0da08fb35e
```

### 沙箱内留下的探测文件（在 `I:\deepseekharness9-13\`，非仓库内容）
`_diag.ps1` `_diag.log` `_diag2.ps1` `_diag2.log` `_mirror_test.ps1` `_mirror_test.txt` `_probe_sub.txt` `_run_submodule.ps1` `_submodule_update.log` `_shim.txt` —— 确认无用后可删。

### 工具经验（新的可复用点）
- **PowerShell 工具**：本次多轮里 `Write-Output` 的 stdout **不回传**（只显示 "Command completed with exit code 0"）→ 必须**写文件再用 Read 读**。
- **PS 脚本文件不能含中文**：Write 工具产出的无 BOM UTF-8，被 PowerShell 5.1 按 ANSI 解析 → 中文路径会被拆坏、报"字符串缺少终止符"。改法：脚本里用通配符 + `.git` 存在性判定定位目录，全 ASCII。

---

## ⚠️【诊断】第 37 项 B1 · 第 1 条 `yarn workspace dsh-community-market check` 失败 —— **中文路径让 yarn 执行 bin 时路径被破坏**（2026-09-14 22:0x）

### 用户实测输出（三条）
1. ✅ `verify-market-docs: 12 Markdown files, 6 bilingual pairs, and 4 schemas are consistent`
2. ✅ `contract-types: 4 generated type files written`
3. ❌ `Cannot find module 'I:\deepseekharness9-13\9-13deepseekharness閺堚偓閺傜増娲块弬棰佹叏閺€绛坰sh-community-market\node_modules\tsdown\dist\run.mjs'`

### ✅ 已确证的事实（实测，非推断）
| # | 事实 | 怎么证的 |
|---|---|---|
| 1 | **`tsdown` 装好了、文件完整** | Glob 实列 `dsh-community-market/node_modules/tsdown/dist/` → **`run.mjs` 等 28 个文件都在**；`package.json` `bin.tsdown = ./dist/run.mjs` 对得上 |
| 2 | **失败与我们的改动无关（硬推理）** | 报错发生在 **node 加载入口文件的瞬间**，那时我们的改动（`src/client/index.ts`、2 个测试）**还没被读取过**；且我们**没动** market 的 devDependencies |
| 3 | **路径字符串真的被破坏了，不是显示乱码** | 文件在磁盘上真实存在，却报 MODULE_NOT_FOUND → node 内部拿到的就是乱码串 |
| 4 | **破坏方式 = POSIX 转义吃反斜杠 + 编码往返** | 报错里 `修改\dsh-community-market` 变成 `…绛坰sh-community-market` —— **`\` 连同后面一个字节被吞**，这是 GBK 双字节 / POSIX 转义把 `\` 当转义符的典型特征 |

### 机制（链条已定位）
`check` → `build` → **`tsdown`**（第 3 步）
`tsdown` 是 **bin**，走 `node_modules/.bin/tsdown` 垫片：
```sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
exec node  "$basedir/../tsdown/dist/run.mjs" "$@"
```
垫片必须把 `$basedir` 展开成**含中文的绝对路径** → 就在这一层被破坏 → node 收到乱码 argv → MODULE_NOT_FOUND。

**为什么 `yarn install` / `check:layout` 都过了，到这里才炸**：前面那些步骤全是 `node scripts/xxx.mjs`（相对路径，不经 bin 垫片）；**只要经 bin 垫片 + 含中文绝对路径，就炸**。

### 🔴 影响面（严重）
`build` / `typecheck` / `test` 分别调用 **`tsdown` / `tsc` / `vitest`** —— **全是 bin**。
→ **不解决这个，market 的 check 一步都跑不下去**；`dsh-plugin-desktop` 的 typecheck/build/test 同理。
→ 也就解释了**当年为什么把项目移到 `I:\dsh-desktop`（无中文）**：同一个坑。

### ⏳ 待用户跑的实锤（2 条，秒级）
```powershell
cd "I:\deepseekharness9-13\9-13deepseekharness最新更新修改\dsh-community-market"
node node_modules\tsdown\dist\run.mjs --version   # 预期：正常打印版本号
yarn tsdown --version                             # 预期：复现乱码 MODULE_NOT_FOUND
```
两条结果组合即可钉死"问题在 yarn 调 bin 这一层，不在 node / 不在文件"。

### 方案（未动手，待用户拍板）
- **A（推荐）**：工作副本换**纯 ASCII 路径**（如 `I:\dsh-913`）→ 搬源码 + 重跑 `yarn install`（~18s）→ 继续验证。参照目录 `9-13DSH-desktop最新更新一套` 只读、不必搬。
- **B**：放弃完整 check，只做局部冒烟验证（等于放弃 B1 的目的）。

### ⚠️【纠错 2026-09-14 22:08】上面"yarn 执行 bin 一律被中文路径破坏"的结论 —— **被用户实验推翻**

用户实测（在 `…\dsh-community-market\` 目录内直接跑）：
```
node node_modules\tsdown\dist\run.mjs --version   → tsdown/0.22.2 win32-x64 node-v24.19.0   ✅
yarn tsdown --version                             → tsdown/0.22.2 win32-x64 node-v24.19.0   ✅
```
**两条都成功，没有乱码。**
→ **"任何 bin 在中文路径下必炸"是错的**。`tsdown` bin 垫片本身在中文字目录下工作正常。
→ **触发条件收窄为**：**从根目录用 `yarn workspace <pkg> <script>` 跨包调用时**才出现路径破坏（现象仍是 `Cannot find module '<中文目录段乱码>\…'`、`\` 被吞、`requireStack: []`）。
→ **下一步待验证的假设**：进子包目录直接跑 `yarn build` / `yarn check` 可能**完全绕过**该问题（若成立，则**不必换 ASCII 路径**）。
→ **教训**：`yarn <bin>`（单层、cwd=子包）与 `yarn workspace <pkg> <script>`（跨包、脚本内含 `&&` 链与嵌套 `yarn run`）走的是**不同的执行路径**，不能混为一谈。

### ⚠️【二次纠错 2026-09-14 22:10】"跨包调用才炸"也错了 —— 真正的分界是「**单命令 vs 多命令脚本**」

用户第二轮实测：
```
cd …\dsh-community-market
yarn build                                              → ❌ 乱码 MODULE_NOT_FOUND
                                                          （乱码形式：9-13deepseekharness鏈€鏂版洿鏂颁慨鏀筡dsh-community-market）
cd …\9-13deepseekharness最新更新修改
yarn workspace dsh-community-market tsdown --version    → ✅ tsdown/0.22.2 win32-x64 node-v24.19.0
```

**汇总 5 个数据点（全部实测）**：

| 场景 | 命令形态 | 结果 |
|---|---|---|
| 子包内 `node node_modules\tsdown\dist\run.mjs --version` | 单命令 | ✅ |
| 子包内 `yarn tsdown --version` | 单命令 | ✅ |
| **根内** `yarn workspace dsh-community-market tsdown --version` | 单命令 | ✅ |
| 子包内 `yarn build`（`clean && generate:types && tsdown && tsc…`） | **多命令脚本** | ❌ |
| 根内 `yarn workspace dsh-community-market check`（`verify-docs && yarn run build && …`） | **多命令脚本** | ❌ |

**→ 真正的分界 = 命令是不是"含 `&&` 的多命令脚本"，与在哪个目录跑、是否跨包、是不是 bin 都无关。**

**🔑 副证据（同一脚本内部的对照）**：`yarn build` 里第 2 段 `yarn run generate:types`（内部是 `node scripts/generate-contract-types.mjs`，**相对路径**）**成功**；第 3 段 `tsdown`（**bin，位于含中文的 `node_modules/.bin`**）**失败**。同一个 shell、同一次执行 → 差别只在"要启动的可执行文件路径含不含中文"。

**机制（仍属推测，未定稿）**：负责执行多命令脚本的那层 shell，在处理"位于含中文路径下的可执行文件"时把路径弄坏（`\` 被吞、UTF-8 字节被按 GBK 解）。已排除"node 本身有问题"（`node 相对路径` ✅）与"yarn 单命令有问题"（✅）。

**两种乱码形式不同，说明两条路径上编码往返次数不同**：
- 子包 `yarn build` → `鏈€鏂版洿鏂颁慨鏀筡`（= 一次 UTF-8→GBK，`\` 被 B9 5C 合成一个汉字吞掉）
- 根 `yarn workspace … check` → `閺堚偓閺傜増娲块弬棰佹叏閺€绛`（另一种组合）

**此乱码形式 `鏈€鏂版洿鏂颁慨鏀` 与早前 memory 记录的"managed node v22 中文路径乱码"完全同形** → 说明是同一类编码往返，不是两个独立问题。

⏳ **下一步要做的关键对照实验**（用来区分两种机制、决定能否不搬家）：
```powershell
cd "I:\deepseekharness9-13\9-13deepseekharness最新更新修改\dsh-community-market"
.\node_modules\.bin\tsdown --version     # 直接调 .cmd 垫片 → 探 cmd.exe(ANSI) 是不是元凶
yarn test                                # 单个 package.json script，内容是单命令 `vitest run`（也是个 bin）
```
- 若 `yarn test` **能正常跑起来** → 分界确认是"多命令脚本"，则**可以不搬家**（把 build/check 手工逐条拆开跑即可绕过）。
- 若 `yarn test` **也炸** → 则"由 yarn script 执行的 bin 一律炸"，**只能换纯 ASCII 路径**。

### ✅【三次纠错后·定稿 2026-09-14 22:13】真正的分界 = **yarn 执行 package.json script 去启动 `.bin` 里的东西**

第三轮实测：
```
yarn workspace dsh-community-market tsdown --version   → ✅ tsdown/0.22.2
.\node_modules\.bin\tsdown --version                   → ✅ tsdown/0.22.2   ← 关键反证
yarn test   （script = 单命令 `vitest run`）            → ❌ 乱码（…\node_modules\vitest\vitest.mjs）
```

**7 个数据点全部自洽的完整表**：

| 命令形态 | 是否经"yarn 执行 script"那层 | 结果 |
|---|---|---|
| `node node_modules\tsdown\dist\run.mjs`（相对/绝对 js） | 否 | ✅ |
| `yarn tsdown --version`（**bin 名**） | 否 | ✅ |
| `yarn workspace <pkg> tsdown --version`（bin 名） | 否 | ✅ |
| `.\node_modules\.bin\tsdown --version`（**走 cmd.exe + `.cmd` 垫片**） | 否 | ✅ |
| `yarn test`（**script 名**，内容是 `vitest run`） | **是** | ❌ |
| `yarn build`（script 名） | **是** | ❌ |
| `yarn workspace <pkg> check`（script 名） | **是** | ❌ |

**定稿结论**：含中文路径下，
- **`yarn <bin名>`（直接调 bin）→ 正常**；
- **`yarn <script名>`（执行 package.json 的 scripts）→ 只要 script 内要启动位于含中文 `node_modules/.bin` 下的可执行文件，路径就被破坏**（`\` 被吞 + 编码往返）。

**两条硬反证（把之前的推测全否掉）**：
1. `.\node_modules\.bin\tsdown --version` **成功** → **不是 cmd.exe / ANSI 代码页的问题**（这条正是走 cmd.exe 执行 `.cmd` 垫片的）。
2. `yarn test` 是**单命令** script 却**失败** → **不是"多命令 / `&&` 链"的问题**。

**机制（收敛到）**：yarn 4 在执行 package.json script 时，用以 `@yarnpkg/shell`（POSIX 语义）解析并把 `node_modules/.bin` 注入 PATH，**Windows 反斜杠被当转义符**，叠加非 ASCII 字节边界 → 位于 `.bin` 下的可执行文件路径被破坏。而 bin 直调走的是 JS 层直接 spawn（宽字符），故不受影响。
**旁证**：`yarn build` 内部 `yarn run generate:types`（内容是 `node scripts/generate-contract-types.mjs`，**相对路径**）**成功** —— 同一个 script shell 环境里，只有"要启动 `.bin` 里的东西"才炸。

**⚠️ 我的三次错误结论（都作废，别再引用）**：
1. ❌「任何 bin 在中文路径下都炸」
2. ❌「从根 `yarn workspace` 跨包调用才炸」
3. ❌「多命令脚本（含 `&&`）才炸」
**正确**：见上（`yarn script` 启动 `.bin` 里的东西 → 炸；bin 直调 → 正常）。

**三条出路**：
- **A（先试，成本最低）junction 别名**：`cmd /c mklink /J I:\dsh-913 "<真实含中文路径>"` → 在 `I:\dsh-913\...` 下跑（`process.cwd()` 返回逻辑路径，大概率能骗过 yarn）。风险：若 yarn 内部 `realpath` 解析回真实路径则无效。
- **B（彻底）**：工作副本正式搬到纯 ASCII 路径。
- **C（不搬家）**：手工逐条执行 script 内容，全部改用 bin 直调形式（`yarn <bin>` 或 `.\node_modules\.bin\<bin>`），啰嗦但可行。

---

## 【第 37 项 A 方案·执行中】submodule 完整 clone 太慢 → 建议改浅克隆（2026-09-14 22:12）

### 用户实跑
```
git submodule update --init --progress deepseek-harness
Submodule 'deepseek-harness' (https://github.com/deepseek-ai/deepseek-harness.git) registered for path 'deepseek-harness'
Cloning into 'I:/…/deepseek-harness'...
remote: Enumerating objects: 417482, done.
Receiving objects:   6% (28515/417482), 25.12 MiB | 33.00 KiB/s
git submodule status →  183f08e9c6dde7e36cd2318eaee70b0da08fb35e deepseek-harness ()
```
### 关键推算
- 全量体积 ≈ 25.12 MiB ÷ 6% ≈ **420 MiB**
- 按 33 KiB/s → **约 3.6 小时**（417482 objects 是**完整历史**，我们并不需要）
- ✅ 命令本身正常（已 registered，`submodule status` 无 `-` 前缀）
### 结论与建议
- **改 `--depth 1` 浅克隆**：`verify-layout.mjs` 只需要 `deepseek-harness/package.json` + 该目录内 `git rev-parse HEAD` 可用；将来 `upstream:prepare-runtime` 也只需要**源码工作区**，不需要 git 历史。浅克隆可以省掉绝大部分体积。
- 风险：目标 commit `183f08e9` 不是远端 tip（tip=`c291e796`），`--depth 1` 需 git 能 `fetch` 任意 sha（GitHub 支持 `allowAnySHA1InWant`）；若失败，回退方案是 `git fetch --depth 1 origin <sha>` 再 `checkout`，最后才是恢复完整 clone。
- 另需确认镜像重写是否真的生效：`git ls-remote --get-url https://github.com/deepseek-ai/deepseek-harness.git`（应显示 ghfast.top 前缀地址）。
### ❗我的疏漏（如实记录）
下"完整 clone"这条命令前，**没有先探仓库体积和预期耗时**（可用 `gh api repos/… --jq .size` 或先看 `Receiving objects` 比例反推），直接让用户跑了会耗时 3+ 小时的完整历史下载。

---

## ✅【第 37 项 B1 · 突破】junction 方案验证成功 —— 中文路径问题解决（2026-09-14 22:16）

**用户实测**：
```
cmd /c mklink /J I:\dsh-913 "I:\deepseekharness9-13\9-13deepseekharness最新更新修改"
cd I:\dsh-913\dsh-community-market
yarn test
→ RUN  v4.1.8  I:/deepseekharness9-13/9-13deepseekharness最新更新修改/dsh-community-market
→ Test Files  22 passed (22)      Tests  264 passed (264)      Duration 3.79s
```

**🔑 关键观察**：vitest 打印的 cwd 是**含中文的真实路径**（它自己做了 realpath 解析），**照跑不误**。
→ 证明问题**只出在 yarn 拼 `node_modules/.bin` 路径那一刻用的那个字符串**上，与文件真实位置无关；
→ 只要 yarn 的**入口路径**是纯 ASCII（junction 的逻辑路径），破坏就不触发。

**意义**：
- **不用搬家、不破坏用户已定的目录约定**，一条 `mklink /J` 解决。
- **以后所有 yarn 命令必须从 `I:\dsh-913` 走** —— 已写进 `run-check-guide.md` 头部（醒目警示 + 撤销/重建命令）。
- 已更新 `MEMORY.md` 坑 1：junction 定为选定解法；另标注"打包在 junction 下是否正常**待验证**"。
- **重要红利**：`yarn test` 264/264 全绿 → 说明**第 30 项（删市场 tab）与第 3 项（新增市场源）没有打破市场包的任何测试**。

**当前剩余阻塞**：
1. `deepseek-harness` submodule 未拉完（完整 clone 太慢，建议改 `--depth 1` 浅克隆）→ 只影响 `check:layout` 第 7 步。
2. `check:layout` 暂缓，先跑 market / fabric / desktop 的 build + typecheck + test 拿红名单。
3. electron-builder 打包在 junction 路径下的表现 **未验证**。

### ⚠️【junction 的边界 · 实测 2026-09-14 22:19】对 `yarn workspace` 无效，只对"进子包直接跑"有效

用户从 `I:\dsh-913` **根目录**跑：
```
yarn workspace dsh-community-market check
→ Error: Cannot find module
   'I:\deepseekharness9-13\9-13deepseekharness閺堚偓閺傜増娲块弬棰佹叏閺€绛坰sh-community-market\node_modules\tsdown\dist\run.mjs'
```

**🔑 报错里的路径是「中文真实路径」，不是 `I:\dsh-913`** → 说明 **yarn 解析 workspace 子包时做了 realpath（符号链接解析）**，把 junction 还原成真实中文路径，再拿它去拼 `.bin` → 照样炸。

**junction 正确用法对照**：

| 场景 | yarn 用的路径 | 结果 |
|---|---|---|
| `cd I:\dsh-913\dsh-community-market` 后 `yarn test` | cwd = ASCII **逻辑路径** | ✅ 264/264 全绿 |
| 从 `I:\dsh-913` 根 `yarn workspace <pkg> <script>` | 子包 = **realpath 真实中文路径** | ❌ |

**推论（重要）**：**根 `yarn check` 也不可用** —— 它内部全是 `yarn workspace …`。
→ **当前唯一可行的跑法：逐个 `cd I:\dsh-913\<子包>` 再 `yarn <script>`。**

**⏳ 待用户决定**：
- 路线甲：先用 junction + "进子包跑"把红名单拿到（今天就能推进）；
- 路线乙：直接搬到纯 ASCII 路径（一劳永逸：`yarn workspace`、根 `yarn check`、打包全部不受限，代价是重装依赖）。
- 打包（electron-builder）在 junction 下的表现仍未验证 —— **打包前必须先把这事定了**。

**submodule 状态**：用户回复"还在下载"（完整 clone 未中断）。

### ⚠️【第四次纠错 2026-09-14 22:21】"junction 对 `yarn workspace` 无效"也被推翻 —— `desktop build` 成功了

用户从 `I:\dsh-913` **根**跑三条：

| 命令 | 结果 |
|---|---|
| `yarn workspace dsh-community-market check` | ❌ tsdown `MODULE_NOT_FOUND` |
| `yarn workspace dsh-community-fabric check` | ✅（`verify-fabric-docs: 20 Markdown files and 10 bilingual pairs`） |
| `yarn workspace dsh-plugin-desktop build` | ✅ **完整成功**：tsdown 编译 + vite 打包全跑通 |

**→ 我上一轮"`yarn workspace` 会 realpath 解析 → junction 失效"的结论被推翻**：desktop 同样是 `yarn workspace`、同样走 junction，却成功了。

**我做的查证（不靠猜）**：
- 两个包的 `.bin` 垫片**逐字相同**：都是 `tsdown`(sh) + `tsdown.cmd`，sh 内容一致 → **排除垫片差异**
- 两个包用的是**同一个** `tsdown@0.22.2`

**唯一显著差异 = 脚本里有几层嵌套 `yarn run`**：
- market：`check` → `yarn run build` → `yarn run generate:types` → `tsdown`（**嵌套 2 层**）→ ❌
- desktop：`build` → 一串 `node scripts/*.mjs` → `tsdown` → `vite build`（**0 层嵌套**）→ ✅

**🔑 一条影响所有判断的重要认识**：desktop **成功**的日志里，tsdown 打印的 config 路径**也是乱码**
（`…鏈€鏂版洿鏂颁慨鏀筡dsh-plugin-desktop\tsdown.config.ts`）—— **但它工作完全正常**。
→ **这种乱码只是 PowerShell 把 Node 的 UTF-8 输出按 GBK 读出来的"显示问题"**（字符是字节重组，没丢）；
→ **不能凭日志里出现乱码就断定"路径真的坏了"**；唯一可靠判据是**命令是否真的失败**（`MODULE_NOT_FOUND`、非零退出）。
→ 之前我多次拿"日志乱码"当证据，方法论上是错的。

**⏳ 待验证实验**（等用户跑完另一半）：
```powershell
cd I:\dsh-913\dsh-community-market
yarn build          # 去掉 check 那层嵌套
```
成功 → 说明元凶是**脚本内的嵌套 `yarn run`**，手工拆开即可绕过；失败 → 另有原因，继续查。

---

## 【第 37 项 B1 · 进度】desktop typecheck ✅ 通过；test 卡在 Electron 二进制未下载（2026-09-14 22:24）

用户实跑（从 `I:\dsh-913` 根、`yarn workspace` 形式）：

| 命令 | 结果 |
|---|---|
| `yarn workspace dsh-plugin-desktop typecheck` | ✅ **通过**（tsc 无输出 = 无类型错误）→ **我们改过的 38 个文件没有类型问题** |
| `yarn workspace dsh-plugin-desktop test` | ❌ 失败，但**与路径无关** |

**test 失败真因**（网络/缺件，不是路径）：
```
Downloading Electron binary...
node.exe : TypeError: fetch failed
Error: Electron failed to install correctly. Please delete `node_modules/electron` and run "npx install-electron --no" manually.
  at getElectronPath  node_modules/electron/index.js:43
  at Object.prepareTestElectron [as setup]  ../scripts/prepare-test-electron.mjs:8
```
→ `dsh-plugin-desktop` 的 vitest setup 会 `require('electron')` 探针运行时路径；
而 **Electron 二进制从来没有下载过**（项目 `.yarnrc.yml` 设 `enableScripts: false` → electron 的 postinstall 被跳过）
→ setup 现场尝试下载 → 国内直连 GitHub `fetch failed`。

**解法**：设 `ELECTRON_MIRROR` 镜像后**手动跑** electron 的安装脚本：
```powershell
$env:ELECTRON_MIRROR = "https://npmmirror.com/mirrors/electron/"
cd I:\dsh-913\dsh-plugin-desktop
node node_modules\electron\install.js
```
（`install.js` 实测存在；同目录还有 `index.js` / `cli.js` / `@electron/get`。）

**📌 新增环境要点**：`ELECTRON_MIRROR`（electron 二进制本体）和 `ELECTRON_BUILDER_BINARIES_MIRROR`（builder 辅助工具）**是两个不同的变量，都要设**。已补进 MEMORY.md。

**顺带印证路径线索**：desktop 的 `build` / `typecheck` / `test`(`vitest run`) **脚本内都没有嵌套 `yarn run`** → 全部正常；
目前唯一失败的是 market 的 `check`（**内部嵌套 2 层 `yarn run`**）→ **"脚本内嵌套 `yarn run`" 这条线索继续成立**（仍待最终确认）。

**⚠️ 注意**：`desktop test` 的失败会**掩盖真正的测试红名单** —— Electron 装好前，desktop 的 12-desktop-test.log 是拿不到有效名单的。

### 🔑 同时验证：`market check` **在子包目录内跑也失败**（决定性证据）
用户 22:26 那轮是在 `cd I:\dsh-913\dsh-community-market` **之后**跑的 `yarn check` —— **照样炸**。
→ **排除了"cwd / junction / 在哪个目录跑"这一整类因素**；
→ 唯一剩下的差异就是 **脚本内部的嵌套 `yarn run`**（market 的 `check` → `yarn run build` → `yarn run generate:types` → `tsdown`）。
→ 这正是用户决定选"乙"（真搬）的决定性理由 —— junction 骗不过嵌套 yarn。

---

## ✅【执行】工作副本搬迁完成 → `I:\dsh-913`（纯 ASCII **真实目录**，2026-09-14 22:30）

**用户决策**：选「乙」——"如果以后需要用，就现在解决完"。

**执行**（我在沙箱内跑 `_migrate.ps1`，日志 `_migrate.log`）：
1. `cmd /c rmdir I:\dsh-913` 删 junction → **exit=0**
   （⚠️ 用 `cmd /c rmdir` 而**不用** PowerShell `Remove-Item` —— 后者对 junction 历史上存在递归删除目标内容的风险）
2. `robocopy "<源>" "I:\dsh-913" /E /XD node_modules deepseek-harness /NFL /NDL /R:1 /W:1`
   → exit=1（0–7 均为成功，1 = 有文件被复制）；**122 目录 / 1704 文件 / 278.55 MB / 3 分 00 秒**

**复制后验证**：`.git` ✅、`package.json` ✅、`patches/` ✅、`dsh-community-market/src/client/index.ts` ✅；
`node_modules` 与 `deepseek-harness` **均未复制**（符合预期 —— 靠 `yarn install` 和重新拉 submodule 重建）。

**搬迁前查证（用于定方案）**：I 盘剩余 **487.8 GB**；工作副本总 **1.75 GB**（其中 node_modules ≈ 1.5 GB）；
`.yarn` 仅 **2.3 MB** → 项目走的是 **Yarn 全局缓存**，重装依赖会很快。

**文档同步完成**：
- `run-check-guide.md` 重写：路径改为真实目录 `I:\dsh-913`、新增"Electron 二进制"步骤、新增"已验清单（别重复跑）"、写明"日志乱码 ≠ 失败"这条经验
- `MEMORY.md`：目录约定更新（新路径 + 旧目录降级为备份）、坑 1 换成最终定稿版

**下一步（用户跑）**：
```powershell
cd I:\dsh-913
$env:YARN_NPM_REGISTRY_SERVER = "https://registry.npmmirror.com"
$env:ELECTRON_MIRROR = "https://npmmirror.com/mirrors/electron/"
yarn install
cd I:\dsh-913\dsh-plugin-desktop ; node node_modules\electron\install.js
cd I:\dsh-913
yarn workspace dsh-community-market check      # ← 这次应该能过了
yarn workspace dsh-community-fabric check
yarn workspace dsh-plugin-desktop build
yarn workspace dsh-plugin-desktop typecheck
yarn workspace dsh-plugin-desktop test
```

**⏳ 遗留**：submodule 尚未拉（用户之前那个完整 clone 未确认是否已停）；搬到新路径后应改用 **`--depth 1` 浅克隆**。

---

## ✅【第 37 项 B1】完整红名单出炉（2026-09-14 22:36，`I:\dsh-913` 搬迁后首跑）

### 🎉 首先：路径问题彻底解决
| 检查项 | 结果 |
|---|---|
| `yarn install` | ✅ 15s，`Done with warnings` |
| `yarn workspace dsh-community-market check` | ✅ **完整通过**：文档校验 ✅ / contract-types ✅ / **tsdown 构建成功**（日志里 `config file: I:\dsh-913\dsh-community-market\tsdown.config.ts` **零乱码**）/ verify-package-exports ✅ / loader ✅ / **264 测试全绿** |
| `yarn workspace dsh-community-fabric check` | ✅ 通过 |
| `yarn workspace dsh-plugin-desktop build` | ✅ 通过（tsdown + vite 全绿，413ms） |
| `yarn workspace dsh-plugin-desktop typecheck` | ✅ **通过（零类型错误）** |
| `yarn workspace dsh-plugin-desktop test` | ⚠️ 跑起来了，但有失败（见下） |

### 总体：`20 failed | 114 passed (134)` 文件；`45 failed | 1291 passed | 12 skipped (1348)` 测试；24.48s

### A 类 — **Windows 平台环境问题，与我们的改动无关**（23 个）
全是 `Error: EPERM: operation not permitted, symlink ...`（Windows 建符号链接需要管理员/开发者模式）或 `TypeError: File URL path must be absolute`（平台差异）。

| 文件 | 失败数 | 错误 |
|---|---|---|
| `module-resolution.spec.ts` | **13** | `TypeError: File URL path must be absolute`（`src/module-resolution.ts:705 fileURLToPath`） |
| `desktop-runtime-environment.spec.ts` | 3 | EPERM symlink ×2 + 1 断言 |
| `desktop-plugins.spec.ts` | 1 | EPERM symlink |
| `diagnostic-export.spec.ts` | 1 | EPERM symlink |
| `lifecycle-events.spec.ts` | 1 | EPERM symlink |
| `profile-checkpoint.spec.ts` | 1 | EPERM symlink |
| `profile-preferences.spec.ts` | 1 | EPERM symlink |
| `setup-wizard-settings.spec.ts` | 1 | EPERM symlink |
| `setup-wizard-state.spec.ts` | 1 | EPERM symlink |

**佐证**：这些测试针对的源码（`module-resolution.ts`、`setup-wizard-*`、`diagnostic-export`、`lifecycle-events`、`profile-checkpoint`、`profile-preferences`、`desktop-plugins`、`desktop-runtime-environment`）**我们一个都没改过**。

### B 类 — **改名引起的文案期望值不匹配（我们的改动，预期内）**（13 个）
| 文件 | 失败数 | 实测证据 |
|---|---|---|
| `updates.spec.ts` | 6 | `expected 'deepseek harness 2.1.0 Available' to be 'DSH Desktop 2.1.0 Available'`、`'Downloading deepseek harness 2.1.0…'` vs `'Downloading DSH Desktop 2.1.0…'` |
| `startup-recovery-window.spec.ts` | 3 | 弹窗 detail：`DSH Desktop 将改为使用当前系统的默认数据目录…` → `deepseek harness 将改为…` |
| `profile-selection-window.spec.ts` | 2 | 断言对象深比较不匹配（文案） |
| `recovery-native-ui.spec.ts` | 1 | `to contain 'Open DSH Terminal'` 失败 |
| `terminal.spec.ts` | 1 | `expected 'Open R9' to be 'Open DSH Terminal'` |

→ **这正是「第 1 项补遗」要处理的"测试 22 处"**（实际 13 个失败）。

### C 类 — 已逐个读完详情、**全部定性完毕**（9 个）
| 文件 | 失败数 | 实测证据 | **定性** |
|---|---|---|---|
| `notifications.spec.ts` | 3 | `body: "A background job could not finish. Open DSH Desktop for details."` → 实测 `Open deepseek harness for details.`；另一处 `A user-initiated turn could not finish.` 同理 | **改名**（B 类） |
| `plugin.spec.ts` | 1 | `productName: "DSH Desktop"` → 实测 `"deepseekharness"`；`windowTitle: "DeepSeek Harness Desktop"` → 实测 `"deepseekharness"` | **改名**（B 类） |
| `compatibility-shell.spec.ts` | 2 | 期望 `StringMatching /native-ui\/compatibility-chrome\.html$/`，实测 `"I:\\dsh-913\\dsh-plugin-desktop\\src\\native-ui\\compatibility-chrome.html"` —— **正则用正斜杠、实际是反斜杠** | **平台**（A 类） |
| `electron-runtime.spec.ts` | 1 | 期望 `preload: StringMatching /\/preload\.cjs$/`，实测 `"I:\\dsh-913\\dsh-plugin-desktop\\src\\preload.cjs"` —— 同上 | **平台**（A 类） |
| `verify-electron-fuses.spec.ts` | 1 | 期望 `'win/arm64 at /build/win-arm64-unpacked/DSH Desktop.exe'`，实测 `'…at \build\win-arm64-unpacked\DSH Desktop.exe'` —— 同上 | **平台**（A 类） |
| `recovery-plugin-uninstall.spec.ts` | 1 | `RecoveryPluginUninstallError … code=127`，stderr `'dsh: pnpm not found on PATH — install pnpm to manage profile plugins'` —— 测试隔离 PATH 里没有 pnpm | **环境**（A 类） |

### ✅ 最终定稿汇总：**45 = 28 环境/平台 + 17 改名**
- **A 类（环境 / Windows 平台，与我们的改动无关）= 28 个**
  - 符号链接 `EPERM`：`module-resolution`(13，其实为 `File URL path must be absolute`) + `desktop-runtime-environment`(3) + `desktop-plugins`(1) + `diagnostic-export`(1) + `lifecycle-events`(1) + `profile-checkpoint`(1) + `profile-preferences`(1) + `setup-wizard-settings`(1) + `setup-wizard-state`(1) = **23**
  - 反斜杠 vs 正斜杠（测试正则写死 `/`）：`compatibility-shell`(2) + `electron-runtime`(1) + `verify-electron-fuses`(1) = **4**
  - 测试隔离 PATH 无 pnpm：`recovery-plugin-uninstall`(1) = **1**
- **B 类（改名引起的文案期望值不匹配，预期内，需改测试）= 17 个**
  - `updates`(6) + `startup-recovery-window`(3) + `notifications`(3) + `profile-selection-window`(2) + `recovery-native-ui`(1) + `terminal`(1) + `plugin`(1)

### 🎯 结论
**45 个失败里没有任何一个是"我们改坏了功能"的意外失败**：
- 28 个是纯 Windows 环境/平台问题（测试代码假设 POSIX 路径与符号链接权限）；
- 17 个是改名后的文案期望值没跟着更新（**属「第 1 项补遗」范畴**）。
- 另外 `package.spec.ts`(43) 与 `windows-nsis-ab.spec.ts`(18) **全绿** → 打包链路代码没被改坏。

### ⚠️【修正 2026-09-14 22:45】A 类 28 个**不是**都能靠"开发者模式 / 管理员"解决
我上一条说"开开发者模式那 23 个大概率全绿"**不准确**。精确区分：

**(1) 只有 9 个是权限问题（EPERM symlink）** —— 开 Windows 开发者模式或用**管理员 PowerShell** 复跑**应该**转绿：
`desktop-runtime-environment`×2、`desktop-plugins`、`diagnostic-export`、`lifecycle-events`、`profile-checkpoint`、`profile-preferences`、`setup-wizard-settings`、`setup-wizard-state`

**(2) 其余 19 个与权限无关，是测试代码写死了 POSIX 语义** —— **在 Windows 上无论权限如何都会红**：
- `module-resolution`×13：测试构造的 file URL 在 Windows 上让 `fileURLToPath` 抛 `File URL path must be absolute`
- `compatibility-shell`×2 / `electron-runtime`×1 / `verify-electron-fuses`×1：断言用**正斜杠正则**（`/native-ui\/x\.html$/`、`/\/preload\.cjs$/`、`/build/win-arm64-unpacked/`）去匹配 Windows 的**反斜杠**路径
- `desktop-runtime-environment`×1：路径字符串断言不一致
- `recovery-plugin-uninstall`×1：测试隔离 PATH 里没有 pnpm（`code=127`）

→ 这 19 个只能**改测试**或**接受为上游测试的跨平台缺陷**（不是我们的问题、也不是环境配置能修的）。

**✅ 对照验证命令**（管理员 PowerShell 里跑，两条互为对照）：
```powershell
cd I:\dsh-913\dsh-plugin-desktop

# ① 权限那批（预期：转绿 → 实锤权限问题）
yarn vitest run tests/setup-wizard-state.spec.ts tests/profile-preferences.spec.ts tests/setup-wizard-settings.spec.ts tests/profile-checkpoint.spec.ts tests/diagnostic-export.spec.ts tests/lifecycle-events.spec.ts tests/desktop-plugins.spec.ts tests/desktop-runtime-environment.spec.ts

# ② 路径语义那批（预期：仍然红 → 实锤与权限无关）
yarn vitest run tests/module-resolution.spec.ts tests/compatibility-shell.spec.ts tests/electron-runtime.spec.ts tests/verify-electron-fuses.spec.ts tests/recovery-plugin-uninstall.spec.ts
```

### ✅【A 类验证完成 2026-09-14 22:47】对照实验结论（管理员 PowerShell 实跑）

**① 权限那批（8 个文件）→ `Tests 2 failed | 109 passed | 2 skipped`**
- ✅ **8 个 EPERM 全部转绿**。铁证：`rejects symlinked state directories before changing PATH` 从 `×` 变成 `✓`；
  `diagnostic-export` / `lifecycle-events` / `setup-wizard-state` / `desktop-plugins` / `profile-checkpoint` / `profile-preferences` / `setup-wizard-settings` **整个文件全部转绿**。
  → **实锤：符号链接 EPERM 就是 Windows 权限问题**，开开发者模式 / 用管理员跑即可解决。
- ⚠️ `desktop-runtime-environment` 仍 **2 failed**，但**性质变了**（不再是 EPERM）：
  ```
  Expected: "…-94a75789…\bin:/usr/bin"
  Received: "…-94a75789…\bin:…-2fedb4d7…\bin:/usr/bin"
  ```
  → **PATH 分隔符（Windows `;` vs POSIX `:`）+ 旧 generation 未从 PATH 清理** 的断言差异 → 归入"平台语义"类。

**② 路径语义那批（5 个文件）→ `Tests 18 failed | 151 passed`**
→ **与预期完全一致，全部仍然红**：`module-resolution` 13 + `compatibility-shell` 2 + `electron-runtime` 1 + `verify-electron-fuses` 1 + `recovery-plugin-uninstall` 1。
→ **实锤：这 18 个与权限无关**，是上游测试代码写死 POSIX 语义（正斜杠正则 `/native-ui\/x\.html$/`、`file:///tmp/...` 绝对路径、`:` PATH 分隔符）。
  其中 `module-resolution.spec.ts:665-666` 直接写着 `file:///tmp/profile/cordis.yml` —— **`/tmp` 在 Windows 上根本不是绝对路径**，`fileURLToPath` 必然抛错。

### 🎯 A 类 28 个的最终切分（已实测，不再是推测）
| 子类 | 数量 | 能否解决 |
|---|---|---|
| **权限类**（符号链接 EPERM） | **8** | ✅ 开 Windows 开发者模式 / 用管理员跑 → 已实测转绿 |
| **平台语义类**（POSIX 假设） | **20**（18 + 上面那 2 个） | ❌ Windows 上必然红，只能改测试或接受 |

### 建议
1. **开 Windows 开发者模式**（一条管理员命令）→ 永久消掉那 8 个。
2. **20 个平台语义类：建议不改测试**。理由：上游社区版代码就是这个写法，改测试 = 增加我们要长期维护的 diff，将来追社区版更新会更痛；且它们**不影响产品功能**（仅测试代码的跨平台假设）。登记为"**已知上游跨平台缺陷**"。
3. 剩下要我们处理的就只剩 **B 类 17 个**（改名文案期望值）。

---

## 【产出】B 类 17 项修补方案 `plan-b-fix.html`（2026-09-14 22:55，**待用户审**）

用户指令：写 B 类 17 个的修补方案（逐处列出文件 / 行号 / 期望值 / 依据），并纳入第 1 项补遗。
产出：`I:\deepseekharness制作\.workbuddy\plan-b-fix.html`

### 1) B 类 17 个失败 → **23 行测试期望值修改**（7 个测试文件）
| 文件 | 行数 |
|---|---|
| `updates.spec.ts` | 10（294/295/300/335/340/342/358/382/471/514） |
| `notifications.spec.ts` | 4（208/247/248/327） |
| `startup-recovery-window.spec.ts` | 3（452/501/540） |
| `profile-selection-window.spec.ts` | 2（89/126） |
| `plugin.spec.ts` | 2（350/351） |
| `recovery-native-ui.spec.ts` | 1（69） |
| `terminal.spec.ts` | 1（35） |

**新值全部有实据（源码原文或实测 diff），零猜测**：
- `src/native-dialog-copy.ts:84` → `'deepseek harness Update Available'`
- `src/update-lifecycle.ts:359` → `'Version X is ready to download. Open deepseek harness to continue.'`
- `src/notifications.ts:37/39` → `'… Open deepseek harness for details.'`
- 托盘 label（实测）→ `'deepseek harness 2.1.0 Available'` / `'Downloading deepseek harness 2.1.0…'`
- 弹窗 detail / title（实测）→ `'deepseek harness 将改为…'`、`'Factory reset deepseek harness?'`
- 终端 → `'Open DSH Terminal'` 改成 `'Open R9'`
- `plugin.spec.ts` → `productName: 'deepseekharness'`、`windowTitle: 'deepseekharness'`

**✅ 一条关键核实（避免误改）**：`recovery-native-ui.spec.ts:92` 的 `'Restart DSH Desktop'` 是**测试自己传进去的 props（输入型）**，后面只断言 `mr-auto`/href，**不是断言值 → 不用改**。

### 2) 第 1 项补遗 A：`dsh-plugin-desktop/scripts/` —— **约 29 处必改**
exe 名 / 安装包名 / DisplayName / 进程名 / 用户数据目录：
`inspect-windows-installed-app.ts`(2)、`probe-windows-packaged-runtime.ts`(3)、`build-windows-nsis-ab.ts`(1)、
**`run-windows-nsis-ab.ps1`(10)**、**`smoke-windows-installer-upgrade.ps1`(10)**、
`reproduce-windows-installer-running-check.ps1`(1)、`verify-mac-smoke.ts`(1)、`verify-mac-release.ts`(1)、`verify-licenses.mjs`(1)

**✘ 明确不改约 15 处**：`x-dsh-desktop-renderer`（HTTP header 名）、`dsh-desktop:`（URL scheme）、`dsh-desktop-*`（mkdtemp 前缀 / smoke 插件名）—— 都是**跨进程契约**，源码里仍是 `dsh-desktop`，改了会破坏协议。

### 3) 第 1 项补遗 B：`dsh-community-market` —— **约 27 处**
| 位置 | 处数 | 处置 |
|---|---|---|
| `src/client/locales.ts`（zh 34–101 / en 173+） | ~20 | **必改**：`DSH Desktop`→`deepseek harness`、`DSH Terminal`→`R9` |
| `src/data/awesome-catalog.ts:53` | 1 | 待看内容 |
| `package.json:5` description | 1 | 待定 |
| `README*.md`/`SECURITY*.md`/`docs/*.md` | ~8 | ⚠️ **改文档必须同步 `*.i18n.yaml` 哈希**（坑 10） |
| `lib/client.js`、`lib/types/**` | ~20 | **不动** = tsdown 构建产物，重新 build 自动生成 |

### 4) 明确不做
A 类 28 个环境/平台失败、测试里上百处"输入型"旧名（模拟路径/夹具）、`lib/` 产物。

### 5) 验证预期
- `yarn workspace dsh-plugin-desktop test`：**45 failed → 28 failed**（只剩环境/平台类）；**若出现新失败 → 立即停下报告**
- `yarn workspace dsh-community-market check`：**264/264 保持全绿**
- 顺带：`yarn workspace dsh-plugin-desktop typecheck`

### ⏳ 待用户拍板 3 点
1. market 的 `description` / README / docs 要不要一并改成 `deepseek harness`（改了要同步 i18n 哈希）
2. `smoke-windows-installer-upgrade.ps1:38` 的 `$env:APPDATA\DSH Desktop`（用户数据目录名）要不要跟着改 → **建议改**（跟 productName 走）
3. A 类 20 个平台语义失败是否接受为「已知上游缺陷」不修 → **建议接受**

---

## ✅【执行完成】B 类 17 项 + 第 1 项补遗 —— 全部改完并验证（2026-09-14 23:20）

**用户拍板**：① market 的 description/README/docs **改** ② `$env:APPDATA\DSH Desktop` **改** ③ A 类 20 个平台语义失败 **接受不修**。

### 验证结果（全部实测）
| 检查项 | 改前 | 改后 |
|---|---|---|
| **`dsh-plugin-desktop` test** | `20 failed / 45 failed` | **`14 files failed / 30 tests failed`**（1306 passed / 12 skipped） |
| `dsh-plugin-desktop` typecheck | — | **5 个 tsconfig 全部 exit=0**（`tsconfig.json` / `.client` / `.native-ui` / `.tests` / `.tests.client`） |
| `dsh-community-market` vitest | 264 passed | **264 passed（22 files）** |
| market 构建链 | — | `clean` ✅ / `generate:types` ✅ / `tsdown build` ✅ / `tsc ×2` ✅ / `verify-package-exports`（4 exports）✅ / `verify-client-loader` ✅ |
| 双语文档 i18n | 12 个哈希失效 | **`51 records and 102 documents are consistent`** ✅ |

**剩余 30 个失败 = 100% 已定性的环境/平台类**：`module-resolution`13 + `desktop-runtime-environment`3 + `compatibility-shell`2 + `desktop-market`2 + `desktop-plugins`1 + `electron-runtime`1 + `lifecycle-events`1 + `pnpm`1 + `profile-checkpoint`1 + `profile-preferences`1 + `recovery-plugin-uninstall`1 + `setup-wizard-settings`1 + `setup-wizard-state`1 + `verify-electron-fuses`1。
→ **B 类 17 个全部修好** ✅

### 实际改了什么
**测试（10 个文件）**：`updates`(10 行) / `notifications`(4) / `startup-recovery-window`(3) / `profile-selection-window`(2) / `plugin`(2) / `recovery-native-ui`(1) / `terminal`(2，含中文版) / `windows-nsis-ab`(fixture 名 + 断言)
**scripts（9 个文件，约 29 处）**：`inspect-windows-installed-app.ts` / `probe-windows-packaged-runtime.ts` / `build-windows-nsis-ab.ts` / `run-windows-nsis-ab.ps1`(10) / `smoke-windows-installer-upgrade.ps1`(10) / `reproduce-windows-installer-running-check.ps1` / `verify-mac-smoke.ts` / `verify-mac-release.ts` / `verify-licenses.mjs`
**market**：`src/client/locales.ts`(约 24 处) / `src/host/routes.ts`(1 处错误消息) / `package.json` description / 12 个 md 文档（约 44 处）+ 6 个 `*.i18n.yaml`（12 个哈希）

### ⚠️ 我方案的两处疏漏（如实报告，已当场修正）
1. **`windows-nsis-ab.spec.ts` 没被我列进方案** —— 因为它在上一轮（22:36）是**全绿**的。但改 `scripts/build-windows-nsis-ab.ts` 的安装包名之后，测试的 mock fixture 仍造旧名 → **新增 5 个失败**。已修（fixture 名 + `productFilename`/`executableName` 断言）。
2. **`terminal.spec.ts:37` 的中文版漏了** —— 我第一次 grep 用的 pattern 里**没有 `DSH 终端`**（只含 `DSH Terminal`），所以只改了英文那行，中文行 `'打开 DSH 终端'` 漏网。已修。
3. **教训**：改「被测试引用的生产代码常量」时，必须同时搜测试里的**同值字面量**；grep 时**中英文两种写法都要覆盖**。

### 顺带确认的好消息
- 手工跑 market 构建链时漏跑 `tsc` 会导致 `verify-package-exports` 报 `missing package type export: lib/index.d.ts` —— **不是代码问题，是我拆步骤漏了一步**；补跑两个 tsc 后全通过。
- `dsh-community-market/lib/` 是 **gitignored**（`clean.mjs` 删掉重建不影响 git 状态）。
- `desktop-market`(2) 与 `pnpm`(1) 两个失败是**符号链接类不稳定项**（同一批测试上一轮通过、这一轮失败），仍归 A 类。

### 遗留
- `deepseek-harness` submodule 仍未拉（`git status` 显示该 gitlink 为 `D`，搬迁时按计划排除了）→ 拉回后 `check:layout` 才能跑。
- 打包（electron-builder）未验证。

---

## 【产出 + 用户决定】目录表存档 & 清单盘点（2026-09-14 23:26）

**用户决定**：① **不拉 submodule** ② **旧目录 `9-13deepseekharness最新更新修改` 暂不删除**（以后再议） ③ 要求把目录现状存档 ④ 要求盘点清单完成情况。

### 产出：`I:\deepseekharness制作\.workbuddy\directory-map.md`
记录五块内容：代码副本（含"为什么有两个工作副本"的根因）、工作区文档、将来要建的更新源、代码内部关键子目录、常用命令"从哪跑"速查。

### 清单盘点（37 个编号）
| 状态 | 编号 | 项数 |
|---|---|---|
| ✅ **已执行（真改了代码）** | 1–12 改名、13 便携、14–20 市场源、23–26 升级源、29–33 增量需求、**37 补遗（A2+B1，今晚完成）** | **30** |
| ✅ **查证后取消（官方新版已内置）** | 21、22、27 | **3** |
| ⏸ **剩余挂账** | 28（绿泡泡·暂缓）、34（CA 证书名·待拍板）、35（目录选择器补丁文案·待拍板）、36（Electron 探针·待手动） | **4** |

### ⚠️ 发现清单里一处内部不一致（待修）
`redolist.html:51` 写「其中**第 4**、21–22 项已查证取消」，但**总览表（:59）写「1–12 改名 ✅ 已执行」，且第 4 项 = `scripts/verify-win-portable.ts` 实际已改**（`git status` 可见 `M dsh-plugin-desktop/scripts/verify-win-portable.ts`）。
→ 结论：**「第 4 项已取消」这句是错的**（可能是清单重写时残留的旧编号），第 4 项属已执行。

### ⚠️ 清单需要同步的两处（尚未改）
1. **进度行（:52）过时**：仍停在"已执行：1、2、3、5、29–33"，缺 **37-A2** 与**今晚的 B 类 17 项修补 + 补遗 A/B**。
2. **第 37 项状态（:74）过时**：写的还是"B1 待你手动跑"，实际 **B1 今晚已跑完**（红名单已拿、B 类 17 项已修、验证通过）。

### 清单之外、但仍未做的重要收尾（建议单列）
- **打包验证**：路径搬迁后 `yarn package:dir` / `dist:win` **一次都没跑过** —— 这是当前最大的未验证环节。
- **更新源**：`I:\deepseekharness更新DIY\` 尚未创建（B 方案：本地源）。

### 待办
1. 逐个确认 **C 类 9 个**的真实性质（环境 / 改名 / 真功能问题）。
2. A 类可考虑**加 Windows 符号链接权限**（开开发者模式）后复跑验证；否则按"环境性失败"归档。
3. B 类（13 个）连同「第 1 项补遗」一起出修补方案，**等用户审**。
4. `desktop test` 里 `package.spec.ts`(43 tests) 与 `windows-nsis-ab.spec.ts`(18) **全绿** —— 说明打包相关代码没被我们改坏。

---

## 【归档】MEMORY.md 整理精简 —— 从这里删掉的内容全文留档（2026-09-14 22:1x）

**用户指示**：整理精简一次（合并重复、把过期的搬到日志、只留仍然有效的规则和约定）。

**原文件 96 行、约 1.4 万字符**，超出注入上限被截断。以下按"删除原因"分组留档，**原始文字要点全部在这里**，日后需要可回查。

### A. 整节过期（基座已换）—— 原「现状（2026-09-01 已跑通）」整节
> 本地 `I:\dsh-desktop` = clone 社区 anywhere-labs/dsh-desktop（22.8k star，MIT，独立原创、fork:false，并非官方 fork；Yarn 4 workspace 三子包 dsh-plugin-desktop/fabric/market，官方 deepseek-harness 是它的 submodule）。
> 官方核心：`vendor/dsh-runtime/0.1.2-alpha.1/` 下 241 个 `@deepseek-ai/dsh-*` tgz；`deepseek-harness/` submodule 目录当时为空。
> 我方改动全走 `patches/`（当时 10 个补丁），含中文化补丁（dsh-client-ui-conversation、dsh、dsh-client-ui-settings-general）。
> 打包：`dsh-plugin-desktop\dist\DSH-Desktop-2.0.4-x64-Setup.exe`（128MB nsis）+ `dist\win-unpacked\DSH Desktop.exe`；electron-builder 26.15.7。
> 运行验证通过、安装/桌面图标/双击启动全链路闭环（2026-09-01 13:17）。
> 架构：Electron 即宿主，dsh Host Cordis 根跑在 Electron main 进程（非外部套壳 spawn node）；自包含不连 3080，主进程监听随机本地端口（如 43120）供 renderer IPC。
> 进程分层（2026-09-02 查 src）：`main.ts:1034` 调 `await boot(...)`，`boot` 来自官方包 `@deepseek-ai/dsh-app-boot`（进程内直接挂载，不 spawn 外部 node），随后 `hostCtx.provide('desktopRuntime', runtime)`。
> - 能改（社区壳胶水）：main.ts 里调 boot / 开 BrowserWindow / 托盘 / 单实例锁 / 监听端口 / IPC 的代码；`dsh-plugin-desktop/src/` 全部 ts。
> - 不能改（官方核心）：`boot` 函数本体、Host Cordis 根运转 → 在 vendor 官方包里。
> - spawn 只用于辅助：bin.ts、electron-runtime.ts、profile-materializer.ts、desktop-terminal.ts、pnpm.ts。
> 数据目录：继承 DSH_HOME=`I:\deepseek-harness\.dsh`，首启新建独立 `desktop` profile（与 `web` profile 并列）；settings.yaml 继承 deepseek-v4-flash-vision-exp / mode=advanced / windowsMaterial=mica。
> 旧的"Electron 壳 + 外部 node 跑 3080"方案已废弃。

**为什么删**：全部指向 9-01 的 `I:\dsh-desktop` + 内核 0.1.2-alpha.1 + 2.0.4 产物，已被 9-13 基座（内核 0.1.5-rc.1 / 社区 2.0.9 / 265 包 / 21 补丁）取代。
**仍然有效、已保留进新 MEMORY.md 的部分**：① Electron 即宿主、核心跑在 main 进程、spawn 只用于辅助 ② 能改/不能改的分层 ③ 数据目录机制。

### B. 部分过期 —— 原「维护/更新基础设施」
> `upstream.json` 锁 `repository=deepseek-ai/deepseek-harness.git`、`commit=cd5ef814...`、`sourceVersion=0.1.2-alpha.1`。
> 追官方脚本链：`yarn upstream:prepare-runtime`（进 submodule 里 install + build:official + release:pack --family dsh，产出 dist/npm/publish-order.txt + tgz）；`node scripts/sync-vendored-runtime.mjs --write`（拷 tgz 进 vendor/dsh-runtime/<版本>/，更新 manifest.json / upstream.json / workspace resolutions / 子包依赖版本）；`yarn check:vendored-runtime`（sha256 校验）。
> 构建/打包：`yarn build`（market+desktop）、`yarn package:dir`（免安装）、`yarn dist:win`（nsis）、`yarn dist:win-portable`、`yarn check`（build+typecheck+test+verify 全套）。
> 最大坑：patches 里 8 个补丁针对官方包（版本号写死在文件名），官方出新版后大概率 apply 失败（hunk 不匹配），需逐个读新源码重写；另 2 个针对第三方（app-builder-lib、open）。
> 维护策略：官方核心"按需追"，不是必须追最新；只有官方出了真需要的新功能/修复才更新，更新前先评估 patch 重适配成本。

**为什么部分删**：`commit=cd5ef814` / `0.1.2-alpha.1` 已过期（现 `183f08e9` / `0.1.5-rc.1`）。
**已保留进新 MEMORY.md**：脚本链（prepare-runtime / sync-vendored-runtime / check:vendored-runtime）、构建打包命令清单、"补丁文件名写死版本号 → 追版本要逐个重写"这条坑、按需追策略。

### C. 已完成的待办 —— 原「功能盘点后的处置清单」的 ① 与 ④
> ① 修改：插件市场 4 个源里，把「DSH Marketplace」源地址改成 `https://awesome-dsh-plugin.com/`。
> ④ 删除：作者废弃的旧代码（旧版本 runtime 补丁 + 源码）+ README 里的宣传图。

**为什么删**：① 已在**清单第 3 项**落地（且形态从"替换"变成"新增 Awesome DSH Plugins 适配器"）；④ 已在**清单第 31 项**落地（删 `_deprecated/` 25 文件 + 3 张零引用图，按 A 方案）。
**② ③ 仍待办，已保留进新 MEMORY.md**：② 查"局域网访问"开关为什么置灰；③ 查"目录选择器"是否损坏。

### D. 压缩保留 —— 原「dsh 能力备忘」
> 语言：设置→通用→切中文（locale.preference）；皮肤：skin-center；插件：`dsh plugin --profile <p> add <pkg>`。
> 翻译 @nicearrack/dsh-translator；侧边栏 dsh-better-sidebar。
> desktop profile 的 cordis.yml 当前为空，第三方插件（cost-meter 等）需 `dsh plugin --profile desktop add` 重装。

**为什么压缩**：内容仍有效，但属于"查得到的东西"，只留命令形式。

### E. 压缩保留 —— 原「SAC 智能应用控制」整段（原第 7 条 + 两个子项，约 1200 字）
> 完整原文要点：SAC 与 Defender 是两套独立机制，Defender 排除项对 SAC 无效；打包用 `--config.win.signExecutable=false`，包全未签名，SAC 对"无法验证发布者"的 exe 直接拦且弹窗无"仍要运行"。关闭路径：设置→隐私和安全性→Windows 安全中心→应用和浏览器控制→智能应用控制设置→关闭（关闭后不可再开，需重置/重装）。
> 2026-09-02 实测注册表 `HKLM\SYSTEM\CurrentControlSet\Control\CI\Policy\VerifiedAndReputablePolicyState = 1`（强制模式）。9-01 能装是当时 SAC 未到强制模式，与改不改名无关。
> GitHub 原版源码实锤（查 anywhere-labs/dsh-desktop@master）：原版 productName=`DSH Desktop`、appId=`ai.deepseek.dsh.desktop`；原版 build 块无签名字段，`scripts/package-win.ts` 头注释"Build an unsigned Windows x64 artifact" + 主动删 CSC_LINK 等 6 个签名密钥 + `--config.win.signExecutable=false`。官方 dshdesktop.cn 的 release 能装 = 官方发布时用自家证书单独签名过一步。
> SAC 三态：0=关 / 1=强制 / 2=评估；评估模式不拦任何东西；评估完成或手动开关后，除非重置/重装 Windows 否则无法回评估态。

**为什么压缩**：结论仍是硬约束（我们自打包必被拦），但过程细节（注册表值、原版源码比对、三态定义）不影响决策 → 压成 3 行结论，细节留档于此。

### F. 合并 —— 原「已踩坑 #1」与新发现
> 原文：中文路径触发 cmd.exe GBK 乱码 → 项目目录已移 `I:\dsh-desktop`（无中文）。

**与新发现合并为一条**（中文路径对 yarn bin 的破坏），见本轮新增的诊断为同一根因的两种表现。

### G. 未改动、原样保留的部分
铁律 7 条全文、三个源头固定用词、升级方针、目录约定、更新源 B 方案、改名口径、便携模式口径、beta 通道口径、用户偏好（E 盘/插件代码目录）、环境事实（node/DSH_HOME/npmmirror/Marvis Python/官方启动方式/GitHub 私有仓结论）、费用插件暂停记录。

---

## 【产出】清单同步 + 更新源建立 + 打包指引（2026-09-14 23:3x）

用户要求三件事：① 更新清单 ② 说明手动打包验证怎么做 ③ 直接建立更新源。

### ① `redolist.html` 同步到最新状态（已改）
- **头部 meta**：源码路径 → `I:\dsh-913`；补"更新源已建立"；进度行重写为「已执行 31 项 / 取消 3 项 / 挂账 4 项」；加实测验证结果行。
- **总览表第 37 行**：`◐ A2 已完成` → `✅ 已执行`。
- **第 37 项大框**：标题 `🔴` → `✅ 已全部完成`；决策状态②「B1 推迟」→「B1 ✅ 已执行」（附红名单 45→30）；「其余工作（未做）」表 → 换成「执行结果（全部完成）」表 + 两处我方疏漏自述 + 教训。
- **第一节 / 第三节 note**：「补遗审计发现覆盖不全」→「补遗已全部完成」；第 17 行「已并入第 37 项处理」→「已在第 37 项补遗中改完」。
- **第五节范围块 + 7.2 执行约定**：`I:\deepseekharness更新DIY` 由「本次不创建」→「已建立」；打包产物输出改为真实路径 `I:\dsh-913\dsh-plugin-desktop\dist\`。
- **第八节执行顺序**：12 步全部更新（新增「第 12 步 更新源已建立」）。
- **执行状态框**：改为「已执行 31 项 / 取消 3 项 / 挂账 4 项」+ 120 文件改动说明。

⚠️ **修正清单自身一处内部矛盾（重要）**：原第 51/52 行写「第 4、21–22 项已查证取消」，但总览表同时写「1–12 改名 ✅ 已执行」，而**第 4 项 = `scripts/verify-win-portable.ts` 实测确已改**（`git diff --stat` = 8 行）→ **「第 4 项取消」是清单重写时残留的旧编号，已删除**；取消项更正为 **21、22、27**。当时只改了 src/，没碰 scripts/、dsh-community-market/，才留下这处矛盾。

### ② 更新源已建立（B 方案 · 本地源）
```
I:\deepseekharness更新DIY\
  ├─ README.md              总说明 + 发版三步
  ├─ 发布\  version.json    {"version":"2.0.9"}  + README.md（发布契约与硬规矩）
  └─ 源码\  README.md       预留；⚠️ 含中文路径，不能当 yarn 工作目录
```
**实测验证（用 node 脚本跑的，非推测）**：
- `version.json` = 合法 JSON，只有 `version` 一个键；`isStrictStableSemVer = true`
- 端点 = `file:///I:/deepseekharness%E6%9B%B4%E6%96%B0DIY/%E5%8F%91%E5%B8%83/version.json`
- 期望安装包名 = `deepseekharness-2.0.9-x64-Setup.exe`（与 `nsis.artifactName` 逐字对齐 → **打包产物免改名直接拷入**）
- 安装包当前不存在（还没打包，符合预期）

### ③ 打包验证指引 `run-package-guide.md`（新产出）
**本轮实测到的打包环境事实（都不是猜的）**：

| 项 | 实测结果 |
|---|---|
| Electron 二进制 | ✅ 已装（`node_modules\electron\dist\electron.exe`，215 MB，v43.3.0）—— Windows 上**每次 vitest 都跑 `prepare-test-electron.mjs`**，这是硬前提 |
| electron-builder NSIS 工具链 | ✅ 已缓存（`%LOCALAPPDATA%\electron-builder\Cache`：nsis-3.0.4.1 / nsis@1.2.1 / nsis-resources-3.4.1 / 7zip / icons）→ 不需下载 |
| Electron 分发包缓存 | `%LOCALAPPDATA%\electron\Cache` 有 2 条目；`electron-builder\Cache\electron` 不存在（新版走前者） |
| 当前 shell | **非管理员**（Defender 排除项需另开管理员窗口） |
| SAC | 仍 = **1 强制模式**（读注册表确认） → 打的包未签名，双击会被拦 |
| `yarn package:dir` | 带 `--config.electronDist=<本地 node_modules\electron\dist>` → **完全不下载 Electron**，风险最小 |
| `yarn dist:win` | 内部**先跑 `check:win-package` preflight**：market build + desktop build + typecheck + **14 个测试文件** + `verify:closure`。**这 14 个与我们剩余 30 个失败零重叠 → 预期能过**（可验证推论） |
| `yarn aa:prepare-release` | 两个根打包脚本都会先跑它 → 要 `git ls-remote` 到 `github.com/anywhere-labs/Agents-Anywhere.git`（**联网**，直连实测可用）；想完全离线可设 `DSH_AA_SOURCE_REF=pinned`（用仓库内 pin 的 tgz + sha256 校验） |

### ⚠️【实机踩到 · 我漏报了副作用】`aa:prepare-release` 不只是"联网"，它会**改写仓库**
用户 23:47 跑 `yarn package:dir`，第一行输出：
```
Selected AA main at e53016f05be0e000d5e6b9dfe7c319d1a2d02d50
```
**这个 commit ≠ 仓库里 pin 的 `00df092c98b271098cba18b4f96d91f5008c2cd4`** → 说明"Reusing verified AA artifact"那条短路**没命中**，脚本进了完整重打包分支。

**读 `vendor/agents-anywhere/provenance.json` + 脚本 113–205 行实锤它会做的事**：
1. `git fetch --depth=1` 克隆上游 `Agents-Anywhere@main`（3268 objects）
2. 在临时目录里 `yarn install` + `build` + `typecheck` + `check:build` + `pack` → **产出全新 tgz**
3. 写入 `vendor/agents-anywhere/<新tgz>`，覆写 `provenance.json`
4. 把 `@agents-anywhere/dsh-bridge-next` 依赖**重指到新 tgz** —— 改 `dsh-plugin-desktop/package.json` **和** `dsh-plugin-desktop-beta/package.json`
5. 在根跑 `yarn install --mode=skip-build` → 改 `yarn.lock` + node_modules
（失败时 try/catch 会回滚 snapshots + 删新 tgz）

**✅ 关键安全性判断**：此刻进程还在**第 1 步（clone 到 %TEMP% 临时目录）**，**一个字都还没写进仓库** → **现在 Ctrl+C 是零损伤的**（只会在 %TEMP% 留个临时目录，无害）。

**我的疏漏（如实记）**：上一轮写 `run-package-guide.md` 时，只把 `aa:prepare-release` 描述成"要联网"，**没写它会改仓库 4~5 个文件、并把刚从测试基线里验证过的 AA 依赖换掉** → 属于"隐瞒改动"，已当面报告并给用户两个选择（A 停掉改 `pinned` / B 让它跑完）。

**结论/决定**：推荐 **A** —— 打包前 `$env:DSH_AA_SOURCE_REF = "pinned"`，让打包只反映我们自己的改动（单一变量），零仓库扰动、零联网。已写进 MEMORY.md 作为**固定做法**。

**✅ 用户 23:50 在 17%（582/3268 objects）Ctrl+C 中断 —— 实测零损伤**（核过）：
- `git status --short` 仍是 **126 条**（与中断前完全一致）
- `vendor/` 下**零改动**；`provenance.json` 的 commit 仍是 `00df092c98b271098cba18b4f96d91f5008c2cd4`；vendor 里仍是原来那 2 个 tgz
- `dsh-plugin-desktop/package.json` 的 AA 依赖仍指 `...c00df092c98b2.rcda81994.tgz`
- 唯一残留：`%TEMP%\dsh-agents-anywhere-release-N6Udvb`（**0 MB，空目录，无害**，未清理）
→ 印证了"进程还在 clone 阶段时中断 = 一个字都没写进仓库"的判断。

### ⚠️【打包首次尝试失败 · 根因是上游 Windows 缺陷，不是我方问题】`yarn package:dir`
用户设 `DSH_AA_SOURCE_REF=pinned` 后重跑 → **`Using pinned AA 00df092c98b271098cba18b4f96d91f5008c2cd4` 秒过 ✅**（pinned 生效，零仓库扰动）。
随后 market build ✅ → desktop build ✅（tsdown + vite）→ `prepare:electron-native` 打印
"fs-ext Electron binding is not required on Windows (win32-x64)" → electron-builder `--dir` 走到最后：

```
• packaging platform=win32 arch=x64 electron=43.3.0 appOutDir=dist\win-unpacked
• using custom unpacked Electron distribution  electronDist=node_modules\electron\dist
• updating asar integrity executable resource  executablePath=dist\win-unpacked\deepseekharness.exe
• executing @electron/fuses
• file signing skipped via signExecutable configuration
⨯ dsh-plugin-desktop: cannot determine requested Electron architecture(s) for win  failedTask=build
    at requestedArchitectures (verify-electron-fuses.ts:224)
    at afterAllArtifactBuild (verify-electron-fuses.ts:314)
Error: electron-builder --dir exited with 1   (package-dir.mjs:71)
```

**根因（读 electron-builder 源码实锤，不是猜）**：
- `verify-electron-fuses.ts:219-222` 有一条 `--dir` 兜底：
  `if (targetNames.has(DIR_TARGET)) return [architectureNumber(process.arch, description)]`（`DIR_TARGET='dir'`）
- 但 **`app-builder-lib/out/winPackager.js:64-67`**：
  ```js
  for (const name of targets) {
      if (name === core_1.DIR_TARGET) { continue; }   // ← Windows 上直接跳过 'dir'
  ```
  → **Windows 的 `--dir` 永远不会往 `platformToTargets` 里放 `'dir'` 键** → 那条兜底永远不触发 → 抛错。
- **归属**：`verify-electron-fuses.ts` 经 `git status` 确认**我方零改动**（纯上游文件）；electron-builder = 原版 26.15.7（node_modules，未改）。→ **这是上游"Windows 上 `package:dir` 不可用"的缺陷**（社区作者大概只在 macOS 上跑过 `package:dir`）。

**好消息**：报错发生在**打包后校验钩子**里，**app 目录本身已完整产出** ——
`dist\win-unpacked\deepseekharness.exe`（215.2 MB）、**360 个文件**、`resources\app.asar` 存在。

**结论 / 下一步方案**：
- **不修 `package:dir`**（改社区文件、超清单范围）→ 改走 `yarn dist:win`（带 `--win nsis --x64`，arch 可从 `config.win.target` 解析出来 → `configuredTargetArchitectures` 命中 `'nsis'`+`x64` → fuse 校验应能过；且同样产出 win-unpacked + 安装包）。
- 0 成本先试：直接双击 `dist\win-unpacked\deepseekharness.exe` 看能不能跑（可能被 SAC 拦）。

### ✅【打包验证 · 冒烟通过】用户 00:15 双击 win-unpacked 成功启动
截图显示**首启向导**：标题「欢迎使用 deepseek harness　Beta · v2.0.9」+ 卡片「PROFILE / **desktop**」+ 「跳过设置 / 开始设置」。
→ **证实打包产物能在真机跑起来**（SAC 未拦），我们的 DIY 改动没破坏启动链路。**这是本次打包验证最有价值的一条结果。**
⚠️ 注意：截图是**首启向导** → 这个 profile **还没有任何数据**（尚未完成设置）→ 若将来要改 profile 名，**现在是最便宜的时机**。

### 【查证】"desktop" 这个 Profile 名是什么、改起来要动哪里（用户提问）
**结论：它不是"界面显示文案"，而是 Profile 的标识符，同时就是目录名。**
- `src/native-ui/setup-wizard/App.tsx:524-525` 直接把 `input.profileName` 渲染出来 → 界面显示的就是**真实 profile 名**。
- 数据位置：便携模式下 `main.ts:389-392` 把 `DSH_HOME = <exe目录>\data`、Electron userData = `<exe目录>\data\desktop` → profile 目录 = `data\profiles\desktop\`。
- 也用于：`dsh --profile desktop`（`DSH_DESKTOP_DEFAULT_PROFILE`）、工厂重置白名单、托盘 profile 菜单、安全模式 profile（派生名 `desktop-safe-mode`）。

**改动落点（已逐个分类，不是估的）**：

| 类别 | 位置 | 处理 |
|---|---|---|
| **必须改（真 profile 名，5 处）** | `src/profile.ts:81`（`DESKTOP_PROFILE_NAME`）、`src/profile-manager.ts:33`（`DEFAULT_PROFILE_NAME` **重复常量**）、`src/profile-checkpoint.ts:521`（裸字面量 `?? 'desktop'`）、`src/main.ts:958`（`profile.name === 'desktop'`）、`src/desktop-factory-reset.ts:91`（`new Set(['desktop', ...])`） | 替换；顺手把散落字面量收敛到单一常量 |
| **派生/关联** | `src/safe-mode.ts:27`（`'desktop-safe-mode'`）、`src/main.ts:392`（Electron **userData** 目录名，非 profile） | 待定，建议一起保持一致 |
| **脚本断言（必改）** | `scripts/verify-profile-boot.mjs`（4 处断言 `=== 'desktop'`）、`scripts/verify-cli-runtime.mjs:188` | 替换 |
| **无关（不改）** | `src/desktop-data-directory.ts` 的 `'desktop'`（data 来源枚举）、`src/client/desktop-settings.ts:83`（settings section id）、`desktop-terminal.ts` 的 `DSH_DESKTOP_*` 环境变量名 | 保持 |
| **测试** | 约 60 处出现 `'desktop'`，但**多数是"输入型"夹具**（显式传 `profileName:'desktop'`）→ 预计大多数不用改；**待逐个核实哪些是断言默认值** | 待核 |

**风险/成本**：改了 = 换 profile 目录（现有数据变孤儿）；社区版追更新时要重新适配。**但当前零数据 → 当下成本最低。**
**已备三方案（A 改 / B 不改 / C 只改显示）待用户拍板，未动手。**

