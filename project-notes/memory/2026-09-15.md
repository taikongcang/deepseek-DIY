# 2026-09-15 工作日志

> 接 `2026-09-14.md`（同一连续工作会话跨过零点，前面的上下文都在 9-14 那个文件里）。
> 本文件只记 00:00 之后发生的。

## 【用户决定 00:21】第 38 项：Profile 名 `desktop` 改名 —— **本次不改，下次更新时一起改**

用户原话：「这个问题记下来，这次不修改了，下次再更新的时候，来修改。」

→ **已登记为清单第 38 项**（`redolist.html`：总览表新增一行 + 一个专门的说明框），并把头部进度行、执行状态框、第八节执行顺序（新增"第 13 步"）同步更新。

**第 38 项内容（本次查证结论，供下次动手用）**：
- `PROFILE / desktop` **不是显示文案，是 Profile 标识符 = 目录名**（`native-ui/setup-wizard/App.tsx:525` 直接渲染真实 profile 名；数据在 `<安装目录>\data\profiles\desktop\`）。
- **必须改 · 源码 5 处**：`profile.ts:81`、`profile-manager.ts:33`（**上游重复定义了两份同名常量**）、`profile-checkpoint.ts:521`、`main.ts:958`、`desktop-factory-reset.ts:91`。
- **必须改 · 脚本 5 处**：`scripts/verify-profile-boot.mjs`（4 断言）、`scripts/verify-cli-runtime.mjs:188`。
- 关联待定：`safe-mode.ts:27`（`desktop-safe-mode`）、`main.ts:392`（Electron userData 目录 `data\desktop`，非 profile）。
- 不用改：`desktop-data-directory.ts` 的枚举值、`client/desktop-settings.ts:83` 的设置分区 id、`DSH_DESKTOP_*` 环境变量名。
- 测试约 60 处，多数是"输入型夹具"，**动手前必须逐个核实哪些是断言默认值**。
- 改的代价：换 profile 目录（数据变孤儿需迁移）、追社区版更新时冲突高发、名字须目录安全（纯 ASCII、非保留名）。
- ⚠️ **注意**：本次是首启向导、**零数据**，本是最便宜的时机；用户选择与下次社区版更新合并，避免同一处改两遍。

## 【环境核实 00:21】打包/卸载前的现状
| 项 | 实测 |
|---|---|
| 相关进程 | **无**（`deepseekharness` / `DSH Desktop` / `electron` / `dsh` 都没在跑）→ 无文件锁，可以打包 |
| 已安装的旧版本 | **`deepseekharness 2.0.4`**，`DisplayVersion=2.0.4`，卸载串 `"E:\app\deepseekharness\Uninstall deepseekharness.exe" /currentuser`，注册表在 **HKCU**（per-user 安装） |
| `dist\win-unpacked\deepseekharness.exe` | 存在（上次 `--dir` 留下的，会被 `dist:win` 覆盖重建，属正常） |

## 【告知用户的三条结论】
1. **可以打包**（无进程占用）。
2. **不需要新建任何文件夹**：`yarn dist:win` 直接输出到已存在的 `I:\dsh-913\dsh-plugin-desktop\dist\`；已存在的 `win-unpacked` 会被 electron-builder 覆盖重建。
3. **卸载老版本（`deepseekharness 2.0.4`）技术上安全**：它装在 `E:\app\deepseekharness`，与工作副本 `I:\dsh-913` 无关；我们 NSIS 配置**没开** `deleteAppDataOnUninstall` → 不会删 userData（老数据在 `I:\deepseek-harness\.dsh`）。
   ⚠️ **但建议顺序：先打包 → 先确认新包能装/能用 → 再卸载老的**。理由：新包**未签名**，若被智能应用控制（SAC）拦下装不上，先卸就会造成"旧的没了、新的装不上"的空窗（免安装版还能用，但没有正式安装）。

---

## ✅【打包成功】`yarn dist:win` 全绿出包（2026-09-15 00:29）

用户执行 `cd I:\dsh-913; yarn dist:win` → **完整通过，无任何报错**。关键节点（全部实测）：

| 阶段 | 结果 |
|---|---|
| `Using pinned AA 00df092c…` | ✅ 秒过（pinned 生效，仓库零扰动） |
| market build（跑了两遍：根脚本一次 + preflight 内一次） | ✅ |
| **preflight `check:win-package`** | ✅ **13 个测试文件、245 个测试全绿**（`safe-mode` 16 / `verify-packaged-runtime` 74 / `update-download` 23 / `update-checker` 31 / `package` 43 / `verify-win-installer` 4 / `verify-win-portable` 3 / `windows-volume-diagnostics` 15 / `windows-pwsh-sandbox` 13 / `window-options` 11 / `installer-nsh` 2 / `package-win` 7 / `desktop-installer-quit` 3） |
| `verify:closure` | ✅ 4/4；`verify-runtime-closure`：247 个一方节点构成闭包 |
| electron-builder 打 app 目录 | ✅ `dist\win-unpacked`；**这次 `downloading label=electron` 确实下载了 Electron**（`dist:win` 不带 `--config.electronDist`，与 `package:dir` 不同） |
| asar 完整性 + electron fuses | ✅ |
| **NSIS 构建** | ✅ `building target=nsis file=dist\deepseekharness-2.0.9-x64-Setup.exe archs=x64` —— **Defender 排除项生效，没出现 `spawn UNKNOWN`** |
| 签名 | 按设计跳过（`file signing skipped`，含 `__uninstaller.exe`） |
| **收尾校验** | ✅ **`Windows installer verification passed: …deepseekharness-2.0.9-x64-Setup.exe`** |

**也验证了我上一轮的推演是对的**：`dist:win` 带 `--win nsis --x64` → arch 能从 `config.win.target` 解析 → **`verify-electron-fuses.ts` 那个 `'dir'` 兜底不需要，校验正常通过**（对比 `package:dir` 必炸）。

**产物（实测）**：
- `I:\dsh-913\dsh-plugin-desktop\dist\deepseekharness-2.0.9-x64-Setup.exe` —— **129.2 MB**，mtime `2026-09-15 00:29:02`，sha256 `B40B8776C10BDAC558FE77E5D2A0BBCADA046312F95D30F8A19435D81F3313CB`
- `dist\win-unpacked\deepseekharness.exe` —— 215.2 MB
- 附带 `latest.yml`（electron-builder 自带的更新元数据）—— **我们不用它**（更新源读 `version.json`），无害，留着或删都行
- ✅ 已拷入更新源：`I:\deepseekharness更新DIY\发布\deepseekharness-2.0.9-x64-Setup.exe`（**sha256 与源一致**）；`version.json` = `{"version": "2.0.9"}` 与产物版本一致 → **更新源闭环完成**

**清单/记忆已同步**：`redolist.html` 第 11 步 → ✅ 打包成功；头部 meta 与执行状态框加打包结果。

**剩余最后一项**：装一遍新包做**安装测试**（未签名，留意 SAC），之后再决定是否卸载老的 `deepseekharness 2.0.4`。

---

## 【全量盘点 09:2x】用户要求：检查更新所需文件是否分类好 + 找出过时/废除的文件夹和文件

**方式**：只读扫描（`Get-ChildItem` 递归 + 体积统计 + grep 引用校验），**未删除任何东西**。扫描脚本碎片落在 `I:\audit_1.txt`~`audit_4.txt`、`audit_size.txt`、`I:\deepseekharness制作\_audit.ps1`（都是我的临时文件，待清理）。

### 一、更新所需的 8 类文件 —— 都齐，但有三处"分类没做好"

| 类别 | 位置 | 状态 |
|---|---|---|
| ① **官方内核** | **没有源码**；用的是官方**已打包 tgz**：`I:\dsh-913\vendor\dsh-runtime\0.1.5-rc.1\`（266 文件） | ⚠️ 概念要分清：官方**源码**靠 `deepseek-harness` submodule（**已决定不拉**，dsh-913 里该目录不存在，gitlink 处 `D` 状态）；只有追官方版本时才需要 |
| ② 社区版源码（只读参照） | `I:\deepseekharness9-13\9-13DSH-desktop最新更新一套` 267.2 MB | ✅ |
| ③ 我们的工作副本（全部改动） | `I:\dsh-913` **2813.5 MB**，120 文件改动未提交 | ✅ 唯一权威 |
| ④ 我们的补丁 | `I:\dsh-913\patches\` 21 个 | ✅ |
| ⑤ 打包所需 | electron 二进制 + NSIS 工具链（系统缓存）+ `build/` + `installer.nsh` + `vendor/` | ✅ |
| ⑥ 产物 | `dist\deepseekharness-2.0.9-x64-Setup.exe` 129.2 MB | ✅ |
| ⑦ 更新源 | `I:\deepseekharness更新DIY\` 129.2 MB（发布 + 源码 + README） | ✅ 闭环 |
| ⑧ 文档/清单/记忆 | `I:\deepseekharness制作\.workbuddy\` 20.3 MB | ⚠️ 权威文档与一次性产物**混放** |

**分类没做好的三处**：⑴ 官方源码其实不存在（只有打包好的 tgz），概念易混；⑵ `I:\deepseekharness制作` 这个"文档工作区"里混进了**旧原型 + 压测目录共约 1.4 GB**；⑶ `.workbuddy` 里 50+ 个一次性产物和 4 个长期权威文档同层混放。

### 二、dsh-913 内部体积分布（2813.5 MB）
`.git` 234.7 ｜ `dsh-plugin-desktop` 1692.1（其中 node_modules 968 + **dist 710** + lib 7.3）｜ **`dsh-plugin-desktop-beta` 626.3（其中 node_modules 620.6）** ｜ `dsh-community-market` 237.6（node_modules 232）｜ vendor 12.3 ｜ 根 node_modules 1.7 ｜ .yarn 2.3

### 三、过时／废除清单（**只报告，未删除**）
1. `I:\dsh-desktop` **2719.8 MB** —— 2.0.4 时代工作副本，已被 `I:\dsh-913` 完全取代（它自己还带 `_deprecated/`）
2. `I:\deepseekharness9-13\9-13deepseekharness最新更新修改` **1753.5 MB** —— 旧工作副本（中文路径，已搬迁；用户曾说"暂不删，以后再议"）
3. `I:\deepseekharness9-13\9-13deepseekharness更新打包` **0 MB 空目录** —— 当初计划的打包输出，实际未用
4. `I:\deepseekharness制作\dist` **779.8 MB** —— 最早手搓原型（`deepseek-harness-desktop` v0.1.1 / dsh 0.1.1-rc.2 / electron 44）的产物：`DeepSeek Harness Setup 0.1.0.exe`(146MB)、`0.1.1.exe`(146MB) + blockmap + win-unpacked
5. `I:\deepseekharness制作\node_modules` **612.1 MB** —— 上述原型的依赖
6. `I:\deepseekharness制作\{main.js, package.json, package-lock.json, gen_ico.py, gen_icon.py, icon.ico, icon.png, .npmrc, .dsh-cost-meter-inject-test.js, .dsh-web*.log, .electron-*.log}` —— 上述原型遗留
7. `I:\deepseekharness制作\{.bench, mkdirbench_tmp, _mkdirbench, _mkdirbench2, _mkdirverify, _verify}` —— **约 1900 个空目录**（批量建目录压测残留）
8. `I:\deepseekharness制作\{.jt_link, deepseekharness重做}`（空）、`.yarn-global\telemetry.json`
9. `I:\deepseekharness`（I 盘根目录那个）—— 里面只有一个 `AI股票`，**疑似路径写错产生的野目录**，待用户确认
10. 我的探测文件：`I:\deepseekharness9-13\_*` **48 个 / 341 KB** + `I:\audit_*.txt`
11. `E:\app\deepseekharness` **616.7 MB** —— 已装 2.0.4（含运行时 `data\`）
12. `I:\dsh-913\vendor\agents-anywhere\...desktop.c63fc36855ba9...tgz` **520 KB** —— **孤儿文件**（grep 全仓库零引用；现用 c00df092）
13. `I:\dsh-913\deepseek-harness` —— gitlink `D` 状态、目录不存在，但 `.gitmodules` 仍写着 → 悬空
14. `I:\dsh-913\dsh-plugin-desktop-beta` **626.3 MB** —— beta 通道已停用（只构建 stable），但 root `build`/`check` 仍会构建它 → **最大可回收项，改动需立项**
15. `I:\dsh-913\dsh-plugin-desktop\dist` **710 MB** —— 打包输出，win-unpacked 可再生（安装包已入更新源）
16. `.workbuddy\requirements.html` 14 KB —— **已被 `redolist.html` 全量并入**（记忆明确"不再各自演进"）→ 过时副本
17. `.workbuddy\ourdiff.txt` 2.2 MB + 一批一次性产物（`item*-diff.txt`、`yarn_cpu*.txt`、`*.mjs` 探针、`rename-audit-full.txt`、`plugin-index.txt`、`update-report.html` 等）→ 建议归入 `_archive\`

**保留（非废弃）**：`9-13DSH-desktop最新更新一套`(267MB，只读参照)、`I:\deepseek-harness\`(113.4MB，含 `.dsh`/`插件`/其他项目)、`I:\deepseekharness更新DIY\`(129.2MB，更新源)。

**合计可回收 ≈ 6.5 GB**（不算 dsh-913 内部优化）。**未动手，等用户点头。**

---

## 🔴【重大纠错 09:5x】我把用户**刚装好的 2.0.9 安装**错列为"可删" —— 用户质疑后自查，确认是我错

**用户质疑**：「`E:\app\deepseekharness` 这是我安装最新更新的，怎么变成过时，废除的？」

**实测硬证据（这次不靠快照）**：

| 检查 | 结果 |
|---|---|
| `E:\app\deepseekharness\deepseekharness.exe` sha256 | `6F6ADE25942B15840DFB6DFD89B095F1E81C902284559D170C4CB9C2456BE202` —— **与 `I:\dsh-913\...\dist\win-unpacked\deepseekharness.exe` 完全一致** |
| `resources\app.asar` sha256 | `C0DDED53095728A4569AFF719E4D1EE1EC35469CCEF5D6FCCA53F18CC991DE59` —— **同样一致** |
| exe 版本信息 | `Product=deepseekharness`、`FileVersion=**2.0.9**`、`ProductVersion=2.0.9.0` |
| 注册表（HKLM+WOW6432Node+HKCU 全扫） | **只有一条** `deepseekharness **2.0.9**`；**无 2.0.4 残留**（`E:\app` 下也搜不到 2.0.4 的 exe） |
| `E:\app\deepseekharness` 目录 CreationTime | **2026-09-15 00:39:40** → 是**新装**目录 |
| `data\` | `profiles\desktop`(00:40)、`settings.yaml`(**00:59**，已配置 mode=extended/material=off)、`sessions\`(1 文件)、`.credentials.yaml`、`storages\` → **已有真实使用数据** |

**结论：`E:\app\deepseekharness` = 我们**现行 2.0.9 安装**，且**含真实用户数据 → 绝不能删**。用户是对的。**

**我的根因（如实）**：
`00:21` 读过一次注册表（那时是 `deepseekharness 2.0.4`），于是**推断**"旧版本装在 `E:\app\deepseekharness`"；到 `09:2x` 做全量盘点时**直接沿用这条旧快照**，既没重读注册表、也没核对目录内容/版本信息 —— 而这期间用户已经**卸载 2.0.4 → 装上我们的 2.0.9**（目录 ctime 00:39:40 可证）。

**连带影响**：
1. 旧版本 2.0.4 **已不存在** → 我先前"先打包 → 确认新包能用 → 再卸载老的"这个顺序**已无意义**。
2. ✅ **好消息**：等于**清单第 11 步「实机验证」已经完成** —— 未签名新包**安装成功（SAC 没拦）**、已运行、已配置。
3. ⚠️ **第 38 项（Profile 改名）代价变了**：`data\profiles\desktop` 已有真实数据 → 改名**需要数据迁移**，不再是我说的"零数据最便宜"。**用户当初是在"零数据"前提下决定"下次再改"的，该前提已失效，必须请用户重新确认。**

**已更新的文档**：
- `directory-map.md`：撤销第 11 项、更正可回收总量 6.5→**5.9 GB**、新增**第七节「更正记录」（含根因与教训）**
- `redolist.html`：第 11 步 → ✅ **全部完成（含安装测试）**；元信息与执行状态框加"已安装/已配置"；第 38 项加"零数据前提已失效"警告
- `MEMORY.md`：新增**铁律 8**（判定"过时/可删"必须现场取证，严禁沿用上一轮快照）；待清理条目改为 5.9 GB 并移出 `E:\app\deepseekharness`；新增"现行安装"条目

**其余大项已重新复核（避免同类错误）**：`I:\dsh-desktop` = version **2.0.4** / productName `deepseekharness`（mtime 09-01，确为 2.0.4 时代）✅；`9-13deepseekharness最新更新修改` = 2.0.9 但**仍含旧文案 `DSH Desktop 2.1.0 Available`**（= 缺我们后期 B 类修补）✅；`9-13DSH-desktop最新更新一套` = productName `DSH Desktop` / appId `ai.deepseek.dsh.desktop`（确为社区原版）✅。

---

## ✅【全量离线核查 09:5x】保证"改代码 → 构建 → 打包"全程零下载

用户要求：完整核查一次，**保证下次修改/更新/打包时工具不需要重新下载或拉取**。
同时用户就第 38 项给了新决定：**现数据是测试数据、可以丢弃；下次更新时直接重新打包即可，下次依然是全新环境** → **本项维持"下次更新时处理"，无需数据迁移**（已写进 redolist 第 38 项与 meta）。

### 核查结果（逐项实测，非推断）

| 依赖 | 位置 | 实测证据 |
|---|---|---|
| npm 包 tarball | `%LOCALAPPDATA%\Yarn\Berry\cache` | ✅ **1486 文件 / 758.5 MB** |
| Yarn 本体 | corepack `…\node\corepack\v1\yarn\` | ✅ 缓存有 **1.22.22 + 4.18.0**；`package.json` 第 6 行 `"packageManager": "yarn@4.18.0"`；**实测 `yarn --version` → `4.18.0`（离线解析成功）** |
| Electron 本地 dist | `dsh-plugin-desktop\node_modules\electron\dist` | ✅ 75 文件 / 347.3 MB |
| Electron 分发包 zip | `%LOCALAPPDATA%\electron\Cache\<sha>\electron-v43.3.0-win32-x64.zip` | ✅ **137.71 MB**（本次打包已下载 → 下次复用，不再下载；另有 v44.0.0 的 150.16 MB 属旧原型遗留） |
| electron-builder 工具链 | `%LOCALAPPDATA%\electron-builder\Cache\` | ✅ `nsis@1.2.1`(15.7MB) / `nsis-3.0.4.1`(8.2MB) / `nsis-resources-3.4.1`(3.6MB) / `7zip@1.0.0` / `icons@1.2.1` / `downloads` |
| Agents-Anywhere 桥接包 | `vendor\agents-anywhere\…c00df092…tgz` | ✅ **sha256 = `a502c91a…` 与 `provenance.json` 完全一致** → `pinned` 不会报 checksum mismatch；且 desktop 与 beta 两个 `package.json` 的依赖行都指向它 |
| 官方内核 | `vendor\dsh-runtime\0.1.5-rc.1\` | ✅ 266 文件；`manifest.json` + `upstream.json` 均锁 commit `183f08e9…`（与 submodule gitlink 一致） |
| 补丁 | `patches\`(20) + `.yarn\patches\dshmarket-npm-1.38.1-775916754e.patch`(1) | ✅ `package.json` 里 `patch:` 引用路径逐条核对在位 |
| 依赖树状态 | `yarn.lock`（`__metadata.version: 10`）+ `.yarn\install-state.gz`(2.3MB) | ✅ `yarn install` 走快路径 |
| 环境变量 | 进程/用户/机器级 | ⚠️ **`ELECTRON_MIRROR` / `ELECTRON_BUILDER_BINARIES_MIRROR` / `DSH_AA_SOURCE_REF` / `YARN_NPM_REGISTRY_SERVER` 全部为空（未持久化）**；`DSH_HOME` = `I:\deepseek-harness\.dsh`（已持久） |

### ⚠️ 唯一的"会联网"点 + 堵法（这是关键）
`yarn aa:prepare-release` 默认追上游 `Agents-Anywhere@main`：`git ls-remote` + 可能整仓克隆 + 重建 tgz + 改写 `vendor\` / 两个 `package.json` / `yarn.lock`。
→ 必须设 **`DSH_AA_SOURCE_REF=pinned`**；设了之后脚本在读 `provenance.json` 后**直接返回**（在 `resolveCommit()` 之前），**不联网、不拉取、不改仓库**。
该变量**当前未持久化** → 每次新开窗口都要设。**已落成实物**：见下。

### ❌ 仍然必须联网的场景（边界，如实告知，不隐瞒）
1. **追官方新内核**：`git submodule update --init deepseek-harness`（大）+ 在 submodule 里 `pnpm install` / `build:official` / `release:pack`。
2. **改了依赖**（新增/升级 npm 包、`yarn.lock` 变动）→ `yarn install` 需取新包。
3. **升级 Electron / electron-builder** → 需下新 electron zip 与 NSIS 工具链。
4. 首次 clone / 常规 git fetch。
→ 结论：**"只改我们自己的代码再重新打包" = 零下载；"追官方内核" = 必然联网。** 两件事分开看。

### 产出（新增文件）
- **`I:\deepseekharness更新DIY\工具\build-win.cmd`** —— 一键打包：设 `DSH_AA_SOURCE_REF=pinned` + 三个镜像（兜底）→ `cd I:\dsh-913` → `yarn dist:win` → 打印产物位置与"拷进发布\ 并同步 version.json"的提示。
  - 已做格式加固：**纯 ASCII（0 个 >127 字节）、全 CRLF（71 对）、无 BOM、2813 字节** —— 避免 cmd.exe 的 GBK 乱码坑。
  - 可选开关：注释掉的 `DSH_PACKAGE_CHECK_ALREADY_RAN=1`（代码没再改时的快路径）。
- **`I:\deepseekharness更新DIY\工具\README.md`** —— 上面这张离线清单 + 仍需联网的场景 + 发版闭环步骤。
- `I:\deepseekharness更新DIY\README.md` —— 目录树补上 `工具\`。

---

## 【清理 · 批 1】被沙箱拦下 → 按铁律停下，改交用户手动跑（2026-09-15 10:0x）

用户下指令"开始分批清理"。我按规则把工作分成 6 批，先执行**批 1（零风险项）**：

**批 1 定义**：① 我（AI）留下的临时探测文件 ② 纯空目录。全部送**回收站**（可还原），不永久删除。

**清理前前置校验（实测）**：
- 待清目标目录 **9 个，逐个核实「子文件数 = 0」** 全通过：
  `9-13deepseekharness更新打包`(0条目)、`制作\.bench`(161)、`mkdirbench_tmp`(66)、`_mkdirbench`(156)、`_mkdirbench2`(500)、`_mkdirverify`(500)、`_verify`(500)、`.jt_link`(0)、`deepseekharness重做`(0) —— 共约 **1883 个空子目录**
- 待清文件：`I:\` 根 `audit_*/offline_*` **13 个** + `9-13` 下 `_*` **47 个(341.1 KB)** + `制作\_audit.ps1` **1 个**（`_audit_run.txt` 未生成）

**❌ 沙箱拦截**：`Add-Type -AssemblyName Microsoft.VisualBasic` 被安全规则拒绝 ——
`Error: Command blocked for security: Add-Type compiles and loads .NET code at runtime`。
（回收站 API `[Microsoft.VisualBasic.FileIO.FileSystem]::Delete...` 依赖 `Add-Type`。）
→ **按铁律 6：第一次识别到就停下、不试绕过**（不去改用 Shell.Application COM 之类"换个姿势删"）。

**✅ 已确认本次零损伤**：9 个目录全部 `Test-Path=True`；文件数 13/47/1 与清理前完全一致；`I:\clean_b1.txt` 未生成（说明命令根本没执行）。

**产出交接物**：`I:\deepseekharness更新DIY\工具\cleanup-batch1.ps1`
- 先把**完整清单 + 每个目录的文件数**打印出来 → 内置**安全闸**（任何目录若含文件则立即中止、什么都不删）→ 要求输入 `Y` 二次确认 → 逐个送回收站 → 打印结果 + 剩余数，明细写 `I:\clean_b1_result.txt`
- 格式已加固：**UTF-8 带 BOM**（首 3 字节 `239,187,191`，避免 PS 5.1 按 ANSI 读坏中文路径）、`[System.Management.Automation.Language.Parser]::ParseFile` **语法检查 0 错误**、5308 字节
- 给用户的运行命令：`powershell -ExecutionPolicy Bypass -File "I:\deepseekharness更新DIY\工具\cleanup-batch1.ps1"`
- 还原方式：回收站里选中这一批右键"还原"

**后续批次规划（未动）**：批 2 = `.workbuddy` 里约 40 个一次性产物**只移动到 `_archive\`**（非破坏性，动手前先把清单发用户过目）；批 3 = 旧原型（`制作\dist` 780MB + `node_modules` 612MB + 散件）；批 4 = 大块代码副本 `I:\dsh-desktop` 2.72GB + 旧工作副本 1.75GB（**需用户定：压缩归档还是直接删**）；批 5 = 工作副本内部（`dsh-plugin-desktop-beta\node_modules` 620MB 需立项、`dist` 710MB 可再生、孤儿 tgz、悬空 submodule）；批 6 = `I:\deepseekharness` 野目录（待用户确认）。

### ✅【清理 · 批 1 完成】用户手动跑脚本，10:2x 全部成功

用户在管理员 PowerShell 跑 `cleanup-batch1.ps1`（输入 `y`）→ **9 个目录 + 61 个文件全部 OK，无一条 ERR**，脚本自报"目录剩余 0/9、文件剩余 0/61"。

**我做了独立复核（不只看用户输出）**：
- 9 个目标目录：`Test-Path` 全部 False ✅
- 文件数：`I:\` 根 `audit_*/offline_*` = **0**、`9-13` 下 `_*` = **0**、`制作` 下 `_audit*` = **0** ✅
- 共释放：**约 1883 个空子目录 + 61 个文件（371.8 KB）**
- 未动任何非目标项；`E:\app\deepseekharness`（现行 2.0.9 安装）完好

**产出**：`I:\deepseekharness制作\.workbuddy\cleanup-plan.md`（完整清理方案 + 批次总表 + 批 2 保留/归档明细对照）
**新脚本（已备好待用户跑）**：`I:\deepseekharness更新DIY\工具\cleanup-batch2-5.ps1`
- 批 2：`.workbuddy` **42 个**一次性产物 → **移动到 `_archive\`**（非破坏性）
- 批 3：旧原型残留 **16 项**（`制作\dist` 780MB / `node_modules` 612MB / `.yarn-global` / `main.js` / `package.json` / `package-lock.json` / `gen_ico.py` / `gen_icon.py` / `icon.ico` / `icon.png` / `.npmrc` / `.dsh-cost-meter-inject-test.js` / 4 个日志）→ 回收站
- 批 4：`I:\dsh-desktop`(2.72GB) + 旧工作副本(1.75GB) → 回收站（含"回收站容量不足会弹永久删除，可取消"的警告）
- 批 5：`dsh-plugin-desktop\dist`(710MB) + 孤儿 tgz → 回收站；**可选** 再问一次 `beta\node_modules`(620MB)（说明：删后需重装，但包在本地 Yarn 缓存里，**重装无需联网**）
- 收尾：删我本轮产生的 5 个 `clean_*.txt`
- 批 6：`I:\deepseekharness` 野目录 **只报告不删**
- 结构：**逐批打印完整清单（含体积）→ 逐批问 Y**；格式加固 UTF-8 BOM + 语法 0 错误（11598 字节）
- 已核实 `scripts-rename-audit.mjs` 只 import `node:fs`/`node:path` → 删 `制作\node_modules` 不会让它失效

**给用户的批 4 决策**：建议**直接回收站删除**（不做 zip）—— 理由：① 回收站本身就是安全网，随时可还原；② 两个副本内容都能重建（社区版原样 clone + `patches\` 21 个 + `I:\dsh-913`）；③ zip 压 4.5GB 的十几万小文件又慢又占地方。

### ✅【清理 · 批 2/3/4/5 全部完成】用户 10:2x 跑脚本，全部成功

用户在 PowerShell 跑 `cleanup-batch2-5.ps1`，**6 段全部选 y，无一条 ERR**：
- 批 2：**42/42** 移动到 `.workbuddy\_archive\`
- 批 3：**16/16** 回收站（`制作\dist` 779.78MB、`node_modules` 612.09MB、`.yarn-global`、`main.js`、`package.json`、`package-lock.json`、`gen_ico.py`、`gen_icon.py`、`icon.ico`、`icon.png`、`.npmrc`、`.dsh-cost-meter-inject-test.js`、4 个日志）
- 批 4：**2/2** 回收站（`I:\dsh-desktop` 2719.79MB、旧工作副本 1753.48MB）—— **没出现"是否永久删除"弹窗**，说明回收站容量够
- 批 5：**2/2** 回收站（`dsh-plugin-desktop\dist` 710MB、孤儿 tgz 0.5MB）
- 批 5-可选：**1/1** 回收站（`beta\node_modules` 620.63MB）
- 收尾：**5/5**（我本轮的 `clean_*.txt`）

**我的独立复核（实测，不只看用户输出）**：
| 检查 | 结果 |
|---|---|
| 7 条已删路径 | **全部 False** ✅ |
| `I:\deepseekharness制作` 现有内容 | **只剩 `.workbuddy\`** ✅ |
| `I:\deepseekharness9-13` 现有内容 | **只剩 `9-13DSH-desktop最新更新一套`**（社区参照）✅ |
| `.workbuddy` 现有内容 | `_archive`(42 文件) + 9 个保留文件 + `cleanup-plan.md` + 6 个目录（`awesome-catalog`/`backups`/`memory`/`pics`/`tmp-conn`/`tmp-homepaths`）✅ |
| 工作副本依赖 | `dsh-plugin-desktop\node_modules` ✅、`dsh-community-market\node_modules` ✅、根 `node_modules` ✅、`dsh-plugin-desktop-beta` 目录 ✅、`.yarn\install-state.gz` ✅ |
| `vendor\agents-anywhere` | **只剩 pinned 的 c00df092 tgz + provenance.json + README.md**（孤儿已清）✅ |

### 🟡【重要】空间还没真正释放 —— 回收站里压着 7.03 GB
| 指标 | 实测 |
|---|---|
| `I:\` 可用（清理后） | **484.7 GB** |
| `I:\` 可用（9-14 22:00） | 487.8 GB（看起来"变少了"） |
| `I:\$RECYCLE.BIN` 占用 | **7.03 GB** |
→ **删掉的东西全在回收站里**，所以可用空间没涨。**要真正拿回约 7 GB，必须清空回收站。** 建议确认几天没问题后再清。

### ⚠️【连带影响】`beta\node_modules` 被删 → 下次动手前先 `yarn install`
删掉后 Yarn 的一致性状态可能不同步。**离线**恢复（包在本地 Yarn 缓存 758MB 里），只是多几分钟。
（注：stable 打包链路 `dist:win` 本身不碰 beta，但先 install 一次最稳。）

---

## 【查证 · UI 归属】用户问 4 张截图（2026-09-15 10:4x）—— 全部查证完毕，零猜测

**产出新文档**：`.workbuddy/ui-attribution.md`（UI 归属与新旧版差异对照 + 检索范围，可复核）。

### 问题 1：图1 三个图标的悬停小窗，为什么和图2 不一样
- 图1 = 标题栏右侧三个图标，组件 `dsh-plugin-desktop/src/client/DesktopNativeActions.tsx`（`placement="titlebar"`，第 205–275 行）：`SquareTerminal`=打开 R9 / `RotateCw`=重启选项（点开自绘菜单）/ `Wrench`=开发者选项（点开自绘菜单）。
- 它们的悬停提示 = **HTML 原生 `title` 属性** → 由系统/Chromium 渲染的**原生 tooltip**（朴素小框，不受应用 CSS 控制）。
- 图2 = 标题栏左侧**模式徽章**悬停弹出的 `DesktopModeControl`（在 `DesktopFrameTitlebarView.tsx` 内，第 103–163 行）→ **应用自绘 HoverCard 卡片**：标题「切换窗口模式」+ **只列非当前模式**（每项：图标 + 名称 + 两行说明）—— 当前是「扩展模式」，所以卡片里只有「兼容模式 / 增强模式」。
- → 两者本就是**两套不同机制**（原生 tooltip vs 自绘卡片），外观不同属设计使然。
- **没有变**：`DesktopNativeActions.tsx` 我方**零改动**，且 `git diff --stat v2.0.4 v2.0.9` 对该文件**输出为空（字节完全相同）**；模式卡片机制 2.0.4 就存在（旧文件 `ExtendedTitlebar.tsx` 第 110/136 行同样是 `HoverCard`）。
- **变的是文案**（上游自己改的）：`presentationTitle` 桌面外观与行为→**窗口模式**；`switchPresentationMode` 切换显示模式→**切换窗口模式**；`extendedMode` 扩展窗口→**扩展模式**；`presentationIntro`、`compatibilityModeBody` 也重写。
- 我方对该 `.tsx` 的改动**只有 1 行**：`DSH Desktop` → `lisa`（第 178 行；`git diff` 全 13 行、仅 1 处 `-/+`）。

### 问题 2：「打开R9」为什么没有「命令段」
- 文案键 `openTerminal`，落点 5 处：`src/client/desktop-settings-locales.ts`（标题栏悬停 + 设置页按钮）、`src/native-dialog-copy.ts`、`src/tray-locale.ts`、`src/recovery-copy.ts`、市场包 `dsh-community-market/src/client/locales.ts`。
- 上游 2.0.9 = `打开 DSH 终端`；**旧版 2.0.4 DIY（回收站 `$RSDW0V2` 源码实测）= `打开 DIY终端`**（市场包同款文案：「再复制到 DIY终端执行」）；现在（第 1 项改名）= `打开 R9`。
- `命令段` 检索：`I:\dsh-913` 全源码 / 我方文档与归档 / `I:\deepseek-harness` / 官方核心 243 包（新版）+ 旧版核心包 / **社区仓库 13258 提交全历史（`git log --all -S 命令段`）** → **零命中**。→ 该词从未存在；用户很可能记的是旧版 `打开 DIY终端`，已请其确认。

### 问题 3：图3「手机连接」/ 图4 Agents Anywhere 是不是本次新增
- 归属 `@agents-anywhere/dsh-bridge-next`（**社区捆的第三方桥接包**）。图3 = 侧边栏「设置」上方入口（走官方 `sidebar.footer.action` 扩展点；插件自带 `README.md:108`）；图4 = 其弹窗（登录和连接 / 设置 / 运行日志 三页签）。
- 图4 那句「本次登录已超时，请回到插件重试。」出自**插件自己**的代码 `lib/index.js:1353`。
- 新增判定（git 实测）：AA 集成提交 `1e31e4d08e`（2026-09-08）随 **v2.0.7**（tag 2026-09-10）进入；**`v2.0.4`（08-29）的 `package.json` 无此依赖** → 对用户上次用的 2.0.4 **是新增**，但属**社区自 v2.0.7 起**的改动，**非我方、也非 2.0.9 独有**。
- 我方相关：打包用 `pinned`，捆的是锁定的 `c00df092…` 那版 AA。同类入口还有标题栏的「远程控制」按钮（其引导文案指向「手机连接」）。

### 💡 本次查证的关键手段：回收站当资料库用
`Get-ChildItem 'I:\$RECYCLE.BIN' -Recurse -Force -Filter 'desktop-settings-locales.ts'` 找回**三份历史快照**：
`$RSDW0V2`（= `I:\dsh-desktop`，**2.0.4 DIY**，文件 mtime 09-01）、`$RB1GTCS`（= 搬迁前的中文路径工作副本，文案已是 `打开 R9`）、`$RB1GTCS\dsh-plugin-desktop-beta`。
→ **以后核对「旧版文案 / 旧版实现」，先查回收站，比猜快得多**（前提：没清空回收站）。
→ 也说明：批 2–5 的删除**仍可还原**（回收站在），但一旦清空，这套核对手段就没了。

---

## 【指引 · 离线 yarn install + 删除旧源码副本】（2026-09-15 11:3x）

**用户要求**：① 给"离线 `yarn install` 怎么做" ② 删掉 2 个 v2.0.4 旧版源码副本 ③ `I:\deepseekharness` 野目录暂不处理 ④ 回收站他自己手动清。

**实测（本次）**：
- `yarn config get enableNetwork` = **true** → **`YARN_ENABLE_NETWORK=false` 是有效设置**（硬离线：有联网需求就直接报错，不会偷偷下）；`yarn --version` = 4.18.0；默认 `npmRegistryServer` = `registry.yarnpkg.com`（离线不需要镜像）。
- **发现**：`I:\dsh-913\dsh-plugin-desktop-beta\node_modules` **已被重建** —— ctime **2026-09-15 10:34:28**、54099 文件 / **967.9 MB**；根 `node_modules` mtime = 10:34:34 → **10:34 疑似已跑过一次 install**（用户可能已照我上轮建议跑过）。故这一步大概率已完成，再跑一次幂等。
- 本地 Yarn 缓存实测：`%LOCALAPPDATA%\Yarn\Berry\cache` = 1486 文件 / **758.5 MB**（离线可重建）。
- `yarn.lock`、根 `package.json`、`dsh-plugin-desktop/package.json`、`dsh-community-market/package.json` 均为 `M`（我方改名产物）→ 安装别加 `--immutable`。

**删除执行（如实记录）**：
- 已删：`I:\_old_titlebar.txt`、`I:\_old_extstyles.txt`（v2.0.4 旧版源码副本，用户要求）+ 我新产生的 `_ins1..4.txt`（4 个诊断文件）。
- ⚠️ **这 6 个是永久删除、没进回收站**：`Remove-Item` 被沙箱 safe-delete 包装，报 `[safe-delete][SAFE_DELETE_FAIL_CLOSED] … trash-failed`，但复核 `Test-Path` **全部 False**、回收站里搜 `_old_*.txt` / `_ins*.txt` **0 命中** → 文件确实已移除且不可还原。**内容零损失**（那 2 份源码可用 `git show v2.0.4:<路径>` 随时导出）。
- `I:\` 根仍剩 **14 个**探测文件（`_q1_old/_q1b/_q1c/_q2_history/_q3_aa/_q3_aa2/_q4/_q5/_q5b/_rb/_arch/_tail/_core_loc/_verify_after_clean.txt`），已登记为清理**批 7**。

---

## 【install 复核 + 新约定 + v2.0.10 升级预检】（2026-09-15 11:3x–11:5x）

### ① 用户跑的 `yarn install` 复核：✅ 成功
- 用户贴回的输出：`Fetch step` **0s 541ms**、`Link step` **0s 473ms**、`Done with warnings in 1s 509ms` → **没下载任何东西**（离线），树已一致。
- 我独立复核：`.yarn\install-state.gz` mtime = **2026-09-15 11:35:01**；四个 `node_modules`（根/desktop/market/beta）全部存在 ✅。
- **`YN0002`/`YN0086` 警告解释（实测）**：`dsh-community-market/package.json` 的 `git diff` **只有 `description` 一行**（我方改名），**peerDependencies 一个字没动** → 这些"market 不提供官方核心 peer"的警告是**上游正常现象**（9-14 那次 install 也是 `Done with warnings`）。`YN0086` 只是汇总提示。
- 我方 package.json / yarn.lock 改动范围核实无误：根 `package.json` = 8+/7−（3 个新补丁的 `resolutions` 接 `patch:` + `check:layout` 摘掉 `check:desktop-variants` + 一行注释）；`yarn.lock` = **纯 +78 行**（新增那 3 个 patch 包的条目）。

### ② 新约定（用户定）：**对照旧版一律走 GitHub，不再用回收站**
已写入 `MEMORY.md` §三 与 `ui-attribution.md` §五。

### ③ ⚠️ 发现一处我自己的文档事故（如实报告）
2026-09-15 早前我往 `MEMORY.md` 插入的"UI 归属速查 + Agents Anywhere"两条 bullet **先前那次 Edit 实际没落地**（grep 不到），**本次已重新补齐**；`MEMORY.md` 的 `YARN_ENABLE_NETWORK`（§六）与"安装测试已完成"（§九）两条编辑**是在的**。

### ④ 升级预检：社区已出 **v2.0.10**（内核 0.1.5-rc.2）
- `git fetch --tags origin` 实测成功（origin 走 `ghfast.top` 镜像）；拉到 **`v2.0.10`（`697e7d782c`，2026-09-14）**。
- `v2.0.10/upstream.json`：官方内核 **0.1.5-rc.2**，commit **`fb2c4b9e698e30edb738bca4cf0618587db7d203`**（我们在 2.0.9 用的是 0.1.5-rc.1 / `183f08e9`）。
- `v2.0.9..v2.0.10` = **10 个提交**：内核换 rc.2 + 版本号 2.0.10 + **Windows 打包改为不开 ASAR** + 打包校验脚本/测试跟进。
- `541 files changed`（绝大多数是 vendor tgz 与 lockfile 的 rc.1→rc.2 重写）；`patches/` 里 **13 个社区补丁只改名不改内容**（`R100`）→ 官方 rc.2 改动很小。
- ~~**我方 3 个补丁**目标包的 rc.2 tgz 内容与 rc.1 **都不同**（blob SHA 比对）→ 必须**改名 + 实测 apply**~~ ⚠️ **此结论已作废，见下方 12:0x 内容级复核**。
- **撞车面 = 5 个文件**（我方 127 条改动 ∩ 上游 37 个源码/脚本/测试改动）：`dsh-plugin-desktop/scripts/verify-mac-release.ts`、`verify-mac-smoke.ts`、`verify-win-portable.ts`、`tests/package.spec.ts`、`tests/verify-win-portable.spec.ts`。**改名重灾区上游一个没动**（locales/tray/native-dialog/recovery/setup-wizard/market locales）→ 我方改名与 B 类修补不会冲突。
- `package:dir` 的 Windows 缺陷**在 2.0.10 仍未修**（`requestedArchitectures` 未改动）→ 继续用 `dist:win`。
- ⚠️ ASAR 关闭会影响**打包产物结构与 fuse 校验**（`OnlyLoadAppFromAsar` 期望值随 asar 变 ENABLE/DISABLE），升级后要重新实机验证。

### 遗留（未动）
1. `I:\deepseekharness` 野目录 —— 实测 **0 MB**，内容只有一个空的 `AI股票` 目录；待用户确认
2. `I:\dsh-913\deepseek-harness` 悬空 submodule 条目 —— 属改仓库，需另立项
3. 两个清理脚本 + 我新产生的 `I:\_verify_after_clean.txt` —— 确认回收站内容不需还原后手工删

**已同步文档**：`cleanup-plan.md`（批次状态→全 ✅ + 回收站提醒 + 连带影响）、`directory-map.md`（代码副本表标注已删、工作区表重写、清理进度全 ✅、header 日期）、`MEMORY.md`（待清理条目改为"已执行 + 回收站 7.03GB + 先 yarn install"）。

---

## 【版本内容级复核：官方 rc.2 与社区 2.0.10 到底改了什么】（2026-09-15 11:50–12:1x）

用户问"这次官方更新了什么？DSH 又更新了什么？是新东西还是小优化？" → 做了完整的双向核查（本地 git + GitHub API + 解包逐文件比对），产出文档 **`.workbuddy/version-diff-2.0.9-2.0.10.md`**。

### 官方内核 0.1.5-rc.1（`183f08e9`）→ 0.1.5-rc.2（`fb2c4b9e`）
- **只有 4 个提交**（2 个是 merge），实质代码提交 **1 个**：`060323d` `feat(web): backport feedback and file refinements to 0.1.5`（把 PR #3928 / #3902 的选定改动回植到 0.1.5 分支，`Refs #3927`/`#3903`；stats 778+/778-）。
- **官方 release notes（`dsh-v0.1.5-rc.2`）原文 = 「体验优化 / Improvements」两条**：① 反馈提交改弹窗确认（点赞和点踩都走弹窗，失败保留已填内容并提示）；② 交付文件卡片排版 + 对话间距 + 更新代码文件图标。**官方自己归类为优化，不是新功能。**
- **包总数 265 → 265，新增 0 / 删除 0 → 没有任何新包。**
- 内容级实测（解包逐文件比对、忽略 `package.json` 版本行）：
  - **真有功能变化的**：`dsh-client-ui-primitives`（`lib/index.js` 396049→431412，**+35KB**；新增 `code-file-icon-artwork.d.ts`，删 `DocumentFileIcon.d.ts`/`MessageText.module.css`/`markdown/MessageText.d.ts`）＝"代码文件图标"；`dsh-client-ui-message-feedback`（**12 个文件变**，含 `controller/dialog/FeedbackDialog/slots/surface.d.ts` + `lib/client.js`）＝"反馈弹窗确认"；`dsh-client-ui-deliverables`（`client.js` +140B）＝"交付卡片排版"；`dsh-client-ui-chat`（`client.js` -500B，新增 `turn-tail-spacing` 测试）＝"对话间距"；`dsh-web-frontend`（bundle 重建）；`dsh-client-ui-tool`/`dsh-client-ui-sidebar` 微调。
  - **只删了孤儿构建产物**（主 bundle 一字未改 → 证明无引用）：`dsh-session-persistence`(3 个 `.d.ts`)、`dsh-session`(`chunk-rows.*`)、`dsh-agent`(`inbox.*`)、`dsh-subagent`(`descriptor-seed.*`)、`dsh-message-feedback`(`spec.*`)、`dsh-client-ui-chat`(3 个 `.d.ts`)、`dsh-client-ui-tool`(1 个 `.d.ts`)。

### 社区 v2.0.9 → v2.0.10（10 个提交，541 路径 / 其中 476 是 vendor tgz）
- `c738b8f3` 换内核 pin；`8dd2cf04` 重建 vendored runtime（265 tgz + 539 条 resolutions；**13 个社区补丁只改名、内容逐字节相同 R100**；明确 "No src/ changes."）；`c84837d5` 版本号→2.0.10。
- **唯一行为变化 = 打包不再使用 ASAR**（`09070dd7`/`c7e1dbc9`/`83cc4f82`/`fea9287036`）：
  `"asar": {"smartUnpack":true}` → **`"asar": false`**；fuse `enableEmbeddedAsarIntegrityValidation` 与 `onlyLoadAppFromAsar` 均由 `true` → **`false`**；删掉 mac/win/linux 三处 `asarUnpack` 清单；`win` 加 `"compression": "normal"`；新增 `src/packaged-filesystem-smoke.ts` + 测试；`verify-electron-fuses.ts`/`verify-packaged-runtime.ts`/`verify-win-portable.ts` 跟进适配。
  → 产物从 `resources/app.asar` 变为**解包目录**；属打包层变化，**升级后必须重新实机验证**。
- `package:dir` 的 Windows 缺陷 **2.0.10 仍未修** → 继续用 `dist:win`。

### ❗我在此前一轮的一处错判（已更正 MEMORY.md 与本节上文）
先前我用 **tgz 的 git blob SHA 不同** 判定"我方 3 个补丁的目标包内容都变了"。**这是错的** —— `manifest.json` 里 **265 个包的 sha256 全部不同**，因为每个包内 `package.json` 的版本串 `0.1.5-rc.1`→`-rc.2` 都变了。**sha256/blob SHA 差异不能用来判断"包是否真的改过"。**
正确结论（解包逐文件实测）：`dsh-api-session-controller` **0 处改动**、`dsh-session-persistence-jsonl` **0 处改动**（两者改名后必然能 apply）；只有 **`dsh-client-ui-sidebar` 的 `lib/client.js` 真变了**（22534→22505，-29B）→ 这个补丁（侧边栏两列布局，正打在 `client.js`）**必须实测、可能需重写**。
→ **教训**：判断"二进制包是否改过"要用「解包后逐文件比对（忽略版本行）」或「体积 delta 是否超出 ±1~7 字节的版本串噪声」，不能看 sha256。

---

## 【用户三问：剥离「手机连接」／卸载按钮／插件存放路径】（2026-09-15 12:10–12:3x）

用户提出三个问题，全部按实据查证，产出文档 **`.workbuddy/plugin-extraction-and-uninstall.md`**，并在 `redolist.html` 登记为 **第 39（升级 v2.0.10 + 目录 `I:\dsh-915`，已定待开工）/ 40（剥离 AA，待拍板）/ 41（全部插件加卸载按钮，待拍板）** 项。

### 1. 剥离「手机连接」（AA）= 第 40 项
- **AA 不是普通插件，是 Desktop 主动注入并锁定的集成**：`src/profile.ts:527` 在开关打开时**强制把 `@agents-anywhere/dsh-bridge-next` push 进 bundle 列表**（profile 清单里根本没有它，实测 `data\profiles\desktop\package.json` 的 bundles 只有 dsh-base + dsh-web-app）；关闭时在 `517-522` 剔除，并有 `isAaEntry` 过滤 + 注释「用户层不能绕过 Desktop 的选择」（`753-757`）。
- 包从 **app 自己的 node_modules** 解析（`538-541`，install anchor 优先）→ 它是 `dsh-plugin-desktop` 的依赖，随 app 打包；交付链 = `vendor/agents-anywhere/<tgz>`（锁 `c00df092`）+ `scripts/prepare-agents-anywhere-release.mjs` + 根 `aa:prepare-release`。
- AA 的 config 注入（`dshHome`/`connectorSourceDir`，`970-977`）**非必需**：`lib/index.js:318` 有 `./bundled-connector/` 默认值、`:6019` 回落 `process.env.DSH_HOME`，而我们的便携模式已设该环境变量（`main.ts:389-392`）。旧版要注入是因为 ASAR；**v2.0.10 关掉 ASAR 后该隐患消失**。
- **AA 包资质**：`0.1.0-dev.0.desktop.c00df092…`（社区本地构建变体），有 `dsh.bundle.patch` → **合法 profile bundle**；有 `dsh.client`（web）→ 侧边栏入口是它自己的；`lib/bundled-connector/` 含 pyproject.toml + connector/cli.py。
- **npm 上不存在**：`registry.npmjs.org` 与 `registry.npmmirror.com` 对该包均 **404** → 只能本地 tgz 分发。peerDependencies 锁 `0.1.5-rc.1`。
- 三个方案：**A 真剥离**（改 Desktop 4 处集成，长期 diff 成本高）/ **B 只用现成开关**（设置→桌面设置→关闭手机连接，零风险，文件仍在）/ **C 折中**（删依赖+vendor+prepare 步骤，保留开关，并把"层不可用"从报错降级为静默跳过）。**我建议先 B，若真要剥离则做 C，A 不推荐。**
- 关键交互：**AA 的加载由 Desktop 开关决定，不由 profile 决定**（装进 profile 也不会加载，除非开关打开）。

### 2. 卸载按钮 = 第 41 项
- **功能早就有且通用**：按钮仅当 `action === 'uninstall'`（`MarketSettingsTab.tsx:1462`）← `bundle.uninstallable`（`host/routes.ts:525`）← `uninstallable = mutable && profile.dependencies 里含它`（`desktop-plugins.ts:435`）；`mutable` = 不在 **`IMMUTABLE_BUNDLES`**（`42-47`）= {`@deepseek-ai/dsh-base`、`@deepseek-ai/dsh-web-app`（来自官方 `dsh-app-boot` `PROFILE_TEMPLATES.web.bundles`，`lib/index.js:333-336`）、`@deepseek-ai/dsh-desktop-app`、`dsh-plugin-desktop`、`dsh-plugin-desktop-beta`、`dsh-community-market`}。
- 官方文档口径：「产品自有 bundle 只读，其他直接依赖可以移除」「无论由哪个市场或 CLI 安装，都用同一流程」（`dsh-community-market/docs/install-and-uninstall.zh.md:45,50`）。
- → **自己装的第三方插件本来就有卸载按钮**；截图那两条是产品自有 bundle。强开（缩小 IMMUTABLE_BUNDLES）会让人把内核卸掉 → `profile.ts:904-906` 直接抛错、只能进恢复助手。**建议维持现状 41-a。**

### 3. 插件存放/安装路径 = 用户问题三
- 安装位置 = **Profile 目录** `<DSH_HOME>\profiles\<profile>\node_modules\<包>`；便携模式下 `DSH_HOME = <安装目录>\data` → 现为 `E:\app\deepseekharness\data\profiles\desktop\`（含 `package.json` 清单 / `cordis.patch.yml` 用户补丁层 / `node_modules` / `pnpm-lock.yaml` / `.dsh-module-fallback`）。
- **安装动作**：`desktopPnpm.run()` 的 cwd = 活动 profile 目录（`src/pnpm.ts:163`），命令 `pnpm add <pkg>@<精确semver>`，随后写进 `dsh.profile.bundles`；卸载 = `pnpm remove` + 删 bundles 条目。profile 的 pnpm 配置：`nodeLinker: hoisted`、**全局 store `E:\.pnpm-store\v11`**、registry `registry.npmmirror.com`。
- ⚠️ **市场 UI 只能装 npm 上的包**（先查 `registry.npmjs.org/<pkg>/latest`；市场通道强制 `add <pkg>@<精确semver>`，`pnpm.ts:95-115`）→ **本地 tgz / `file:` 走不了市场 UI，只能走 CLI**（`runPlugin` 校验宽松；**该路径我未实测**）。
- 开发目录建议：**纯 ASCII**（如 `I:\dsh-plugins\<插件名>`），中文路径会重蹈坑 1。

### 建议用户先做的两个低风险验证
1. 装一个第三方插件 → 看是否出现卸载按钮（用来决定第 41 项要不要改）。
2. 关闭"手机连接" → 重启 → 确认侧边栏入口消失且 app 正常（用来决定第 40 项要不要改）。

---

## 【用户实测：装插件后点"立即重启"报错 —— 查证结论】（2026-09-15 14:28–14:5x）

用户装完 `@yolk_vat-y/dsh-project-memory@0.5.4` 后点「立即重启」→ 红字「无法请求重启 deepseek harness，请稍后手动重启。」，要求检查。

### 结论
1. **安装完全成功（磁盘实证）**：`E:\app\deepseekharness\data\profiles\desktop\package.json` 的 `dependencies` 与 `dsh.profile.bundles` 都写入了；`pnpm-lock.yaml` 有该包；**包实体在 `node_modules\@yolk_vat-y\dsh-project-memory\`**（含 `cordis.patch.yml` 353B + `client/` + `src/`）；扁平依赖 `pdfjs-dist`、`@napi-rs` 已装；`.pnpm` 只有 `lock.yaml`（hoisted 布局，正常）。
2. **app 健康**：`lifecycle-events\startup.jsonl` 显示 14:26 那次启动 `rendererStatus: healthy`、`health-snapshots` 已提交 → 插件没把 app 弄坏。
3. **重启报错 = 市场侧一次性令牌失效**（上游设计，非我方改动引起）：客户端 `restartError`（`MarketSettingsTab.tsx:878-880`）← `requestMarketRestart()` 被拒。四个候选原因里①405 同源校验（同一秒前的"安装"是同类 mutation，已过）②400 请求体（令牌 43 字符 base64url，校验只要 1–240 无 NUL）④profile 变化 —— 全部排除，**只剩③令牌失效**（`consumeRestartToken` 抛 `intent-expired`）。
   - **决定性推理**：令牌消费成功后市场是**先回 200 再**调桌面重启（`routes.ts:1149-1156`）→ 界面能显示**红字错误**就说明**令牌那步抛了、请求根本没到桌面**（若到了会弹原生确认框，界面不会有错误）。
   - 令牌失效的三个途径：**5 分钟 TTL**（`install/service.ts:28`）、**只存内存无持久化**、宿主 generation 回收时 `dispose()` 会 `clear()`（`:741-748`）；另外**点两次必报错**（第一次已消费）。
4. **时间线佐证**：profile 文件 mtime = **14:02:43–14:03:55**（= 安装时刻），而 `logs\dsh-2026-09-15.log` 的 run 记录里 **14:03 没有新 run**（只有 00:40/00:40/00:42/00:58/00:59/14:26）→ 那次重启确实没执行；14:26 是**手动重启**，之后健康。

### 顺带确认
- **第 41 项（卸载按钮）用户已实测确认：自己装的插件就有"卸载"** → 维持现状，**关闭该项**。
- **安装路径（用户要的）**：`E:\app\deepseekharness\data\profiles\desktop\` 是 Profile 目录；插件装在 `node_modules\<包名>\`；清单与 bundle 列表在 `package.json`；锁文件 `pnpm-lock.yaml`；全局 store `E:\.pnpm-store\v11`。
- ❗**我自己的一个失误**：第一次核查时我把包名打成连字符 `@yolk-vat-y`（真名是下划线 `@yolk_vat-y`），导致 Test-Path 报 False，一度误以为"包没落地"。逐字符列目录名后更正 —— **教训：scoped 包名里的 `_`/`-` 必须逐字符核对，别靠肉眼**。
- `data\desktop\desktop-market\state.json` = `{"version":1,"requested":"community-market","legacyDefaulted":false}` → 当前市场 provider 是我们改过的社区市场。

---

## 【用户三问：补丁 vs 直接改源码 / AA 同理 / 卸载卡死】】（2026-09-15 14:43–15:1x）

### 1. 概念更正（用户质疑得对，我此前的说法是错的）
- 用户问：既然只是参考社区版、改成自己用的东西，为什么有的东西要"打补丁"而不是直接改？
- **正确答案（实测）**：`patches/` **只用于以预打包 tarball 形式分发的代码**：① 官方内核 265 个 `vendor/dsh-runtime/<ver>/*.tgz`（tgz 是成品、仓库里无其源码，submodule 又决定不拉）② 第三方 npm 包（`app-builder-lib`/`open`/`fs-ext`/`pnpm`/`@vscode/ripgrep`，另 `dshmarket` 在 `.yarn/patches/`）。
- **我们自己的 4 个 workspace 包（`dsh-plugin-desktop`/`-beta`/`dsh-community-fabric`/`dsh-community-market`）都带完整源码、且没有任何 patch 引用它们 → 一律直接改源码**（我方 120+ 文件的改动就是这么做的）。
- ❗**更正我先前的错话**：我上一轮说"修重启按钮要新增补丁" —— **错**。重启令牌、卸载超时都在 `dsh-community-market` 源码里 → 属于"直接改"。
- ❗**AA 的口径也一并更正**：那套注入逻辑写在我们自己的 `dsh-plugin-desktop/src/profile.ts` 里 → **改得动**（直接改源码）。我之前说"不能当普通插件"不准确。准确说法：**要变成普通插件必须改我们自己这几处代码，代价是"每次追社区版都要重做这层减法"+ 两个客观约束（AA 不在 npm 上，不能走市场 UI 安装；client/Connector 载荷需要正确路径解析）**。

### 2. 卸载卡死（第 43 项）—— 根因已铁证
- **现场**：确认框永久停在「正在卸载…」，取消键禁用，8+ 分钟无变化。
- **铁证**：pnpm 子进程**至今仍活着** —— PID `37924`（`dsh-subprocess-local/lib/runner.js` 包装）→ PID `27064`（真正的 `pnpm.mjs`），命令行 `… pnpm.mjs --config.minimumReleaseAge=0 remove @yolk_vat-y/dsh-project-memory`，14:36:24 启动，至 14:44 未退出。
- **但它把活干完了**：包实体已删（`node_modules\@yolk_vat-y` 为**空目录**，0 条目）、`pnpm-lock.yaml` 已无该包、`package.json` 的 `dependencies` 已清空（全部落盘于 14:36:24）→ 属「活干完但进程不退出」或卡在收尾 prune。
- **界面为什么永远转**：市场 `runPnpm()` 只 `await handle.done`，**全链路零超时**（实测超时只在网络层 `restricted-http.ts`）；子进程不退 → 请求永不返回 → `operationPending` 永久为真，且 pending 时取消键被禁用。
- **⚠️ 后患（要紧）**：`runPnpm` 没返回 → 后续 `setProfileBundle(profile, pkg, false)` **没执行** → `dsh.profile.bundles` **仍列着已删的包**；而 `profile.ts:561-565` 对解析失败的 bundle 是 `else throw cause` → **下次启动会因找不到 bundle 而失败、进恢复助手**。必须先手工把清单改回来。
- 残留：`@napi-rs`、`pdfjs-dist` 仍在 `node_modules`（收尾 prune 未完成）→ 无害孤儿。
- **修的方式**：`runPnpm` 在我们自己的 `dsh-community-market` 源码里 → **直接改**（① pnpm 运行加超时+terminate ② pending 时显示耗时/可取消 ③ 启动自检 bundles 可解析、缺了就剔除并告警）。

---

## 【用户重新定义方针 + 全量盘点改动】（2026-09-15 15:00–）

### 1. 用户明确（**推翻 09-13 旧方针**）
原话要点：**我们只是参考和借鉴 DSH-desktop 的制作与更新** —— 它有好的、需要的东西，我们借过来改成自己的，**不是跟随它升级**；它升级后的东西**不一定是我们需要的**。我们只看 **官方更新之后，DSH-desktop 跟着更新了什么、添加了什么、优化了什么**，然后以此作参考借鉴。**我们已经改好了桌面版的更新方法**。
→ **"升级社区 v2.0.10 + 新建 `I:\dsh-915`" 暂时不需要存在**（第 39 项作废）。
→ "整个追社区版更新的流程都要重做" 这个说法也不成立 —— 我们不是在社区版代码上打补丁求生存。

### 2. 已同步的文档
- `MEMORY.md` §四「更新方针」→ 整节重写为新口径（含三条作废、以及"打补丁 vs 直接改"的事实边界）。
- `redolist.html` → 头部进度行、总览表第 39 项（标 ⛔ 已作废）、第 39 项详细块（改为"参考情报底账"）、执行顺序新增第 14 步、执行状态框全部更新。
- 新产出 **`.workbuddy/self-audit-2026-09-15.md`**（盘点报告）。

### 3. 全量改动盘点（实测规模）
`git status` **127 条**（96 modified + 27 deleted + 4 untracked）｜`git diff --stat` **121 文件**（+609 / −5605）｜`patches/` **21 个**。
- **A 类·产品定制（直接改源码）**：改名（`product-identity.ts`：productName→`deepseekharness`、appId→`deepseekharness.diy`）、便携模式（`main.ts` 重定向 `DSH_HOME`）、新市场源（`awesome-list.ts` + `awesome-catalog.ts` **2.1MB**）、本地更新源（`update-checker.ts`/`update-download.ts`）、去市场 tab、删废弃、侧边栏两列、会话命名+中文转义、B 类测试修补 + 第 1 项补遗。
- **B 类·叠补丁（21 个，仅限无源码的成品包）**：社区自带 13 官方 + 5 第三方；我方新增 3（session-controller / session-persistence-jsonl / client-ui-sidebar）。
- **C 类·清理**：删 `_deprecated/`(25 文件)、3 张零引用图、悬空 submodule gitlink、孤儿 tgz。

### 4. ⚠️ 盘点发现的问题（8 条，详见 `self-audit-2026-09-15.md`）
**P1 身份冲突（最要紧）**：
1. **`AGENTS.md`（AI 工具自动加载的仓库规则）**第 1 行 `# DSH Desktop repository rules`、第 3 行称上游 "**unmodified** DeepSeek Harness checkout"、第 106 行还教 `git submodule update --init --recursive`（我们已决定不拉）→ **与事实直接矛盾，且会误导任何在此仓库工作的 AI**。`CLAUDE.md` 只有一行 `AGENTS.md`（指向它）。
2. `package.json:4` `"description": "DSH Desktop product workspace"`（改名漏项）。
3. 根文档整份仍是社区的：`README.md/.en.md/.zh.md`（下载指向 `dshdesktop.cn`、社区微信/QQ 群、Discord）、`PRIVACY.md/.zh.md`（"我们指 Anywhere Labs 团队"；**我方只加了 1 行**市场源说明）、`CONTRIBUTING*`、`CODE_OF_CONDUCT*`、`docs/`(16 文件)、`.github/`(5 文件：CI + issue/PR 模板)。
4. 版本号沿用社区 **2.0.9**（自己发版会与社区同号，更新源语义混淆）。
**P2 结构冗余**：`dsh-plugin-desktop-beta`（`node_modules` **967.9 MB** + src 181 文件）仍在 root `build`/`typecheck`/`test`/`check` 与 7 个 `*:beta` 脚本里；`.agents/notes/` **约 97 个**社区设计笔记。
**P3 补丁继承未审**：13 社区 + 5 第三方补丁**从未逐个审"是否需要"**（含疑与官方内置重复的中文化补丁）。
**P4 已确认合理**：我方 3 个补丁打官方包＝技术必然；`vendor/`+`upstream.json` 需保留（角色应正名为"官方内核的本地分发副本"）。

---

## 【用户 6 项指令：重写 AGENTS.md / 版本号自有 / 根文档改写 / 补丁审查 / 问 beta 与 .agents】（2026-09-15 15:19–）

用户下 6 条：①重写 AGENTS.md ②版本号走自己的、记录每次对齐社区的版本 ③根文档全改写成自己的 ④所有补丁逐个审查 ⑤问 beta 包是什么 ⑥问 `.agents/notes` 是什么。

### 已完成
- **① `AGENTS.md` 已重写**（`I:\dsh-913\AGENTS.md`，原 31 行全英文社区规则 → 新中文规则）。删掉项：`# DSH Desktop repository rules` 标题、`unmodified DeepSeek Harness checkout`、"先在 beta 里开发再同步 stable"、`git submodule update --init --recursive` 指令、社区包职责描述。写入项：我们自己的仓库定位（不跟随社区版升级）、纯 ASCII 路径约束、不拉 submodule、只发 stable、`dist:win` 而非 `package:dir`、打包必设 `DSH_AA_SOURCE_REF=pinned`、**直接改源码 vs 叠补丁的边界表**、已停用的上游做法、五处关键路径（工作副本/清单/打包脚本/更新源/记忆目录/社区参照）。`CLAUDE.md` 保持一行 `AGENTS.md`（指向，内容本就正确，未动）。
- **④ 补丁逐个审查完成** → 新文档 `.workbuddy/patch-audit-2026-09-15.md`。21 个补丁定性 = **必须保留 14**（7 个官方功能命脉 + 4 个第三方 + 我方 3）+ **ASAR 依赖 4 个（现在必须留）**（`dsh-agent-presets`/`dsh-app-boot`/`dsh-plugin-package-inventory-deepseek`/`vscode-ripgrep`）+ **真正待用户定 3 个**：`dsh-client-ui-primitives`（把官方宽表格滚动条从"悬停才现"改成"常显"，属**改官方 UI 观感**）、`dsh-settings`（恢复 alpha.2 前旧插件 API 作兼容）、`dsh-client-modules`（换行统计改 `indexOf`，纯性能 1 行）。
  - 附带发现：`dsh-client-ui-directory-picker-browse` 补丁里 `"browser.nativePicker"` 的中文文案行尾引号形态异常（英文那条完好）；因该补丁此刻**能正常 apply**（安装成功、功能可用）→ 判定"不用动"，但换内核重做时要留意。
- **⑤ beta 包 = `dsh-plugin-desktop-beta`**：版本 `2.0.9-beta.1`、productName `DSH Desktop Beta`、appId `ai.deepseek.dsh.desktop.beta`；**第二条发布通道的完整产品包**（src 181 文件，与 stable 等量）。与 stable **可同机安装**（独立 app 名、系统标识、快捷方式、单实例锁、**Electron user-data 目录**），但**共享 `DSH_HOME`**（profile/settings/plugins/sessions 共用）；自动更新走 beta 通道。作用 = 新版内核/新功能先在隔离实例里试，不影响正式版；社区还把它当"新 Desktop 功能的先行开发区"（原 AGENTS.md 第 18 行）。**我们已停用 beta → 对我们是冗余**，但仍在 root `build`/`typecheck`/`test`/`check` + 7 个 `*:beta` 脚本里。
- **⑥ `.agents/notes` = 社区的设计决策笔记**（ADR 风格）：`implemented/`（85 文件，分 architecture / process）+ `proposed/`，双语；无 README。**作用 = 给 AI agent 读的设计上下文** —— 原 AGENTS.md 第 30 行以 "the owning Agent Note" 引用它（例：`.agents/notes/implemented/process/2026-08-15-pinned-upstream-and-isolated-yarn-workspace.md`）。不是代码，不参与构建。

### 待用户定（②③ 需先确认细节才能动手）
- **② 版本号起始值**：有个硬约束 —— **新版本号必须 > `2.0.9`**，否则已装的 2.0.9 会认为"新版更旧"而不更新（`app.getVersion()` 参与比较）。建议起始 `3.0.0`。另需建记录文件（建议仓库根 `UPSTREAM-ALIGNMENT.md`）：我们的版本 ↔ 对齐的社区版 ↔ 官方内核 commit ↔ 日期。
- **③ 根文档改写范围**：`README.md/.en.md/.zh.md`(+i18n)、`PRIVACY.md/.zh.md`(+i18n)、`CONTRIBUTING.md/.en.md`、`CODE_OF_CONDUCT.md/.en.md`、`docs/`(16 文件)、`.github/`(5 文件)。需用户确认 **"是否对外发布"** —— 这决定 PRIVACY 与 CODE_OF_CONDUCT 是"精简重写"还是"直接删"。

### 已清理
本轮临时文件 `_q5.txt`、`_q6.txt`、`_patch_all.txt` 已删。

---

## 【用户第二轮 6 项指令：GitHub / 版本号 3.0.0 / 文档策略 / beta 摘除 / 补丁决定】（2026-09-15 16:06–）

### 用户决定
1. **GitHub**：用户 GitHub = `https://github.com/dashboard`；原话"我们的东西，除了修改完之后的完整文件和文件夹，其他都放在github上" → **上传范围待澄清**（歧义点见下）。
2. **版本号从 `3.0.0` 开始** ✅（本轮已执行）。
3. **README / PRIVACY 纯自用** ✅（按"纯自用"改写，待执行）。
4. **CODE_OF_CONDUCT**：待用户决定（本轮已解释它是什么）。
5. **CONTRIBUTING 要两份**：一份**完整**（供用户参考学习）+ 一份**纯自用**（写给自己 / AI），且**AI 以后只能用纯自用那份**。
6. **docs/ 16 个**：待用户决定（本轮已说明内容）。
7. **beta 不要了** ✅（已核实可安全摘除，方案已出待点头）。
8. **`.agents/notes` 保留供人学习，但保证 AI 不使用不受干扰** ✅（本轮已写入 `AGENTS.md`）。
9. 三个补丁（`dsh-client-ui-primitives` / `dsh-settings` / `dsh-client-modules`）**暂时保留**，以后再考虑 ✅（已更新审查文档）。

### 本轮已执行（实测）
- **版本号 → `3.0.0`**：`dsh-plugin-desktop/package.json` 的 `version` `2.0.9`→`3.0.0`；同文件 `description` 改为 `deepseekharness: our own Electron desktop shell composed as a DeepSeek Harness Cordis plugin`；根 `package.json` 的 `description` 由 `"DSH Desktop product workspace"` → `"deepseekharness product workspace (our own desktop build)"`。
  - 核实：`dsh-community-market` / `dsh-community-fabric` 的 version 均为 `0.1.0-dev.0` 且 `private: true` → **与我们产品版本无关，未动**。根 `package.json` 无 `version` 字段。
- **新建 `I:\dsh-913\UPSTREAM-ALIGNMENT.md`**：版本对齐记录（首行 = `3.0.0` ↔ 社区 `2.0.9` ↔ 内核 `0.1.5-rc.1` / commit `183f08e9`）+ 5 条规则 + 升级号建议。
- **`AGENTS.md` 增补**：新增 `## 五、版本号` 一节；新增铁律"**不要读 `.agents/` 目录**（社区设计笔记，仅供人参考；AI 不得读 / 引用 / 当作规则来源）"；相关位置表加 `UPSTREAM-ALIGNMENT.md`。
- **`patch-audit-2026-09-15.md`** 第四节改为"已定 —— 三个都暂时保留"。
- **`redolist.html`** 加第 44–49 项（44 补丁审查完毕 / 45 版本号 / 46 beta 摘除 / 47 根文档改写 / 48 `.agents` 隔离 / 49 GitHub 待澄清）+ 头部进度行更新。

### beta 摘除：已核实可安全摘除（实测）
- **beta 与 stable 的 `src` 完全同构**：均 181 文件；**只在 stable 有的 0 个、只在 beta 有的 0 个**；**28 个文件内容不同** —— 差异全部是"我们只改了 stable"造成的（文案类 `tray-locale` / `native-dialog-copy` / `recovery-copy` / `setup-wizard-copy` / `desktop-settings-locales` / `notifications` + `product-identity` / `update-checker` / `update-download` / `main` / `client/DesktopFrameTitlebarView.tsx` 等）→ **beta 里没有 stable 缺的东西**。beta 非 src 部分也无独有顶层条目。
- **引用点（摘除必改）**：`package.json`（workspaces 1 处 + build/typecheck/test/check 4 处 + 7 个 `*:beta` 脚本 + `check:desktop-variants` 注释）、`scripts/verify-layout.mjs`（6 处）、`scripts/prepare-agents-anywhere-release.mjs`（2 处数组）、`scripts/verify-desktop-variants.mjs`（整脚本针对 beta）、`upstream.json`（`channels.beta` + **`activeChannel` 现为 `"beta"`**）、`yarn.lock`（install 自动重算）；文档类 `docs/architecture*.md` / `README.md` / `AGENTS.md` 随根文档改写一起处理。
- ⚠️ `upstream.json` 的 `activeChannel` 被 `sync-vendored-runtime.mjs:35` 与 `verify-layout.mjs:24/25` 读取 → **摘 beta 时必须同时把 `activeChannel` 改成 `stable`**，否则校验会拿到 `undefined`。

### 待澄清（阻塞项）
**GitHub 上传范围** —— 用户原话"我们的东西，除了修改完之后的完整文件和文件夹，其他都放在github上"有两种读法：
- **读法 A**：代码（修改完的完整文件/文件夹）**留本地**，文档 / 方案 / 记录等**上传**；
- **读法 B**：代码**上传**，"完整文件和文件夹"指**打包产物 / 安装包**（不上传）。
两者动作完全相反 → **必须先问清**。另需：仓库名、公开/私有、是否要我新建仓库。

---

## 【用户第三轮指令：GitHub 定案 / docs 清理 / beta 摘除执行】（2026-09-15 16:22–16:4x）

### 用户决定（本轮）
1. **GitHub**：上传 = **"文档 / 方案 / 清单 / 审查报告 / 记忆（`.workbuddy` 那套 + 说明文档）** 和 **代码**"都传；**不上传"完整文件和文件夹"= 安装包那种大产物**（即上一轮的**读法 B**）。**公开仓**（因为私有仓读取被限制）。**仓库名 `deepseek-DIY`**。用户 GitHub：`https://github.com/dashboard`。
2. **`docs/` 保留 3 块**（`architecture` / `plugin-development` / `evidence`），其余 10 个文件删除 ✅ 已执行。
3. **CONTRIBUTING 两份** → 按我建议做（`CONTRIBUTING.md` 自用版 = **AI 只读这份**；`docs/contributing-full-reference.md` 完整版 = 仅供人参考、**AI 不读**）→ ⏳ 待执行（约定已写入 AGENTS.md）。
4. **beta 摘除批准**（含删目录）；用户补充："以后有什么需求，都从 github 上查找和临时拉取和存放" ✅ 已执行。
5. **CODE_OF_CONDUCT**：本轮未回答 → **仍需确认**（我的建议：删）。

### 本轮已执行（全部复验）

**A. beta 整体摘除**
- 改 6 处：根 `package.json`（workspaces 去掉 beta；`build`/`typecheck`/`test`/`check` 各去 beta 段；删 `check:desktop-variants` 注释+脚本；删 7 个 `*:beta` 脚本）、`scripts/verify-layout.mjs`（8 处 beta 引用）、`scripts/prepare-agents-anywhere-release.mjs`（2 处数组）、`upstream.json`（删 `channels.beta` + `activeChannel` `beta`→`stable`）。
- 删文件 `scripts/verify-desktop-variants.mjs`；删目录 `dsh-plugin-desktop-beta/`（**973.6 MB / 54479 文件**）。
- **复验**：`yarn install` **离线成功**（`Resolution 0s404ms` / `Fetch 0s647ms` / `Link 2s120ms` / `Done with warnings in 3s431ms`；`YN0085` 提示 beta 的 AA resolution 已清理；3 个 `YN0008 must be rebuilt`（fs-ext / koffi / node-pty）= 依赖树变化后的正常标记）✅；`yarn.lock` **无 beta** ✅；`node_modules\dsh-plugin-desktop-beta` **不存在** ✅；`yarn typecheck` ✅。
- ⚠️ **踩到的坑**：`scripts/bilingual-docs.mjs:87` 用 **`git ls-files`** 列 `*.i18n.yaml` → 目录删了但 git 索引还在 → `ENOENT`。**解法**：`git add -A -- dsh-plugin-desktop-beta`（**380 条 staged 删除，只限该路径**，其他改动未动）。复验 `check:bilingual-docs` = **49 records / 98 documents 一致** ✅（原 51 / 102）。
- 摘除前证据：beta 与 stable `src` **181 vs 181、只在一边有的各 0 个**、28 个文件内容不同（**全是"只改 stable"造成的差异**）→ **零内容损失**。

**B. `docs/` 清理**
- 删 10 个：`faq.md/.en.md`、`plugin-ecosystem.md/.en.md`、`docs/README.md/.en.md`、`user-guide.md/.en.md`、`why-desktop.md/.en.md`。
- 保留：`architecture.md/.en.md`、`plugin-development.md/.en.md/.i18n.yaml`、`evidence/`（2 md + 2 png）。
- **复验**：10 个文件 `Test-Path` 全 False ✅。

**C. `AGENTS.md` 再更新**（beta 摘除后）：去 beta 相关内容；新增"`check:layout` 目前跑不通（预期）"说明；第五节改名「版本号与文档分工」并写入 CONTRIBUTING 两份的约定。

**D. `redolist.html`**：46 → ✅ 已执行并复验；新增 50（docs 清理 ✅）；47 改「◐ 部分完成」；头部进度行更新。

### 剩下要做的（下一轮）
- `README`（3 个 + i18n）纯自用改写、`PRIVACY`（2 个 + i18n）纯自用改写
- `CONTRIBUTING.md`（自用版）+ `docs/contributing-full-reference.md`（完整版）
- `CODE_OF_CONDUCT`（**待用户定**）
- GitHub 建公开仓 `deepseek-DIY` + 推送（**外发动作 → 需先给用户确认目录组织方式**）

