# 2026-09-16　工作日志

> 前一日（09-15）的工作在 `2026-09-15.md`。本文件从此日起，**追加式**记录。

## 00:11　N8-a/b/c 复述 · N3 暂缓 · N8 前置验证步骤查证

**用户问**：① N8-a/b/c 分别是什么（忘了，要重讲）② 绿泡泡暂时不做 ③ N8 前置验证手动怎么做。

### 1) N8-a/b/c 重讲（已写进 `redolist2.html` 自解释表格）
- **N8-a** = **以后怎么把「手机连接」装回来**（市场界面装不了本地包 → 终端装/界面卸？还是另做界面入口？还是先不管？）
- **N8-b** = **AA 那个 522 KB 的 tgz 以后放哪**（`I:\dsh-plugins\` / 更新源 / 你指定）
- **N8-c** = **要不要把它传到 GitHub**（第三方 MIT 包，含 Python 脚本）

### 2) N3 绿泡泡 → 暂缓，移出开工清单
用户定：「暂时不做了，等我考虑好了再说」→ 不是"不做"，是"不在本轮"，**不阻塞**。清单里已划掉并标注。

### 3) ⭐ N8 前置验证：查证后**卡口基本解除**
**读源码所得（有据可依，非推断）**：
- `@deepseek-ai/dsh/lib/plugin-Ddi42qoW.js` 是官方 `dsh plugin` 的实现，**注释原文点名 tarball**：
  「a git/path/**tarball**/alias spec on the command line reconciles by its **true package name**」
  → **本地 tgz 是被明确支持的**。
- 它还实现 `reconcilePlugins()`：装完后**按已安装状态对账 `dsh.profile.bundles`**（声明了 `dsh.bundle` 的依赖自动进 bundle 列表；被删或不带 bundle 的自动移出）。
- 另有 `anchorPathSpec()`：相对路径（`.` / `../x` / `file:` / `link:`）会**锚定到调用方 cwd**；**绝对路径原样透传** → 用绝对路径的 tgz 最稳。

**⭐ 另一个关键发现：app 内置终端里本来就有 `dsh` 命令**
- app 生成 `data\desktop\host-commands\<profile>\generations\<hash>\bin\dsh.cmd`，内容已自动设好
  `ELECTRON_RUN_AS_NODE=1` + `DSH_HOME=<安装目录>\data` + `DSH_DESKTOP_DEFAULT_PROFILE=desktop` → 调 `app.asar\lib\desktop-cli.js`；
  内置终端会把这个目录**前置到 PATH** → **打开终端就能直接敲 `dsh`**。
- 另有 `data\desktop\cli\<hash>\bin\` = **dsh + node + pnpm 三件套**；`runtime-commands\generations\...\bin\` 只有 `pnpm.cmd`。
- 市场那条路（`validatedExternalMarketArgv`）**强制 `add <pkg>@<精确 semver>`**，不认 tgz → 再次印证"市场装不了本地包"。

**⚠️ 顺带查清一条约束**：`DSH_HOME` 是**无条件覆盖**的（`main.ts:387-391` 注释 `Deliberately unconditional`；只有恢复助手的数据目录切换器能覆盖）→ **不能用外部环境变量跑测试**，要用 app 自己的 **Profile 切换 UI**（设置 → Profile 可创建/选择）。

**手动验证步骤（7 步，已写进清单）**：备份 → 界面新建 `aatest` profile（**不碰现有 `desktop`**）→ 内置终端 `dsh --help` 探路 → `dsh plugin --profile aatest add "<tgz 绝对路径>"` → 查 `package.json` 是否**自动**出现依赖 + bundles → 切到 `aatest` 看侧边栏是否出现「手机连接」→ 再 `remove` 验闭环 → 删测试 profile。

### 本轮未做
**没有动任何产品代码、没有跑验证**（验证要联网拉依赖 + 需要你在 app 里操作）→ 只做查证 + 清单更新。

### 落档
`redolist2.html`：新增「📌 N8 的 3 项决策（自解释版）」表 +「🧪 N8 前置验证 · 怎么手动验」7 步表 + 两个发现框；N3 划掉并移出；开工清单的「当前卡在哪」改为只剩两件。

## 00:20　答疑：Profile 是什么 + `desktop` 这个词出现在哪几处

用户发来设置里「Profile」区块截图（含「desktop 当前 / 可使用」+「Profile 名称」输入框 + 创建按钮），问：① 是不是在这里手动做验证 ② Profile 是什么 ③ 为什么图里有 `desktop` 字样。

**本次现场取证（磁盘 + 源码）**：
- **磁盘上只有一个 profile：`desktop`**（`E:\app\deepseekharness\data\profiles\desktop`）。目录内含：`package.json`（清单）、`node_modules\`（插件实体）、`cordis.patch.yml`、`cordis.yml`、`pnpm-lock.yaml`、`pnpm-workspace.yaml`、`.dsh-module-fallback\`，以及我们昨天做的备份 `package.json.bak-20260915-1715`。
- ✅ **顺带实证 N2 已生效**：它的清单现在正是修好后的状态 ——
  `"bundles": ["@deepseek-ai/dsh-base", "@deepseek-ai/dsh-web-app"]`（脏的 `@yolk_vat-y/dsh-project-memory` 已不在）。
- **`desktop` 这个词在三处出现（易混）**：① **Profile 名 = `desktop`**（= 截图里那个，上游默认名）② **`data\desktop`** = Electron 的 **userData 缓存目录**（`main.ts:392` `app.setPath('userData', join(portableDataDir,'desktop'))`，**不是** profile）③ **`desktop-safe-mode`** = 安全模式派生的 profile 名（`safe-mode.ts:27` `DESKTOP_SAFE_MODE_PROFILE_NAME`）。
- ⇒ **截图里的 `desktop` 就是清单 N7 要改的那个名字**（用户前定"下次更新时一起做"）。

**答复要点**：① 是，截图就是验证第 1 步的入口（填 `aatest` → 创建）② Profile = 一套独立配置环境（各自独立的清单 + 插件目录 + 补丁层 + 依赖锁），可有多套、启动时选一套，切换要重启 ③ 给出"Profile 存放结构"示意图（visualizer）。

## 00:30　答疑：图1「系统提示词」是什么（**不是新增**）+ 右侧栏功能

**用户**：手动验证放到最后先不做；问 ① 截图里「系统提示词」是什么、是否这次更新后才有 ② 右侧栏有什么功能。

### ① 「系统提示词」= 官方「轨迹」(Trajectory) 视图的一个标签页
- 出处：`@deepseek-ai/dsh-client-ui-trajectory`，键名 `tab.systemPrompt` = 「系统提示词」。同视图另有：概述/原始输出/预览/原文/来源/参数/结果/Schema/计时/差异/**系统提示词**/工具/选项/用量。
- 内容 = **这次请求真实发给模型的系统提示词原文**（截图里的 `You are an AI agent powered by DeepSeek Harness.`、`coding agent powered by the deepseek-flash model`、`@` 工作区路径说明、非零退出码说明都是真内容）。「已思考」= thinking 折叠。

### ② ⚠️ 关键结论：**它不是这次更新新增的（有决定性证据）**
- 社区历史：`dsh-client-ui-trajectory` 于 **`588bef5dec`（2026-08-28）** 进 `dsh-plugin-desktop/package.json`；`git merge-base --is-ancestor` 判定**已包含在 `v2.0.4`（2026-08-29）**。
- 版本对照：`v2.0.4` 内核 = `0.1.2-alpha.1`；`v2.0.9` 内核 = `0.1.5-rc.1`（同一批 UI 包同步升版）。
- **实测对比**：从 npmmirror 下载 `dsh-client-ui-trajectory@0.1.2-alpha.2`（旧线）与 `@0.1.5-rc.1`（现装）解包逐项比对 → **两者都含「系统提示词」，`tab.*` 标签列表完全一致**（各 6 处命中）。
- ⇒ **功能不是新的**；用户之前那版就有，可能只是没注意，或因内核跨大版本（alpha→rc）导致外观/入口有变化。
- ⚠️ **边界（未验）**：「入口按钮的位置有没有变」**没验** —— 需要 2.0.4 实机，现在没有。不猜。

### ③ 右侧栏 = `dsh-client-ui-sidebar-right`（是个**容器**，不是单一功能）
- **容器能力**（读包内文案键）：打开/收起侧边栏 · 全屏/退出全屏 · **多标签页**（新标签/关闭）· **分栏**（最多两格，宽度不足提示"拖宽侧边栏后再分栏"）· **拖拽放置**（移到这里/左分栏/右分栏/上分栏/下分栏）· **浮动**（拖出来浮着，可"收回到侧边栏"）· 空面板 · "这类内容还没有可用的查看方式"。
- **往它里面注册内容的包（现装）**：自带 **「开始」引导页**（`tab.guide.title`）｜`dsh-client-ui-sidebar-files` = **工作区文件**（`guide.title`）｜`dsh-client-ui-sidebar-documentpreview` = **文档预览**｜`dsh-client-ui-chat` = **会话内容**。
- 左侧栏家族则是 `dsh-client-ui-sidebar`（我们打过补丁改两列布局）+ `sidebar-files` + `sidebar-documentpreview`。

### 落档
结论写入本日志；临时对比包已清理（`%TEMP%\traj-cmp`）。

## 00:40　答疑四问：系统提示词能否中文化 / `@` 功能 / 右侧栏能否直接改文件 / 轮次导航

**用户四问**：① 系统提示词的英文能不能改成中文 ② 输入框里 `@` 有没有功能 ③ 右侧栏能不能像 WorkBuddy 那样直接在文件/代码/文档上修改 ④ 有没有像 marvis 那样的对话记录查找。

### ① 系统提示词 = **4 个官方包的内容拼起来的**（已逐段溯源）
| 截图里的段落 | 来自 |
|---|---|
| `You are an AI agent powered by DeepSeek Harness.` | `@deepseek-ai/dsh-system-prompt`（`lib/index.js:216`，代码里的默认段落） |
| `You are a coding agent powered by the {{model}} model.` + `Your working directory is {{cwd}}.` | `@deepseek-ai/dsh-agent-presets` 的 preset `persona` 行（`presets/standard/agent.cordis.yml:24-29`，**整文件 255 行 / 12.9 KB**；`cordis` preset 266 行 / 14.0 KB） |
| `Tokens prefixed with @ are workspace paths…` | `@deepseek-ai/dsh-file-reference` |
| `Non-zero exits are reported as '[exit code: N]'…` | `@deepseek-ai/dsh-tool-bash` / `dsh-tool-pwsh` |
**改法三选**：**(A) 用产物自带补丁层 `cordis.patch.yml` 覆盖 `persona` 那一行的 config**（零补丁、可回退；⚠️ 按 id 的 patch 是**整体替换**，必须重述该行所有字段 —— 已知坑 11）｜**(B) 叠补丁**改 4 个官方包（可全中文化，但要维护补丁、工作量随译文线性增长，其中 `dsh-agent-presets` 我们已有补丁）｜**(C) 不改**。
⚠️ **必须提示的风险**：系统提示词是模型的"工作说明书"，**改中文可能影响表现**（英文指令是主流、模型训练更充分）。且它属"借用官方内核"的部分，改动要长期维护。⇒ 建议：只想"看得懂"就用 A；要"全中文"才走 B，且**改完必须实测对比效果**。

### ② `@` 是**真功能**（不是装饰）
- 输入框 placeholder 原文：「发消息或创建任务, / 调用指令, **@ 文件或对话**」（`dsh-client-ui-conversation`）
- `dsh-client-ui-chat` 有 `message.referenceSummary` = 「**引用会话 · {labels}**」
- `dsh-client-ui-input-trigger` 是它的选择器实现（含 `reference`）
- 系统提示词里也定义了它的语义 → `@` = **显式引用工作区文件/目录/会话**，插进去后作为引用喂给模型。

### ③ 右侧栏**不能直接编辑**（按文案+代码线索判断）
- `sidebar-files` = 「工作区文件」（浏览）｜`sidebar-documentpreview` = **预览**（代码里有 `readOnly` / `preview`）
- 全仓搜「保存/编辑」中文，命中的全是**消息队列编辑**（`queue.edit` = 编辑排队消息）、**工具名**（`tool.title.edit`/`write`）、**反馈** —— **没有一条是"在侧栏编辑文件并保存"**
- ⇒ 文件/文档在右侧栏是**只读**；改文件要靠 AI 调工具（`edit` / `write`）。
- ⚠️ **边界**：以上是**线索判断**，未实机点验；不排除看漏。

### ④ ✅ **有**，官方叫「**轮次导航**」
- `chat.turnNavigation.label` = 「**轮次导航**」｜`chat.turnNavigation.jump` = 「**跳转到第 {turn} 轮**」｜`chat.turnNavigation.jumpLoad` = 「**加载并跳转到第 {turn} 轮**」（**未加载的历史也能跳**）｜`chat.turnNavigation.turn` = 「第 {turn} 轮」
- ⇒ 这就是图 2 那个界面（一列历史消息，点了跳过去），**官方自带**。
- 另注：`search.*`（「{shown} 处匹配 · {files} 个文件」）属**搜索结果渲染**，不是"对话记录搜索框"。

## 00:50　四件事：系统提示词放弃 · `@` 没反应定因 · 轮次导航条件 · 官方记忆功能

**用户**：① 系统提示词不改了，放弃 ② `@` 没反应，查原因 ③ 轮次导航是不是因为上下文不够 ④ 把右侧栏进化加进清单 ⑤ 官方源码有没有记忆功能。

### ① 系统提示词中文化 = ⛔ **放弃**（用户定）
已写入清单「已明确放弃、不入清单」框（避免以后重复讨论）；理由与 4 个来源一并留档在 `tech-notes.md §七`。

### ② `@` 没反应 —— **找到两个原因（均有出处）**
1. **语法要求**：`dsh-file-reference` 的 `@` 词法正则 `/(?:^|\s)(@([^\s]*))$/` → **`@` 前面必须是行首或空白**。中文后**不加空格**直接打 `@` → **不触发**。
2. **候选为空**：候选 = `remote.fileReferences.list(sessionId, query)`（取**该会话绑定工作区**的文件）+ `sessionReferenceResolver`（会话引用）。**实测用户唯一会话的工作区 = `I:\deepseekharness\AI股票`**（`data/storages/workspace.json` 确认），而**该目录一个文件都没有** → 文件候选空；会话引用因**只有 1 个会话**也空。
- 插件本身**是挂载的**：`dsh-web-app/cordis.patch.yml:286-301` 有 `ui-input-trigger` / `ui-commands` / `ui-reference`；`ui-reference` 的 `trigger: "@"`。
- ⚠️ **未验**："候选为空时 UI 是完全不弹还是弹空菜单"—— 未实机验，不猜。
- **给用户的测试法**：在**空输入框最开头**（保证行首）打 `@`；并往工作区放个文件（或换一个非空目录）再试。

### ③ 轮次导航的显示条件 = **至少 2 轮**（决定性）
`dsh-client-ui-chat` 的 `TurnNavigatorRail` 里有一行 **`if (items.length < 2) return null;`** → 少于 2 轮不渲染。
⇒ 用户"上下文不够"的直觉**方向对、原因不对**：不是长度，是**轮数 < 2**。

### ④ 「右侧栏进化」= ✅ **已加入清单 丁组 N30**
动机（只读→想能直接看/改文件·代码·文档）／现状取证／形态待定／**落点待查（两个方向：改官方包叠补丁 vs 自制新标签页插件）**／验证待补／备注"不阻塞当前开工"。总览丁行与开工清单丁行同步。

### ⑤ ⭐⭐ 官方**不内建记忆功能** —— 走 **MCP 外挂第三方记忆服务**
- 官方 `packages/` 54 个目录**无 memory 包**；本地 265 个官方包名**也无**。
- 官方文档 **`docs/user/guide/mcp-memory.zh.md`「连接第三方记忆 MCP 服务」**，提供**三份默认关闭的参考配置**（`apps/cli/config/examples/mcp-memory/`），经 `@deepseek-ai/dsh-mcp-client` 接入 **Memorix** / **MCP Reference Memory** / **Engram**（均 stdio）。
- DSH 只负责"解析 overlay → 起子进程 → 发现工具 → 以 `mcp__<serverName>__<tool>` 暴露"；**交付组合不含记忆服务器**，不传 `--patch` 就全部关闭。
- **接法**：`dsh web --patch <yml>`；**持久化 = 把 `insert` patch 合并进用户补丁层** `$DSH_HOME/profiles/<名>/cordis.patch.yml` 或 `$DSH_HOME/cordis.patch.yml`。⇒ **接记忆服务不需要改一行代码。**
- 来源：GitHub MCP（`deepseek-ai/deepseek-harness`）— `packages/` 目录清单 + `search_code` + 读取 `docs/user/guide/mcp-memory.zh.md`。

### 落档
`tech-notes.md` 新增 **§七「UI / 功能溯源」**（6 小节：系统提示词来源/`@`机制/右侧栏/轮次导航/记忆 MCP），并把原 §七 顺延为 §八（⚠️ 中途误删过 §八 标题，已修正并复核章节顺序为 〇一二三四五六七八）；`redolist2.html` 丁组新增 **N30**、总览与开工清单同步、新增「已明确放弃」框。

## 01:00　答疑：接入记忆服务后会不会出现「记忆管理」界面 → **不会**（已取证）

**用户问**：接入 Memorix / Reference Memory / Engram 之后，设置里会不会出现一个管理"记忆"的功能键（像 WorkBuddy 那样）？

**结论：⛔ 不会。** 证据（四条，均读源码/包元数据所得）：
1. **全仓 UI 包搜「记忆」二字 → 零命中** ⇒ 界面上根本没有"记忆"这个概念。
2. **没有任何 UI 包处理 `mcp`**（`dsh-client-ui-*/lib/client.js` 搜 `mcp__` / `"mcp"` / `mcpServers` → 全空）。
3. **`dsh-mcp-client` 是纯 host 侧插件** —— 其 `package.json` **无 `dsh.client` 字段** ⇒ 没有界面。
4. 设置里只有 5 个设置类包：`settings`（通用）/ `settings-models`（模型）/ `settings-plugins`（插件）/ `settings-plugin-inventory`（插件清单）—— **无 MCP 页、无记忆页**。

**记忆的真实形态** = **模型工具** `mcp__<serverName>__<tool>`。能"看到"它的地方是**轨迹视图 →「工具」标签页**（`tab.tools` = 「工具」；`record.toolsMissing` = 「本次请求没有工具」，列的是**本次请求可用工具**）。⚠️ 此条为**按代码推断，未实机验**（要真接一个 MCP 才能确认渲染）。
**管理只能靠**：① 直接编辑提供方存储文件（如 `~/.dsh-mcp-reference-memory.jsonl`）② 让模型调其工具查改。
⇒ **"像 WorkBuddy 那样的记忆管理界面" = 官方没有 → 自制功能候选**（与 N30 右侧栏进化同类）。**已向用户提出"要不要记进清单"，等其表态。**

**落档**：`tech-notes.md §七` 新增第 7 小节。

